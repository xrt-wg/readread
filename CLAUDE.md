# ReadRead — Project Guidelines

## 颜色使用规范

所有组件中的颜色必须通过 CSS 变量（`var(--*)`）引用。变量定义在 `src/index.css` 中。

### 规则

1. **使用现有 CSS 变量**：新增颜色前先检查 `:root` 中是否已有对应语义的令牌
2. **新增变量需同步定义**：如需新增颜色变量，必须在 `:root` 和 `[data-theme="night"]` 中**同时定义**
3. **允许硬编码的例外**：
   - 红色系：`#dc2626`、`#ef4444`、`#b91c1c`（错误/危险操作）
   - 绿色系：`#16a34a`、`#059669`、`#15803d`（成功/确认操作）
   - 琥珀色系：`#b45309`、`#92400e`（警告）
   - 金色强调色：`rgba(196,154,60,...)` 系列（品牌 accent）
   - 反馈按钮的语义色
4. **阴影规范**：
   - 日间模式：阴影使用 `var(--card-shadow)` / `var(--card-shadow-hover)` / `var(--popup-shadow)`
   - 禁止在组件中新增硬编码的 `rgba(28,25,23,...)` 阴影
   - 禁止在组件中新增硬编码的 `#xxxxxx` 颜色值（例外除外）
5. **hover 暗色**：统一使用 `var(--btn-hover-bg)`，不再使用 `#2d2926`

### 速查：常见硬编码 → 正确变量

| 避免 | 使用 |
|---|---|
| `#ffffff` | `var(--card-bg-warm)` |
| `#fdfaf5` | `var(--popup-bg)` |
| `rgba(28,25,23,0.06~0.12)` 边框 | `var(--border-subtle)` |
| `rgba(28,25,23,0.04~0.08)` 背景 | `var(--hover-bg)` |
| `rgba(255,255,255,0.6)` | `var(--surface-bg)` |
| `#2d2926` | `var(--btn-hover-bg)` |
