# 翻译服务框架重设计 — 实施方案

- **版本**：v1.0
- **日期**：2026-04-01
- **依据**：02_框架说明.md

---

## 一、变更全览

### 新建文件

| 文件 | 说明 |
|------|------|
| `config/translation.js` | 统一开发者配置入口（直译服务、AI 服务、Prompt、Token 限制） |
| `src/hooks/useDirectTranslation.js` | 弹窗直译 Hook，替换 useAIWordTranslation + useDirectWordTranslation |
| `src/hooks/useBookmarkAI.js` | 收藏卡片后台 AI 精译 Hook |

### 修改文件

| 文件 | 变更程度 | 说明 |
|------|---------|------|
| `src/services/aiProviders/index.js` | 中 | buildPrompt/getMaxTokens 从 config 读取 |
| `src/services/directTranslation/index.js` | 小 | 签名简化，provider 从 config 读取 |
| `src/store/storage.js` | 小 | createBookmark 增加 translationStatus 字段 |
| `src/components/ReaderPage.jsx` | 大 | 主流程重构：弹窗→直译，收藏→后台 AI |
| `src/components/TranslationPopup.jsx` | 中 | 移除 AI 相关 props，简化展示逻辑 |
| `src/components/BookmarkPanel.jsx` | 小 | 支持 translationStatus='pending' 的 loading 展示 |
| `src/components/BookmarkHoverCard.jsx` | 小 | 支持 translationStatus='pending' 的 loading 展示 |

### 删除文件

| 文件 | 说明 |
|------|------|
| `src/hooks/useAIWordTranslation.js` | 功能拆分至 useDirectTranslation + useBookmarkAI |
| `src/hooks/useDirectWordTranslation.js` | 同上 |
| `src/store/aiConfig.js` | 用户配置入口下线，开发者配置迁移至 config/translation.js |
| `src/components/AISettingsPanel.jsx` | 用户侧配置界面下线 |

---

## 二、各文件详细方案

---

### 1. `config/translation.js`（新建）

**职责**：唯一开发者配置入口。

```js
// 直译服务：弹窗翻译使用
export const directConfig = {
  activeProvider: 'myMemory', // 可选：'myMemory' | 'deepl' | 'youdao'
}

// AI 精译服务：收藏卡片使用
export const aiConfig = {
  activePreset: 'gemini-preset', // 可选：'gemini-preset' | 'deepseek-preset'
}

// Prompt 模板
export const prompts = {
  // 词/短句收藏卡片：返回 JSON {meaning, contextTranslation}
  bookmarkCard: (word, context) =>
    `You are a translation assistant.
Translate according to the context and return STRICT JSON only (no markdown, no explanation):
{"meaning":"...","contextTranslation":"..."}

Rules:
- "meaning": concise Chinese meaning of "${word}" in this context, <= 12 Chinese characters.
- "contextTranslation": Chinese translation of this sentence: "${context}".
- Keep wording natural and accurate.`,

  // 句子收藏卡片：返回中文译文字符串
  sentence: (text) =>
    `Translate the following English sentence to Chinese. Reply with ONLY the Chinese translation, no explanation:\n\n${text}`,

  // 段落收藏卡片：返回中文译文字符串
  paragraph: (text) =>
    `Translate the following English text to Chinese. Reply with ONLY the Chinese translation, no explanation:\n\n${text}`,
}

// Token 上限
export const tokenLimits = {
  bookmarkCard: 260,
  sentence: 200,
  paragraph: 600,
}
```

---

### 2. `services/aiProviders/index.js`（修改）

**变更**：

1. 新增 import：`import { prompts, tokenLimits, aiConfig } from '../../../config/translation'`

2. `buildPrompt(text, type, context)` 改为从 config 读取：
   ```js
   function buildPrompt(text, type, context) {
     if (type === 'word_phrase_bundle') return prompts.bookmarkCard(text, context)
     if (type === 'sentence') return prompts.sentence(text)
     return prompts.paragraph(text)
   }
   ```

3. `getMaxTokens(type)` 改为从 config 读取：
   ```js
   function getMaxTokens(type) {
     if (type === 'word_phrase_bundle') return tokenLimits.bookmarkCard
     if (type === 'sentence') return tokenLimits.sentence
     return tokenLimits.paragraph
   }
   ```

4. 新增导出 `getBookmarkAIConfig()`，供 `useBookmarkAI` 使用：
   ```js
   export function getBookmarkAIConfig() {
     const preset = aiConfig.activePreset
     const model = presetModels[preset]?.defaultModel ?? ''
     return { provider: preset, model, apiKeys: {} }
   }
   ```

5. `translateText` 签名和行为不变。

---

### 3. `services/directTranslation/index.js`（修改）

**变更**：

1. 新增 import：`import { directConfig } from '../../../config/translation'`

2. `translateDirect` 签名简化，provider 从 config 读取，不再接收 `contextSentence`：
   ```js
   // 旧：translateDirect(word, contextSentence, provider, signal) → {meaning, contextTranslation}
   // 新：translateDirect(text, signal) → string
   export async function translateDirect(text, signal) {
     const provider = directConfig.activeProvider
     if (provider === 'myMemory') {
       return translateOne(text, signal) // myMemory.js 中已有的函数
     } else if (provider === 'deepl' || provider === 'youdao') {
       return netlifyDirectText(text, provider, signal) // 见下
     }
     throw new Error(`Unknown direct provider: ${provider}`)
   }
   ```

3. 新增内部函数 `netlifyDirectText(text, provider, signal)`：
   ```js
   async function netlifyDirectText(text, provider, signal) {
     const res = await fetch('/.netlify/functions/translate', {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
       body: JSON.stringify({ provider, word: text }),
       signal,
     })
     if (!res.ok) {
       const err = await res.json().catch(() => ({}))
       throw new Error(err?.error ?? `服务暂时不可用 (${res.status})`)
     }
     const data = await res.json()
     // 此处 result 为 {meaning, contextTranslation}，只取 meaning
     return data.result?.meaning ?? data.result ?? ''
   }
   ```

4. 保留 `netlifyDirectBundle`（原函数）以备向后兼容，但不对外导出。

---

### 4. `src/hooks/useDirectTranslation.js`（新建）

**职责**：弹窗直译 Hook，所有选中文本统一走直译，返回字符串。

```js
import { useState, useRef, useCallback } from 'react'
import { translateDirect } from '../services/directTranslation/index'

export function useDirectTranslation() {
  const [result, setResult] = useState(null)
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState(null)
  const abortRef = useRef(null)

  const translate = useCallback(async (text) => {
    if (!text?.trim()) return
    abortRef.current?.abort()
    const controller = new AbortController()
    abortRef.current = controller
    setLoading(true)
    setResult(null)
    setError(null)
    try {
      const out = await translateDirect(text.trim(), controller.signal)
      if (!controller.signal.aborted) setResult(out)
    } catch (e) {
      if (!controller.signal.aborted) setError(e.message ?? '翻译失败')
    } finally {
      if (!controller.signal.aborted) setLoading(false)
    }
  }, [])

  const clear = useCallback(() => {
    abortRef.current?.abort()
    setResult(null)
    setLoading(false)
    setError(null)
  }, [])

  return { result, loading, error, translate, clear }
}
```

---

### 5. `src/hooks/useBookmarkAI.js`（新建）

**职责**：收藏触发后台 AI 精译，完成后更新 bookmark 数据并通知调用方。

```js
import { useCallback } from 'react'
import { translateText, getBookmarkAIConfig } from '../services/aiProviders/index'
import { bookmarkStore } from '../store/storage'

export function useBookmarkAI() {
  const translateBookmark = useCallback(async (bookmark, onComplete) => {
    const config = getBookmarkAIConfig()
    const isShort = bookmark.type === 'word' || bookmark.type === 'phrase'
    try {
      let translation = null
      let contextTranslation = null

      if (isShort) {
        const out = await translateText(
          config,
          bookmark.text,
          'word_phrase_bundle',
          bookmark.contextSentence
        )
        translation = out?.meaning ?? ''
        contextTranslation = out?.contextTranslation ?? null
      } else {
        translation = await translateText(
          config,
          bookmark.text,
          bookmark.type,
          null
        )
      }

      const updated = {
        ...bookmark,
        translation,
        contextTranslation,
        translationStatus: 'done',
      }
      bookmarkStore.save(updated)
      onComplete?.(updated)
    } catch {
      const updated = { ...bookmark, translationStatus: 'error' }
      bookmarkStore.save(updated)
      onComplete?.(updated)
    }
  }, [])

  return { translateBookmark }
}
```

---

### 6. `src/store/storage.js`（修改）

**变更**：`createBookmark` 工厂函数

1. 移除参数：`translation`、`contextTranslation`（不再在创建时传入）
2. 新增字段：`translationStatus: 'pending'`

```js
export function createBookmark({
  type,
  text,
  contextSentence,
  articleId,
  paragraphIndex,
  charOffset,
}) {
  return {
    id: `bm_${Date.now()}_${Math.random().toString(36).slice(2, 7)}`,
    type,
    text,
    translation: null,
    contextSentence: contextSentence ?? null,
    contextTranslation: null,
    translationStatus: 'pending',
    articleId,
    paragraphIndex,
    charOffset,
    createdAt: new Date().toISOString(),
    reviewCount: 0,
    nextReviewAt: null,
    familiarity: 0,
  }
}
```

---

### 7. `src/components/TranslationPopup.jsx`（修改）

**移除 props**：`ctxLoading`、`ctxResult`、`aiResult`、`aiLoading`、`aiError`

**保留 props**：
```
selectedText, selectionType, position
result, loading, error   ← 直译结果（适用所有类型）
contextSentence          ← 原文语境句（仅展示，不翻译）
onClose, onBookmark, isBookmarked
```

**词/短句区块变更**：
- 移除 `aiResult`/`aiLoading`/`aiError` 展示块，改用 `result`/`loading`/`error`（与句/段统一）
- 移除 `ctxResult`/`ctxLoading` 展示块，语境句仍显示原文但不显示翻译
- 收藏按钮：不再依赖 `aiLoading`，改为仅 `isBookmarked` 控制禁用状态（可随时收藏）

**`useEffect` 依赖数组**：从 `[..., ctxResult, ctxLoading, aiResult, aiLoading]` 简化为 `[position, result, loading]`

---

### 8. `src/components/ReaderPage.jsx`（大幅修改）

**Import 变更**：
```js
// 移除
import { useAIWordTranslation } from '../hooks/useAIWordTranslation'
import { useDirectWordTranslation } from '../hooks/useDirectWordTranslation'
import { aiConfigStore } from '../store/aiConfig'
import AISettingsPanel from './AISettingsPanel'
import { Settings } from 'lucide-react'  // 若 Settings 仅用于 AISettingsPanel

// 新增
import { useDirectTranslation } from '../hooks/useDirectTranslation'
import { useBookmarkAI } from '../hooks/useBookmarkAI'
```

**State 变更**：
- 移除 `settingsOpen` / `setSettingsOpen`
- Hook 由 3 个精简为 2 个：
  ```js
  const { result, loading, error, translate, clear } = useDirectTranslation()
  const { translateBookmark } = useBookmarkAI()
  ```

**`handleMouseUp` 简化**：
- 移除 mode 判断，统一调用 `translate(selected)`
- 移除 `aiClear()`、`directClear()` 调用，仅保留 `clear()`
- 保留 `findContainingSentence` 逻辑（contextSentence 仍存入 popup state，供收藏时用）

**`handleBookmark` 重构**：
```js
const handleBookmark = useCallback(() => {
  if (!popup) return
  const bm = createBookmark({
    type: popup.selectionType,
    text: popup.text,
    contextSentence: popup.contextSentence ?? null,
    articleId,
    paragraphIndex: popup.paragraphIndex,
    charOffset: popup.charOffset,
  })
  bookmarkStore.save(bm)
  setBookmarks(bookmarkStore.getByArticle(articleId))
  // 后台触发 AI 精译
  translateBookmark(bm, () => {
    setBookmarks(bookmarkStore.getByArticle(articleId))
  })
}, [popup, articleId, translateBookmark])
```

**`closePopup` 简化**：
```js
const closePopup = useCallback(() => {
  setPopup(null)
  clear()
  window.getSelection()?.removeAllRanges()
}, [clear])
```

**TranslationPopup 调用简化**：
```jsx
<TranslationPopup
  selectedText={popup.text}
  selectionType={popup.selectionType}
  position={popup.position}
  result={result}
  loading={loading}
  error={error}
  contextSentence={popup.contextSentence}
  onClose={closePopup}
  onBookmark={handleBookmark}
  isBookmarked={isPopupBookmarked}
/>
```

**移除 JSX**：
- `<AISettingsPanel>` 整块删除
- Settings 按钮删除

---

### 9. `src/components/BookmarkPanel.jsx`（修改）

在卡片主体区域，对 `bm.translation` 的渲染加入状态判断：

```jsx
{/* 主翻译区域 */}
{bm.translationStatus === 'pending' ? (
  <div className="flex items-center gap-1.5" style={{ marginBottom: '4px' }}>
    <Loader2 size={11} className="animate-spin" style={{ color: 'rgba(28,25,23,0.2)' }} />
    <span style={{ fontSize: '11px', fontFamily: 'DM Sans', color: 'rgba(28,25,23,0.3)' }}>
      翻译生成中…
    </span>
  </div>
) : bm.translation ? (
  <p style={{ /* 现有样式 */ }}>{bm.translation}</p>
) : null}
```

新增 import：`import { Loader2 } from 'lucide-react'`

---

### 10. `src/components/BookmarkHoverCard.jsx`（修改）

同 BookmarkPanel，在 `bookmark.translation` 展示处加入状态判断：

```jsx
{bookmark.translationStatus === 'pending' ? (
  <div className="flex items-center gap-1.5" style={{ marginBottom: '6px' }}>
    <Loader2 size={11} className="animate-spin" style={{ color: 'rgba(28,25,23,0.2)' }} />
    <span style={{ fontSize: '11px', fontFamily: 'DM Sans', color: 'rgba(28,25,23,0.3)' }}>
      翻译生成中…
    </span>
  </div>
) : bookmark.translation ? (
  <p style={{ /* 现有样式 */ }}>{bookmark.translation}</p>
) : null}
```

---

### 11. 删除文件

按顺序在最后执行，确保所有引用已清除：

1. `src/hooks/useAIWordTranslation.js`
2. `src/hooks/useDirectWordTranslation.js`
3. `src/store/aiConfig.js`
4. `src/components/AISettingsPanel.jsx`

---

## 三、未变更文件

| 文件 | 说明 |
|------|------|
| `netlify/functions/translate.js` | 无需改动，原有接口向后兼容 |
| `config/presetModels.json` | 仍被 Netlify Function（Node.js）读取，保留 |
| `services/aiProviders/gemini.js` | 无改动 |
| `services/aiProviders/openaiCompat.js` | 无改动 |
| `services/directTranslation/myMemory.js` | 无改动 |
| `config/directTranslation.json` | 不再运行时读取，作为文档性配置保留 |

---

## 四、关键边界行为说明

### 收藏按钮时机
新架构下收藏按钮**无需等待翻译**即可点击（不再受 `loading` 控制）。用户可在弹窗出现后立即收藏，AI 在后台处理。

### 直译失败处理
弹窗中 `error` 显示时，用户仍可收藏（原文已足够创建卡片），AI 会独立处理翻译。

### AI 精译失败处理
`translationStatus` 设为 `'error'`，卡片显示原文但翻译区域留空（不显示 loading）。后续可扩展"重试"按钮。

### 存量 bookmark 数据兼容
旧收藏没有 `translationStatus` 字段。渲染时若该字段为 `undefined`，按 `'done'` 处理（有 `translation` 就展示，无则不展示），不影响现有数据显示。
