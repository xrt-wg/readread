# 实施前审核反馈处理记录 — 阅读区进度计算逻辑

- **日期**：2026-06-19
- **审核对象**：[03_实施前审核反馈.md](../修复/03_实施前审核反馈.md)
- **处理人**：Claude
- **处理结论**：✅ 全部反馈已处理 — 3 条件性问题 + 1 增强建议已落实到方案文档

---

## 一、条件性问题处理

| # | 审核反馈 | 严重度 | 处理方式 | 更新位置 |
|---|---------|--------|---------|---------|
| C1 | `getMarkdownParagraphs` 中 h5/h6 未跳过 | 中 | 正则 `#{1,4}` → `#{1,6}`，覆盖全部 6 级标题 | [02_解决方案说明.md:81](../修复/02_解决方案说明.md#L81) |
| C2 | 未处理代码块、水平线、引用块等边缘情况 | 中 | 见下方 C2 逐项处理 | — |
| C3 | Fix 4 引入额外网络往返 | 低 | 方案文档增加性能提示；记录为技术债务（后续 RPC 优化） | [02_解决方案说明.md:293](../修复/02_解决方案说明.md#L293) |

### C2 逐项处理

| 子项 | 边缘情况 | 影响 | 处理 |
|------|---------|------|------|
| C2-a | 代码块 (```) 内含空行被 `\n\n+` 错误分裂 | 产生幽灵段落，**+N 偏移** | 新增：分割前用占位符 `%%CODEBLOCK_N%%` 替换围栏代码块，处理后跳过占位符 |
| C2-b | 水平线 (`---`) 被当作普通段落 | **+1 偏移** | 新增：`/^[-*_]{3,}\s*$/` 跳过 |
| C2-c | 引用块 (`>`) 被当作段落 | **+1 偏移** | 新增：`/^>\s/m` 跳过（与 `MarkdownContent` 中 `<blockquote>` 不递增 `paraIdxRef` 一致） |

### C3 补充说明

`saveReadingMark` 增加 `fetchCloudReadingMarkRow` 后从 1 次变 2 次 Supabase 请求。阅读标记是用户单击按钮的离散操作（非滚动触发），频率低，延迟在可接受范围。后续优化方向：将 read+write 合并为单个 RPC（`SECURITY DEFINER`），已在方案文档「不在此次修复范围」中记录。

---

## 二、增强建议处理

| # | 建议 | 优先级 | 处理方式 | 更新位置 |
|---|------|--------|---------|---------|
| S1 | 修复 `handleSetReadingMark` 的多章节 `sectionId` 比对缺陷 | 高 | 新增 Fix 5：`isSameMark` 条件增加 `(readingMark?.sectionId ?? null) === (currentSection?.id ?? null)` | [02_解决方案说明.md:297-319](../修复/02_解决方案说明.md#L297) |
| S2 | `saveReadingMark` read+write 合并为单个 RPC | 中 | 记录为技术债务，不阻塞本次 | 方案 §五 + [02_解决方案说明.md:293](../修复/02_解决方案说明.md#L293) |
| S3 | `getParagraphs` 空 markdown 字符串 truthy 检查 | 低 | `article.markdown != null` → `!!article.markdown`，两处（函数体 + ReaderPage 调用处） | [02_解决方案说明.md:53,217](../修复/02_解决方案说明.md#L53) |

---

## 三、分析完备性缺陷处理

| # | 审核发现的缺失 | 处理方式 | 更新位置 |
|---|-------------|---------|---------|
| G1 | 未识别 `handleSetReadingMark` 缺少 `sectionId` 比对 | 分析报告新增 §3.7 + 汇总表条目 6 | [01_问题分析报告.md:215-247](../修复/01_问题分析报告.md#L215) |
| G2 | 未讨论 Fix 3/4 的额外网络往返 | 方案文档 Fix 4 后增加性能提示 | [02_解决方案说明.md:293](../修复/02_解决方案说明.md#L293) |
| G3 | 未评估 `getMarkdownParagraphs` 与 AST 渲染的偏差风险 | 方案文档已通过 C2 的逐项处理覆盖所有审核识别的边缘情况 | C2-a, C2-b, C2-c |

---

## 四、更新文档清单

| 文件 | 更新项数 | 主要变更 |
|------|---------|---------|
| [01_问题分析报告.md](../修复/01_问题分析报告.md) | 4 | 新增 §3.7（sectionId 比对缺陷）；汇总表 +1 条目；现象描述 +1；根因推导扩展 |
| [02_解决方案说明.md](../修复/02_解决方案说明.md) | 8 | `getMarkdownParagraphs` 增加 4 类跳过逻辑；truthy 检查 ×2；新增 Fix 5；方案概览/文件清单/不修复范围 同步更新；性能提示 |

---

## 五、处理签署

| 角色 | 结论 | 日期 |
|------|------|------|
| 处理人 | ✅ 全部反馈已处理 | 2026-06-19 |
