# 回归验证记录 — 划选选区异常过扩（问题 12 关闭）

> **日期**：2026-09-16
> **验证版本**：修复 `36a5d0f`（F1 readingMark 引用稳定化 + F2 拖选延迟落地）
> **证据**：`selection-probe-regression-1789523109405.json`（Chrome 152，39 次拖选手势，467 条记录）
> **用户结论**：验证完成，未再出现之前的问题。

---

## 一、硬判据复核（脚本全量，非抽样）

| 判据（09 报告 §三） | 修复前（基准日志） | 回归日志 | 结论 |
|---|---|---|---|
| 收藏后无 104 条级全树重挂载爆发（F1） | 12 次爆发，每次恰好 104 条（52 段落 div 全移除+全重建） | 30 次段落区变更，**最大 24 条**，全部为单段局部更新（文本节点→span 切分） | ✅ F1 生效 |
| 拖选途中零 paragraph 区 mut（F2） | 3 次爆发落在拖选途中（手势 4/7/14） | **153 条 mut 全部发生在非拖选窗口（duringDrag=false 占比 100%）** | ✅ F2 生效 |
| 06 清单手势复测零发作 | 20–30% 发作率，过扩 74/100 字符、跨段落 | **39 次手势零过扩**：选区均为目标词/短语，无跨段落（head 无 `\n\n`）、无段落起点异常长选区 | ✅ 零发作 |

## 二、附带观察

- 词选区两侧带空格（如 `' philosophy '`、`' me '`）仍出现——这是已定性的**无害浏览器边界光标规范化**（09 报告 §一手势 14 表格、§二拟合表），非问题复发；
- 长选区（len=28/29/88）均为用户主动的短语/句子收藏（head 与选区内容一致、无锚点回落形态）；
- 会话含 2 条 popup 区 mut——本次回归在弹窗（非预读）模式下进行，F2 对弹窗管线同样生效；
- 审核 P2（阅读标记视觉）、P3（blur/窗外松手兜底）、P7（混排区抽查）三项由用户回归覆盖，未报告异常。

## 三、关闭动作

1. 移除探针：`src/debug/selectionProbe.js` + ReaderPage 接入点（import + 初始化 useEffect）；
2. 构建验证 → 提交推送；
3. 问题 12 正式关闭。

## 四、过程资产留存

- 探针源码与两份日志（基准 + 回归）留存于本目录与 git 历史，若未来出现同类选区异常可直接复用同一探针（从 git 历史恢复）；
- 经验（09 报告 §四）：低频间歇问题以直接观测（探针）定案，时间线吻合与排除法仅作弱证据。
