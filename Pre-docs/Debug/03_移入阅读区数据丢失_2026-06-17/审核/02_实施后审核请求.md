# 实施后审核请求 — 「移入阅读区」数据写入静默丢失

- **日期**：2026-06-17
- **分支**：feat/community-recommendations
- **状态**：待审核
- **审核对象**：P0 bug 修复 — `copyToReadingZone` 补全 `canUseCloudLibrary` 参数传递
- **依据文档**：
  - [00_问题分析报告.md](../00_问题分析报告.md)
  - [01_修复方案.md](../01_修复方案.md)
  - [审核/01_实施前审核反馈.md](01_实施前审核反馈.md)

---

## 一、审核说明

### 1.1 审核目标

验证修复实施是否正确、完整，确保：

- 修复完全匹配方案设计（3 处改动，2 个文件）
- 修复后的数据流读写路径统一（写入 Supabase ↔ 读取 Supabase）
- 无遗漏的调用点
- 现有功能无退化

### 1.2 审核方式

| 标记 | 含义 |
|------|------|
| ✅ 通过 | 实施完全符合方案，无遗留问题 |
| ⚠️ 有条件通过 | 实施基本符合方案，存在已知的、可接受的偏差 |
| ❌ 不通过 | 实施与方案存在实质性偏差，需修复后重新审核 |
| — 不适用 | 该项在当前阶段不适用 |

---

## 二、代码改动审核

### 2.1 改动清单对照

| 编号 | 方案要求 | 实际实施 | 结果 |
|------|---------|---------|------|
| C-001 | `importItems.js:237` — 函数签名增加 `{ canUseCloudLibrary }` | [importItems.js:237](src/services/importItems.js#L237)：`export async function copyToReadingZone(importItem, { canUseCloudLibrary })` | |
| C-002 | `importItems.js:272` — `saveArticle` 调用增加 `canUseCloudLibrary` | [importItems.js:272](src/services/importItems.js#L272)：`saveArticle(document, { canUseCloudLibrary, userId: importItem.userId })` | |
| C-003 | `ImportPage.jsx:124` — 调用处传递 `{ canUseCloudLibrary }` | [ImportPage.jsx:124](src/components/ImportPage.jsx#L124)：`copyToReadingZone(item, { canUseCloudLibrary })` | |

### 2.2 附加改动

| 编号 | 改动 | 位置 | 说明 | 结果 |
|------|------|------|------|------|
| A-001 | `handleMoveToReading` 依赖数组补全 `canUseCloudLibrary` | [ImportPage.jsx:133](src/components/ImportPage.jsx#L133) | `[canUseCloudLibrary, loadLibraryState, refreshAuthState]` — React Hooks 规范，避免闭包过期 | |

---

## 三、修复后数据流审核

### 3.1 写入路径

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| W-001 | `copyToReadingZone` 接收 `canUseCloudLibrary` 并传递给 `saveArticle` | 方案 §2.1 | |
| W-002 | `saveArticle` 的 `useCloudSource({ canUseCloudLibrary, userId })` 返回 `true`（用户已登录） | 方案 §3 | |
| W-003 | Supabase `articles` 表执行 upsert（`onConflict: 'id'`） | library.js:181-209 | |
| W-004 | `articleStore.save()`（localStorage）不再被触发 | 方案 §3 | |

### 3.2 读取路径

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| R-001 | `loadLibraryState()` → `listArticles({ canUseCloudLibrary: true, userId })` → 查询 Supabase | library.js:155-172 | |
| R-002 | 写入 Supabase 的记录在读取结果中可见 | 方案 §3 | |
| R-003 | 文章卡片展示「来源：书架「xxx」」标签（`sourceImportId` 匹配） | ImportPage.jsx:577-580 | |

### 3.3 错误路径

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| E-001 | `canUseCloudLibrary` 为 `false` 时（匿名用户）`copyToReadingZone` 不会被调用——书架仅登录用户可见 | ImportPage.jsx:319 | |
| E-002 | Supabase 写入失败 → `saveArticle` throw → `handleMoveToReading` catch 显示「移入阅读区失败」 | ImportPage.jsx:129-132 | |

---

## 四、回归审核

### 4.1 `copyToReadingZone` 其他功能不变

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-001 | `structuredClone(importItem.sections)` 深拷贝 sections — 不受影响 | |
| G-002 | `createDocument(extractionResult)` 生成独立 Document — 不受影响 | |
| G-003 | `source_import_id` 和 `imported_at` 溯源字段正确设置 — 不受影响 | |
| G-004 | `sourceType` 和 `kind` 映射 — 不受影响 | |

### 4.2 其他写入路径不受影响

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-005 | 直接导入（App.jsx handleImport）— 走 `saveArticle({ canUseCloudLibrary, userId })` ✅ 原本即正确 | |
| G-006 | 推荐区加入书架（addRecommendationToBookshelf）— 走 `createImportItem`，不涉及 saveArticle | |
| G-007 | 数据导入（importLibraryData → saveArticle）— 透传 options ✅ 原本即正确 | |

### 4.3 书架功能不变

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-008 | 书架列表正常加载（fetchImportItems → import_items 表） | |
| G-009 | 移入计数更新正常（countImportReferences → articles 表） | |
| G-010 | 编辑素材、删除素材功能不受影响 | |

---

## 五、验证方案

### 5.1 自动化验证

```
□ 构建通过：npm run build 无错误
```

### 5.2 手动验证

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-001 | 清空 localStorage：`localStorage.removeItem('rr_articles')` | — | |
| V-002 | 登录 → 书架 → 点击「移入阅读区」 | 绿色提示「已移入阅读区」 | |
| V-003 | 检查 localStorage | `rr_articles` 不存在或为空 | |
| V-004 | 切换到「阅读」标签 | 列表中显示刚移入的文章 | |
| V-005 | 检查文章底部溯源信息 | 显示「来源：书架「{标题}」」 | |
| V-006 | Supabase Dashboard 查询 `articles` 表 | 新记录存在，`source_import_id` 指向书架素材，`user_id` 匹配 | |
| V-007 | 点击文章 → 进入阅读器 → 正常阅读 | 划词收藏、标记进度正常 | |
| V-008 | 返回书架 → 同一素材再次「移入阅读区」 | 第二次移入也正常工作，移入次数更新 | |

### 5.3 边界条件验证

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-009 | 多次移入同一素材（生成多个独立副本） | 阅读区出现多篇同名文章，各自独立 | |
| V-010 | 移入书籍（kind = 'book'，多 sections） | 文章展示书籍标记「📖 书」和章节数 | |
| V-011 | 移入后删除书架素材 | 文章保留，来源标签消失 | |

---

## 六、审核结论

### 6.1 整体评估

| 维度 | 状态 | 备注 |
|------|------|------|
| 改动匹配方案 | | C-001 ~ C-003 三处改动，附加 A-001 依赖数组修正 |
| 修复后数据流 | | 写入路径 Supabase ↔ 读取路径 Supabase，统一 |
| 回归安全 | | `copyToReadingZone` 其他逻辑不变，其他写入路径不受影响 |
| 边界情况 | | 多次移入、书籍移入、匿名用户门控 |

### 6.2 阻塞项

| 编号 | 问题描述 | 对应审核项 | 责任人 | 期限 |
|------|---------|-----------|--------|------|
| | | | | |

### 6.3 已知偏差

| 编号 | 偏差描述 | 原因 | 后续计划 |
|------|---------|------|---------|
| D-001 | localStorage 中残留的旧测试数据（`rr_articles` 中的 `doc_17816587...` 记录）不会被自动迁移到 Supabase | 数据量极少（测试环境），不作为本次修复范围 | 已在实施前审核反馈中记录为 Tech Debt——若分支有真实用户使用过，需增加一次性数据抢救提示 |

### 6.4 审核签署

| 角色 | 结论 | 日期 | 签署 |
|------|------|------|------|
| 审核人 | | 2026-06-17 | |
