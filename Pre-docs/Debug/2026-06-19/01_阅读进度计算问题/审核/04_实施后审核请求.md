# 实施后审核请求 — 阅读区进度计算逻辑

- **日期**：2026-06-19
- **分支**：feat/translation-test
- **状态**：待审核
- **审核对象**：阅读进度计算逻辑全面修复 — 5 个 Fix，4 个文件
- **依据文档**：
  - [修复/01_问题分析报告.md](../修复/01_问题分析报告.md)
  - [修复/02_解决方案说明.md](../修复/02_解决方案说明.md)
  - [审核/01_实施前审核反馈.md](01_实施前审核反馈.md)
  - [审核/01_实施前审核反馈处理记录.md](01_实施前审核反馈处理记录.md)

---

## 一、审核说明

### 1.1 审核目标

验证修复实施是否正确、完整，确保：

- 5 个 Fix 完全匹配方案设计（1 新建 + 3 修改）
- 段落解析统一性——`calcProgress` 和 ReaderPage 使用同一 `getParagraphs`
- markdown 边缘情况（代码块/标题/水平线/引用块）正确处理
- 多章节进度（`calcMultiSectionProgress`）算法正确
- `completed` / `completedSections` 保留行为与本地路径一致
- 现有功能无退化

### 1.2 审核方式

| 标记 | 含义 |
|------|------|
| ✅ 通过 | 实施完全符合方案，无遗留问题 |
| ⚠️ 有条件通过 | 实施基本符合方案，存在已知的、可接受的偏差 |
| ❌ 不通过 | 实施与方案存在实质性偏差，需修复后重新审核 |
| — 不适用 | 该项在当前阶段不适用 |

---

## 二、代码改动审核

### 2.1 改动清单对照

| 编号 | 方案要求 | 实际实施 | 结果 |
|------|---------|---------|------|
| C-001 | 新建 `src/services/progress.js`，含 `getParagraphs`/`getWordOffsetAtParagraph`/`calcProgress` 等 | [progress.js](src/services/progress.js)：156 行，含完整 markdown 边缘情况处理（h1-h6 跳过、代码块占位符、水平线跳过、引用块跳过）+ 单/多章节进度计算 | |
| C-002 | `ImportPage.jsx` — 删除本地 `calcProgress`，从 `progress.js` 导入 | 新增 L13 `import { calcProgress } from '../services/progress'`；删除 L49-56 本地函数 | |
| C-003 | `ReaderPage.jsx` — `paragraphs` 改用 `getParagraphs` | L183-186：`!!article.markdown ? getParagraphs(article) : parseText(text)` | |
| C-004 | `ReaderPage.jsx` — `handleSetReadingMark` 增加 `sectionId` 比对 | L432-438：`isSameMark = paragraphIndex === paraIndex && sectionId === currentSection.id && !completed`；依赖数组增加 `currentSection` | |
| C-005 | `library.js` — `saveReadingMark` 云端路径先读后写 | L420-421 + L427-428：`existing = await fetchCloudReadingMarkRow(...)`；`completed: existing?.completed ?? false`；`completedSections: existing?.completed_sections ?? []` | |
| C-006 | `library.js` — `clearReadingMark` 云端路径先读后写 | L441 + L447：`existing = await fetchCloudReadingMarkRow(...)`；`completedSections: existing?.completed_sections ?? []` | |

### 2.2 附加改动

无。实施严格按方案执行。

---

## 三、progress.js 逐项审核

### 3.1 段落解析完整性

| 编号 | 审核项 | MarkdownContent 行为 | getMarkdownParagraphs 行为 | 结果 |
|------|--------|---------------------|--------------------------|------|
| M-001 | 去除 frontmatter | 不渲染 YAML 前置块（`body = markdown.slice(fmMatch[0].length)`） | `.replace(/^---\r?\n[\s\S]*?\r?\n---\r?\n?/, '')` | |
| M-002 | h1-h4 标题 → 跳过 | 自定义组件，不递增 `paraIdxRef` | `/^#{1,6}\s/.test(block)` → continue | |
| M-003 | h5/h6 标题 → 跳过 | `react-markdown` 默认渲染，不递增 `paraIdxRef` | `/^#{1,6}\s/` 覆盖 h5/h6（审核反馈 C1） | |
| M-004 | 代码块 → 跳过 | 不递增 `paraIdxRef` | `%%CODEBLOCK_N%%` 占位符替换 → continue（审核反馈 C2-a） | |
| M-005 | 水平线 → 跳过 | 不递增 `paraIdxRef` | `/^[-*_]{3,}\s*$/` → continue（审核反馈 C2-b） | |
| M-006 | 引用块 → 跳过 | `<blockquote>` 不递增 `paraIdxRef` | `/^>\s/m` → continue（审核反馈 C2-c） | |
| M-007 | `<p>` 段落 → 累计 | 递增 `paraIdxRef` | 非标题/非列表块 → push | |
| M-008 | `<li>` 列表项 → 拆分累计 | 递增 `paraIdxRef`，每项独立 | `isListBlock` 检测 → `split(/\n/)` → 逐项 push | |
| M-009 | 行内 markdown 清洗 | N/A（统计用） | `cleanInlineMarkdown` — 去除 `**`、`*`、`` ` ``、`[link]()`、`>` | |
| M-010 | 空 markdown 保护 | N/A | `!!article.markdown` 而非 `!= null`（审核反馈 S3） | |

### 3.2 进度计算算法

| 编号 | 审核项 | 方案要求 | 实际 | 结果 |
|------|--------|---------|------|------|
| M-011 | `calcProgress` 入口 | null guard：无 mark / completed / 无 paragraphIndex → null | L111-113：✅ | |
| M-012 | 单章节 `calcSingleProgress` | `readWords / totalWords × 100`，max 99 | L123-128：✅ | |
| M-013 | 多章节 `calcMultiSectionProgress` | 按 section 词数汇总：已完成章节累计全词数 + 当前章节按节内段落索引 | L130-155：✅ | |
| M-014 | 多章节已完成章节判断 | `completedIds.has(section.id)` → `cumulativeWords += section.body.wordCount` | L138-140：✅ | |
| M-015 | 多章节当前章节处理 | `section.id === mark.sectionId` → 构造 `sectionArticle` → `getWordOffsetAtParagraph` → break | L141-149：✅ | |
| M-016 | `sectionArticle` 构造 | `{ text, markdown, wordCount }` — 传递 section 级别数据，`getParagraphs` 内部 `!!markdown` 检查正确路由 | L143-147：✅ | |

---

## 四、修复后数据流审核

### 4.1 段落索引统一性

| 编号 | 审核项 | 之前 | 之后 | 结果 |
|------|--------|------|------|------|
| F-001 | `calcProgress` 段落来源 | `article.text.split(/\n+/)` | `getParagraphs(article)` — 统一函数 | |
| F-002 | ReaderPage 段落来源 | `parseText(text)` → `text.split(/\n+/)` | `!!markdown ? getParagraphs(article) : parseText(text)` — 对 markdown 文章使用统一函数 | |
| F-003 | 纯文本文章回退 | `parseText` | `parseText` — 与 `getParagraphs` 纯文本路径等效（均使用 `split(/\n+/)`） | |
| F-004 | 段落索引一致性 | markdown 文章 ReaderPage 用 AST 索引、calcProgress 用 text split 索引 → **不一致** | 两端统一使用 `getParagraphs` → **一致** | |

### 4.2 多章节进度

| 编号 | 审核项 | 之前 | 之后 | 结果 |
|------|--------|------|------|------|
| F-005 | 进度计算范围 | 全文 `article.text` → 节内 `paragraphIndex` 被当作全文索引 → 错位 | 按 section 逐节汇总 → 节内索引在节 scope 内评估 | |
| F-006 | 已完成章节计入 | 无此概念 | `completedSections` Set → 全词数计入 | |
| F-007 | section 切换时标记独立性 | `paragraphIndex` 相同即误判为"同一位置" | `isSameMark` 增加 `sectionId` 比对 | |

### 4.3 状态保留（云端/本地一致性）

| 编号 | 审核项 | 之前 | 之后 | 结果 |
|------|--------|------|------|------|
| F-008 | `saveReadingMark` — `completed` | 写死 `false` | `existing?.completed ?? false` — 与本地一致 | |
| F-009 | `saveReadingMark` — `completedSections` | 未传递 → `[]` | `existing?.completed_sections ?? []` — 保留已有 | |
| F-010 | `clearReadingMark` — `completedSections` | 未传递 → `[]`（数据丢失） | `existing?.completed_sections ?? []` — 保留已有 | |

---

## 五、回归审核

### 5.1 阅读区核心功能

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-001 | 打开文章 → 阅读器正常渲染 | |
| G-002 | 点击段落标记按钮 → `saveReadingMark` → 阅读位置保存 | |
| G-003 | 再次点击同一段落 → `clearReadingMark` → 标记清除 | |
| G-004 | 标记已读完 → `setReadingMarkCompleted` → `completed: true` | |
| G-005 | 文章列表进度显示 → `calcProgress` 百分比正确 | |
| G-006 | 多章节书切换 section → 段落索引独立、标记独立 | |
| G-007 | 收藏（bookmark）保存/删除不受影响 | |
| G-008 | 划词翻译不受影响 | |

### 5.2 书架/导入区

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-009 | 文章列表（阅读标签）排序正常 | |
| G-010 | 阅读统计（已读完/收藏/累计阅读）正常 | |
| G-011 | 书架移入阅读区正常 | |

---

## 六、验证方案

### 6.1 自动化验证

```
✅ 构建通过：npx vite build — 1865 modules，0 errors
```

### 6.2 手动验证 — 单章节文章进度

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-001 | 登录 → 阅读标签 → 打开一篇 markdown 文章 | 文章正常加载 | |
| V-002 | 滚动到第 3 段，点击段落标记按钮 | 黄金书签标记显示，保存成功 | |
| V-003 | 返回阅读标签 → 查看该文章 | 进度百分比显示，且与标记段落位置匹配 | |
| V-004 | 继续阅读 → 标记更后面的段落 | 进度百分比增加 | |
| V-005 | 点击已标记段落 → 标记清除 | 进度百分比变为 null（不显示） | |

### 6.3 手动验证 — 多章节文章进度

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-006 | 打开一本 EPUB 书（多 section） | 阅读器正常渲染，目录按钮可见 | |
| V-007 | 在 Section A 标记第 5 段 | 标记保存（sectionId = "s_0"） | |
| V-008 | 切换到 Section B → 标记第 2 段 | Section A 标记不被清除，Section B 独立标记保存 | |
| V-009 | 返回阅读标签 → 查看进度 | 进度反映 Section A + Section B 的累积阅读量 | |
| V-010 | 切换回 Section A → 点击已标记段落 | 仅清除 Section A 的标记（sectionId 匹配），Section B 标记保留 | |

### 6.4 手动验证 — 边缘情况

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-011 | 打开一篇含代码块（```）的 markdown 文章 | 段落索引不计算代码块，进度仅基于文本段落 | |
| V-012 | 打开一篇含 `# 标题` 和 `- 列表` 的 markdown 文章 | 标题不占段落索引，列表项各自独立计数 | |
| V-013 | 已读完的文章（completed=true）重新标记段落 | `completed` 保持 true，进度仅更新位置 | |

### 6.5 手动验证 — 回归

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-014 | 纯文本文章（无 markdown 字段）— 标记段落 | 段落索引使用 `parseText` 回退路径，行为不变 | |
| V-015 | 匿名模式 — 标记段落 | localStorage 路径，行为不变 | |
| V-016 | 云端模式 — 标记段落后清除 | `completedSections` 不被清空 | |

---

## 七、审核结论

### 7.1 整体评估

| 维度 | 状态 | 备注 |
|------|------|------|
| 改动匹配方案 | | C-001 ~ C-006 六处改动，严格按方案实施 |
| 段落解析完整性 | | 10 项 MarkdownContent 行为匹配，全部通过 |
| 进度计算正确性 | | 6 项算法审核，全部通过 |
| 修复后数据流 | | 段落统一、多章节汇总、状态保留，全部正确 |
| 实施前审核反馈处理 | | 3 条件性问题 + 3 增强建议，全部落实 |
| 回归安全 | | 阅读区、书架、导入区核心功能不受影响 |

### 7.2 阻塞项

| 编号 | 问题描述 | 对应审核项 | 责任人 | 期限 |
|------|---------|-----------|--------|------|
| | 无 | | | |

### 7.3 已知偏差

| 编号 | 偏差描述 | 原因 | 后续计划 |
|------|---------|------|---------|
| D-001 | `saveReadingMark` 云端增加 1 次额外 `fetchCloudReadingMarkRow`（2 次请求） | read-before-write 模式，引入轻微延迟 | 已在方案文档记录为后续优化项（合并为单 RPC） |
| D-002 | `calcMultiSectionProgress` 当前依赖 `completedSections` 字段，但该字段 UI 层未使用（P3-7 技术债务）→ 多章节进度目前仅反映当前 section | UI 层集成是多章节阅读功能的后续迭代 | 不阻塞本次修复——比修复前完全错位的进度值有本质改善 |
| D-003 | `getMarkdownParagraphs` 使用正则解析而非 AST，极端边缘情况（嵌套列表、代码块含 markdown 语法）可能仍有微小偏差 | 当前 ReadRead 文章来源（EPUB 导入、手动粘贴）不产生此类极端 markdown | 可接受——偏差远小于修复前的不对齐 |

### 7.4 审核签署

| 角色 | 结论 | 日期 | 签署 |
|------|------|------|------|
| 审核人 | | 2026-06-19 | |
