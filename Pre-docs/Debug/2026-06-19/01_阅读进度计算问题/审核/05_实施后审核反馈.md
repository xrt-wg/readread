# 实施后审核反馈 — 阅读区进度计算逻辑

- **日期**：2026-06-20
- **审核依据**：[04_实施后审核请求.md](04_实施后审核请求.md)
- **审核结论**：✅ 通过（无阻塞项，1 处表述偏差）

---

## 一、代码改动审核

### 1.1 改动清单对照 — 独立验证

逐文件独立验证 git diff 与方案设计的匹配度：

| 编号 | 方案要求 | 实际实施 | 结果 |
|------|---------|---------|------|
| C-001 | 新建 `src/services/progress.js`，含 `getParagraphs`/`getWordOffsetAtParagraph`/`calcProgress` 等 | [progress.js](src/services/progress.js)：156 行。逐函数验证：`getParagraphs`(L16)、`getMarkdownParagraphs`(L27)、`isListBlock`(L65)、`cleanInlineMarkdown`(L71)、`countWords`(L82)、`getWordOffsetAtParagraph`(L87)、`getTotalWords`(L96)、`calcProgress`(L111)、`calcSingleProgress`(L123)、`calcMultiSectionProgress`(L130) — 全部存在且签名匹配方案 | ✅ |
| C-002 | `ImportPage.jsx` — 删除本地 `calcProgress`，从 `progress.js` 导入 | git diff 验证：新增 L14 `import { calcProgress } from '../services/progress'`；删除 L49-56 本地函数（8 行）；ImportPage 内 2 处 `calcProgress` 调用 (L536, L545) 自动路由到导入版本 | ✅ |
| C-003 | `ReaderPage.jsx` — `paragraphs` 改用 `getParagraphs` | git diff 验证：新增 L29 `import { getParagraphs } from '../services/progress'`；L183-187 `paragraphs` 从 `parseText(text)` 改为 `!!article.markdown ? getParagraphs(article) : parseText(text)` | ✅ |
| C-004 | `ReaderPage.jsx` — `handleSetReadingMark` 增加 `sectionId` 比对 | git diff 验证：L432-434 新增 `isSameMark` 三条件判断（含 `sectionId` 比对）；L438 新增 break 逻辑；依赖数组 L450 增加 `currentSection` | ✅ |
| C-005 | `library.js` — `saveReadingMark` 云端路径先读后写 | git diff 验证：L420 `existing = await fetchCloudReadingMarkRow(...)`；L427 `completed: existing?.completed ?? false`；L428 `completedSections: existing?.completed_sections ?? []` | ✅ |
| C-006 | `library.js` — `clearReadingMark` 云端路径先读后写 | git diff 验证：L441 `existing = await fetchCloudReadingMarkRow(...)`；L447 `completedSections: existing?.completed_sections ?? []` | ✅ |

**总改动量**：4 文件，+36 行 / -13 行。纯前端，无迁移文件。**全部 6 处改动与方案完全匹配，无附加改动。**

### 1.2 构建验证

```
npx vite build → ✓ built in 4.44s, 1865 modules, 0 errors, 0 warnings
```

（与审核请求声明的「1865 modules，0 errors」一致）

---

## 二、progress.js 逐项审核 — 独立验证

### 2.1 段落解析完整性

| 编号 | 审核项 | MarkdownContent 行为 | getMarkdownParagraphs 实现 | 独立验证 |
|------|--------|---------------------|--------------------------|------|
| M-001 | 去除 frontmatter | `body = markdown.slice(fmMatch[0].length)` | L29: `.replace(/^---\r?\n[\s\S]*?\r?\n---\r?\n?/, '')` | ✅ 等价 |
| M-002 | h1-h6 标题跳过 | 自定义组件 `<h1>`~`<h4>` 不递增；`<h5>`/`<h6>` 默认渲染不递增 | L46: `/^#{1,6}\s/.test(block)` → continue | ✅ 全 6 级覆盖 |
| M-003 | 代码块跳过 | `<code>`/`<pre>` 不递增 `paraIdxRef` | L32-36: 占位符 `%%CODEBLOCK_N%%` 替换；L44: 占位符跳过 | ✅ 比简单正则更健壮 |
| M-004 | 水平线跳过 | `<hr>` 不递增 `paraIdxRef` | L48: `/^[-*_]{3,}\s*$/.test(block)` → continue | ✅ |
| M-005 | 引用块跳过 | `<blockquote>` 不递增 `paraIdxRef` | L50: `/^>\s/m.test(block)` → continue | ✅ |
| M-006 | `<p>` 段落累计 | 递增 `paraIdxRef` | 非特殊块 → push (L59) | ✅ |
| M-007 | `<li>` 列表项拆分 | 递增 `paraIdxRef`，每项独立 | L53-57: `isListBlock` 检测 → `split(/\n/)` → 逐项 push | ✅ |
| M-008 | 行内清洗 | N/A（用于词数统计） | L71-78: `cleanInlineMarkdown` — 去图/链/代码/强调/引用符 | ✅ |
| M-009 | 空 markdown 保护 | N/A | L18: `!!article.markdown` 而非 `!= null` | ✅ |
| M-010 | 词数统计 | `split(/\s+/).filter(Boolean).length` | L82-84: 相同逻辑 | ✅ |

### 2.2 进度计算算法 — 独立验证

| 编号 | 审核项 | 方案要求 | 实际实现 | 独立验证 |
|------|--------|---------|---------|------|
| M-011 | `calcProgress` 入口 guard | 无 mark / completed / 无 paragraphIndex → null | L111-113: 三条件完整 | ✅ |
| M-012 | 单章节计算 | `readWords / totalWords × 100`，max 99 | L123-128: `Math.min(99, Math.round(...))` | ✅ |
| M-013 | 多章节路由 | `sectionCount > 1 && sections` → `calcMultiSectionProgress` | L116-118: 条件完整 | ✅ |
| M-014 | 已完成章节全词数 | `completedIds.has(section.id)` → `cumulativeWords += section.body.wordCount` | L138-140: 逻辑正确 | ✅ |
| M-015 | 当前章节节内索引 | `section.id === mark.sectionId` → `getWordOffsetAtParagraph(sectionArticle, mark.paragraphIndex)` → break | L141-149: 构造 sectionArticle、调用统一函数、break | ✅ |
| M-016 | `sectionArticle` 数据 | `{ text, markdown, wordCount }` — 与 `getParagraphs` 的 `article` 参数兼容 | L143-147: 字段完整，`!!markdown` 检查在 `getParagraphs` 内部执行 | ✅ |

---

## 三、修复后数据流审核 — 独立验证

### 3.1 段落索引统一性

| 编号 | 审核项 | 之前 | 之后 | 独立验证 |
|------|--------|------|------|------|
| F-001 | `calcProgress` 段落来源 | `article.text.split(/\n+/)` | `getParagraphs(article)` — 统一 | ✅ |
| F-002 | ReaderPage 段落来源 | `parseText(text)` | `!!markdown ? getParagraphs(article) : parseText(text)` | ✅ |
| F-003 | 纯文本回退 | `parseText` | `parseText` — 与 `getParagraphs` 文本路径等效 | ✅ |
| F-004 | 段落索引一致性 | markdown 端 AST vs text split → 不一致 | 两端均通过 `getParagraphs` 计算段落 → 索引语义统一 | ⚠️ 见下方注 |

> **F-004 细化说明**：`calcProgress` 和 `paragraphs` 变量现在都使用 `getParagraphs`。但对于 markdown 文章的**阅读器渲染**，段落索引仍来自 `MarkdownContent` 的 `paraIdxRef`（AST 渲染时递增）。`getMarkdownParagraphs` 努力镜像此行为（M-001~M-008 全部匹配），但**两者不是同一段代码**——文本正则解析和 AST 遍历在数学上不完全等价。对 ReadRead 的常见 markdown 来源（EPUB → epub2md，手动粘贴），两者结果一致；极端边缘情况（嵌套引用块内嵌列表、HTML 标签等）可能存在微小偏差。审核请求中 D-003 已将此记录为已知偏差。✅ 标注合理。

### 3.2 多章节进度

| 编号 | 审核项 | 之前 | 之后 | 独立验证 |
|------|--------|------|------|------|
| F-005 | 进度计算范围 | 全文 text → 节内索引当作全文索引 | 按 section 逐节汇总 → 节 scope 正确 | ✅ |
| F-006 | 已完成章节计入 | 无 | `completedSections` Set → 全词数 | ✅ |
| F-007 | section 切换标记独立 | `paragraphIndex` 相同 = "同一位置" | `isSameMark` 增加 `sectionId` 比对 | ✅ |

### 3.3 状态保留

| 编号 | 审核项 | 之前 | 之后 | 独立验证 |
|------|--------|------|------|------|
| F-008 | `saveReadingMark` — `completed` | 写死 `false` | `existing?.completed ?? false` | ✅ |
| F-009 | `saveReadingMark` — `completedSections` | 未传递 → `[]` | `existing?.completed_sections ?? []` | ✅ |
| F-010 | `clearReadingMark` — `completedSections` | 未传递 → `[]` | `existing?.completed_sections ?? []` | ✅ |

**跨文件一致性验证**：`saveReadingMark` 和 `clearReadingMark` 的 read-before-write 模式与 `setReadingMarkCompleted`（[library.js:456](src/services/library.js#L456)）一致，使用了已在同一文件中验证过的 `fetchCloudReadingMarkRow` + `saveCloudReadingMarkRecord` 模式。✅

---

## 四、回归审核

### 4.1 核心功能路径未受影响

| 编号 | 审核项 | 改动触及 | 风险评估 | 结果 |
|------|--------|---------|---------|------|
| R-001 | 文章列表排序（`calcProgress` 比较器） | 是 — `calcProgress` 实现变更 | L536 比较器逻辑不变（两个进度值相减），仅数值来源变更 | ✅ |
| R-002 | `parseText` 函数 | 否 — 保留 | ReaderPage L31 定义保留，L187/L971 仍使用 | ✅ |
| R-003 | 非 markdown 路径 | 否 — 回退到 `parseText` | `!!article.markdown` 为 false → `parseText(text)` 原路径 | ✅ |
| R-004 | 本地模式（`!useCloudSource`）| 否 | `saveReadingMark`/`clearReadingMark` 本地分支未修改 | ✅ |
| R-005 | 收藏创建/删除 | 否 | 仅涉及 `handleSetReadingMark`，书签逻辑独立 | ✅ |
| R-006 | 标记已读完（`setReadingMarkCompleted`） | 否 | 已有 read-before-write，本次未修改 | ✅ |
| R-007 | `MarkdownContent` 渲染 | 否 | `paraIdxRef` 逻辑未修改 | ✅ |

### 4.2 `handleSetReadingMark` 依赖数组变更影响

`useCallback` 依赖数组增加了 `currentSection`。对以下场景的影响：

| 场景 | 之前 | 之后 | 评估 |
|------|------|------|------|
| 单章节文章 | `currentSection` = `null`，依赖稳定 | 同左 | ✅ 无变化 |
| 多章节 — 同 section 内点击 | `currentSection` 引用不变，回调不重建 | 同左 | ✅ 无变化 |
| 多章节 — 切换 section 后点击 | **旧回调闭包捕获旧 `currentSection.id`** → 保存错误 sectionId | 新回调捕获新 `currentSection.id` → 保存正确 sectionId | ✅ 修复了闭包过期 bug |

---

## 五、审核请求准确性复核

对审核请求本身的声明进行逐条复核：

| 审核请求声明 | 独立验证 | 一致性 |
|------------|---------|--------|
| "1865 modules，0 errors" | `npx vite build`：1865 modules，0 errors | ✅ |
| "156 行，含完整 markdown 边缘情况处理" | 156 行，M-001~M-010 全部确认 | ✅ |
| "无附加改动" | git diff 确认仅 4 个目标文件修改 | ✅ |
| "M-001 ~ M-010 全部通过" | 独立逐项验证全部通过 | ✅ |
| "M-011 ~ M-016 全部通过" | 独立逐项验证全部通过 | ✅ |
| "F-001 ~ F-004 段落统一" | 验证通过（F-004 见细微偏差注） | ⚠️ |
| "F-005 ~ F-010 状态/多章节" | 全部验证通过 | ✅ |
| "D-001 ~ D-003 已知偏差" | 偏差识别准确 | ✅ |

**F-004 细微偏差**：审核请求写「两端统一使用 `getParagraphs` → **一致**」。如前所述，markdown 文章的阅读器渲染端仍使用 `MarkdownContent` 的 AST 索引，与 `getMarkdownParagraphs` 的文本正则索引是「高度近似等价」而非「完全统一」。但这已在 D-003 中作为已知偏差记录，故此偏差**不影响审核结论**。

---

## 六、审核结论

### 6.1 整体评估

| 维度 | 独立评估 | 备注 |
|------|---------|------|
| 改动匹配方案 | ✅ | C-001~C-006 全部与方案 diff 一致，git diff 确认无附加改动 |
| 段落解析完整性 | ✅ | M-001~M-010 全部通过独立验证，边缘情况处理超出审核反馈要求 |
| 进度计算正确性 | ✅ | M-011~M-016 算法逐行追踪正确 |
| 修复后数据流 | ✅ | 段落统一、多章节汇总、状态保留全部正确 |
| 实施前审核反馈处理 | ✅ | C1/C2/C3 + S1/S2/S3 全部落实到代码 |
| 回归安全 | ✅ | 7 条路径验证，核心功能未受影响 |
| 构建验证 | ✅ | 1865 modules，0 errors，4.44s |

### 6.2 阻塞项

| 编号 | 问题描述 | 严重度 |
|------|---------|--------|
| — | **无** | — |

### 6.3 已知偏差（确认审核请求中的 D-001~D-003）

| 编号 | 偏差 | 独立评估 |
|------|------|---------|
| D-001 | `saveReadingMark` 2 次请求 | ✅ 可接受 — 低频点击操作；已记录为后续 RPC 优化 |
| D-002 | `calcMultiSectionProgress` 依赖未使用的 `completedSections` | ✅ 可接受 — 当前仅反映当前 section，但比修复前完全错位有本质改善 |
| D-003 | `getMarkdownParagraphs` 正则 vs AST 近似 | ✅ 可接受 — 当前文章来源不产生极端边缘情况；偏差远小于修复前的不同解析引擎完全不统一 |

### 6.4 审核签署

| 角色 | 结论 | 日期 |
|------|------|------|
| 独立审核人 | ✅ 通过 | 2026-06-20 |

---

## 附录 A：审核范围

| 文件 | 审核方式 |
|------|---------|
| `src/services/progress.js` (156 行) | 全文阅读 + 逐函数签名/逻辑验证 |
| `src/components/ImportPage.jsx` (diff) | git diff + 调用点确认 (L536, L545) |
| `src/components/ReaderPage.jsx` (diff) | git diff + `parseText` 保留确认 + 渲染分支确认 + `useCallback` 依赖追踪 |
| `src/services/library.js` (diff) | git diff + `setReadingMarkCompleted` 模式对照 |
| 构建产物 | `npx vite build` — 1865 modules, 0 errors |

## 附录 B：关键调用链追踪

```
用户点击段落标记按钮
  → ReaderPage.handleSetReadingMark(paraIndex)
    → isSameMark = (paragraphIndex === paraIndex) && (sectionId === currentSection.id) && !completed
      → true:  clearReadingMark → fetchCloudReadingMarkRow → saveCloudReadingMarkRecord (保留 completedSections)
      → false: saveReadingMark  → fetchCloudReadingMarkRow → saveCloudReadingMarkRecord (保留 completed + completedSections)

文章列表展示进度
  → ImportPage.render → calcProgress(article, mark)
    → markdown 文章: getParagraphs(article) → getMarkdownParagraphs
    → 纯文本文章: getParagraphs(article) → text.split(/\n+/)
    → 单章节: calcSingleProgress → getWordOffsetAtParagraph / getTotalWords
    → 多章节: calcMultiSectionProgress → 按 section 逐节汇总词数
```
