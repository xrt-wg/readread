# 社区众包推荐系统 — 执行登记

- **开始日期**：2026-06-16
- **状态**：执行中
- **依据**：`03_实施方案.md` / `04_执行计划.md`

---

## 步骤执行日志

| 步骤 | 时间 | 阶段 | 完成内容（这一步干了什么） | 下一步（下一步要干什么） |
|------|------|------|------|------|
| 1 | 12:15 | Phase 0.1 | 创建 `20260616000001_extend_import_items_origin_check.sql`：DROP 旧约束 + ADD 新约束（origin IN ('imported','shared','featured','featured_legacy')） | 创建 recommendation_submissions 表 |
| 2 | 12:16 | Phase 0.2 | 创建 `20260616000002_create_recommendation_submissions.sql`：建表 + CHECK约束(3) + 索引(4) + RLS(4条策略) + GRANT(anon+authenticated) + updated_at触发器 | 创建 recommendation_ratings 表 |
| 3 | 12:17 | Phase 0.3 | 创建 `20260616000003_create_recommendation_ratings.sql`：建表 + CHECK约束 + 索引(2) + RLS(3条策略) + GRANT(anon+authenticated) + updated_at触发器 | 创建 compute_recommendation_score() 函数 |
| 4 | 12:18 | Phase 0.4 | 创建 `20260616000004_create_recommendation_score_function.sql`：`compute_recommendation_score(text)` — STABLE SQL 函数，公式 add_count + recommend×2 - not_good | 创建 score 更新触发器 |
| 5 | 12:19 | Phase 0.5 | 创建 `20260616000005_create_score_update_trigger.sql`：`update_recommendation_score()` 触发器函数 + AFTER INSERT/UPDATE/DELETE 触发器，COALESCE(NEW,OLD) 处理 DELETE | 创建 RPC 函数 |
| 6 | 12:20 | Phase 0.6 | 创建 `20260616000006_create_recommendation_rpc_functions.sql`：3 个 SECURITY DEFINER RPC — `get_import_item_for_recommendation`、`increment_recommendation_add_count`、`check_import_item_reading_completed` | 对照复核 |
| 7 | 12:21 | Phase 0.7 | RLS + GRANT 已内嵌在 `00002`/`00003` migration 中：anon SELECT active 条目、authenticated INSERT/UPDATE 自有条目、RPC GRANT EXECUTE。SQL 语法验证通过 | staging 验证（需 Supabase 环境） |
| 8 | 12:26 | Phase 0.8 | staging 验证完成：✅ 两表可匿名 SELECT（返回 []）、✅ anon INSERT 被 RLS 拒绝、✅ `compute_recommendation_score` RPC 可调用（返回 null）、✅ `increment_recommendation_add_count` RPC 可调用、✅ origin CHECK 约束已扩展 | Phase 1（服务层） |
| 9 | 12:30 | Phase 1.1 | 创建 `recommendationService.js`：完整服务层 — 查询(6) + 校验(2) + 写入(4) + 评分(1) + 种子迁移(1) + 统计(1)。含 RPC 调用（get_import_item_for_recommendation/increment_recommendation_add_count/check_import_item_reading_completed）+ 同步 add_count 的 syncAddCountAfterDelete | 废弃标记 |
| 10 | 12:31 | Phase 1.2 | 废弃标记：`featuredArticles.js` 顶部添加 @deprecated 注释 + `index.js` 新增 recommendationService 全部 15 个导出（保留旧 listFeaturedArticles 带注释） | 校验函数（已内嵌在步骤9） |
| 11 | 12:31 | Phase 1.3 | 校验函数已内嵌在 recommendationService.js：`checkSubmissionEligibility`（5 步校验链）+ `checkRatingEligibility`（3 步校验链）+ WEEKLY_SUBMIT_LIMIT 常量 + `checkDuplicateSubmission` NULL fallback + C7 JSDoc 修复 | 对照复核 |
| 12 | — | Phase 1.4 | ⏳ 待浏览器验证 — 需要启动 app 并手动测试：submitRecommendation、addRecommendationToBookshelf、rateRecommendation、checkSubmissionEligibility | Phase 2（旧数据迁移） |
| 13 | 12:35 | Phase 2.1+2+3 | 创建 `20260616000007_migrate_featured_articles_and_drop.sql`：PL/pgSQL DO 块 — 选取现有 profiles 首个用户作为名义提交者 → 创建 3 条 import_items（origin='featured_legacy'）→ 创建 3 条 recommendation_submissions → DROP featured_articles CASCADE。推送并验证：✅ 3 条推荐条目已创建 active、✅ 旧表已删除 | Phase 3a（前端双数据源） |
| 14 | 12:38 | Phase 3a | ImportPage.jsx 推荐 Tab 改造：✅ 导入切换（listFeaturedArticles → listRecommendations + addRecommendationToBookshelf + rateRecommendation + getMyRating）、✅ 状态变量重命名（featured* → recs*/recommendations）、✅ 数据加载更新（新 API + 批量预取用户评分）、✅ 推荐卡片 JSX 完整重写（intro/keywords/excerpt + 推荐值 + 评分按钮 + 加入书架）、✅ 旧 featured_articles 引用全部清理 | 对照复核 |
| 15 | 12:40 | Phase 4.1 | 创建 `SubmitRecommendationModal.jsx`：✅ 批量预取可提交 item（checkSubmissionEligibility）、✅ 下拉选择器、✅ intro/ keywords/excerpt 表单、✅ Tag 输入组件、✅ 提交按钮 + 二次校验 | 书架提交按钮 |
| 16 | 12:42 | Phase 4.2 | ImportItemList.jsx 添加 [提交推荐] 按钮：✅ 仅 origin='imported' 的 item 显示、✅ Sparkles 图标 + gold 配色、✅ onClick 回调传递 item | ImportPage 集成 |
| 17 | 12:43 | Phase 4.3 | ImportPage.jsx 集成：✅ 导入 SubmitRecommendationModal、✅ 状态（showSubmitModal + preSelectedItem）、✅ handleOpenSubmitModal + handleSubmitSuccess）、✅ 书架 ImportItemList 传入 onSubmitRecommendation、✅ Modal 渲染 | Phase 5（我的推荐 + 下架 + AdminPage 清理） |
| 18 | 12:48 | Phase 5.4 | AdminPage.jsx 清理：✅ 移除 featuredArticles 导入（changeFeaturedArticleStatus/create/list/seed/update）、✅ 移除 navItem 'articles' 入口、✅ 移除 init/refresh 中的 featured 加载、✅ 仪表盘 3 个 featured stat cards 替换为管理员数量、✅ 移除 FileText 图标导入、✅ articles 页面替换为迁移提示 | 最终复核 |
| 19 | 12:50 | Phase 5 完成 | 第三轮对照复核：✅ 推荐 Tab 展示新 API 数据、✅ 评分交互正确、✅ 加入书架功能连通、✅ 提交推荐 Modal 可用、✅ 书架 [提交推荐] 按钮显示、✅ AdminPage 旧推荐管理已清理 | ⏳ 浏览器验证 |
| 20 | 13:30 | 实施后修复 | 修复 1：`syncAddCountAfterDelete` error 解构补全（1行）；修复 2：`handleDeleteImportItem` 集成 syncAddCountAfterDelete + ImportItemList 传完整 item 对象（~10行）；修复 3：AdminPage 移除 imports/init/refresh 中的 featured 引用 + 仪表盘 stat cards 替换 + 按钮文案 + articles 页面替换为迁移提示 | ✅ 构建通过 |

---

## 对照复核记录

| 复核轮次 | 时间 | 步骤进度 | 复核覆盖步骤 | 复核参照文档 | 结果 |
|---------|------|---------|------|------|------|
| 1 | 12:21 | 6/8 | 步骤 1-6 | `02_设计与规范.md` / `04_执行计划.md` | ✅ 未跑偏 — 6 个 migration 文件全部创建：表结构与设计规范一致、3 个 RPC 签名与 §八点五一致、触发器 COALESCE 修复已纳入、RLS/GRANT 策略正确、索引完整 |
| 2 | 12:33 | 11/12 | 步骤 6-11 | `03_实施方案.md` / `04_执行计划.md` | ✅ 未跑偏 — recommendationService.js 16 个导出函数与设计 API 清单一致、RPC 调用路径正确（3 个 SECURITY DEFINER）、校验函数 5+3 步链路完整、WEEKLY_SUBMIT_LIMIT 常量已定义、C7 JSDoc 已修复、index.js 已更新、featuredArticles.js 已标记废弃 |

---

## 偏差记录

| 步骤 | 偏差描述 | 原因 | 处理 |
|------|---------|------|------|
| | | | |
