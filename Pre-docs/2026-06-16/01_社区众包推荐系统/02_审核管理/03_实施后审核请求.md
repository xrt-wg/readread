# 社区众包推荐系统 — 实施后审核请求

- **日期**：2026-06-16
- **状态**：待审核
- **审核对象**：社区众包推荐系统全量实施交付物
- **依据文档**：
  - `../01_开发管理/01_决策记录.md`
  - `../01_开发管理/02_设计与规范.md`
  - `../01_开发管理/03_实施方案.md`
  - `../01_开发管理/04_执行计划.md`
  - `../01_开发管理/05_执行登记.md`
  - `01_实施前审核反馈.md`
  - `02_审核反馈处理记录.md`

---

## 一、审核说明

### 1.1 审核目标

对照设计与规范文档，逐项验证实施成果是否符合预期，确保：

- `import_items.origin` CHECK 约束已扩展为 4 个值
- `recommendation_submissions` 表 schema、约束、索引、RLS、GRANT 正确
- `recommendation_ratings` 表 schema、约束、索引、RLS、GRANT 正确
- `compute_recommendation_score()` 函数正确
- `update_recommendation_score()` 触发器 + COALESCE 修复正确
- 3 个 SECURITY DEFINER RPC 函数正确且安全
- `recommendationService.js` 完整 API 功能正确
- 旧 `featured_articles` 表已废弃，数据已迁移
- 推荐 Tab 展示新 API 数据（intro/keywords/excerpt + 推荐值 + 评分）
- 评分交互正确（三级评分 + 资格校验）
- 加入书架功能正确（跨用户 RPC 深拷贝 + add_count 更新）
- 提交推荐 Modal + 书架 [提交推荐] 按钮正确
- AdminPage 旧推荐管理已清理
- 旧 `featuredArticles.js` 已标记废弃

### 1.2 审核方式

| 标记 | 含义 |
|------|------|
| ✅ 通过 | 实施完全符合规范，无遗留问题 |
| ⚠️ 有条件通过 | 实施基本符合规范，存在已知的、可接受的偏差 |
| ❌ 不通过 | 实施与规范存在实质性偏差，需修复后重新审核 |
| — 不适用 | 该项在当前阶段不适用 |

---

## 二、Phase 0 审核：数据库层

### 2.1 origin CHECK 约束扩展

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| O-001 | `import_items_origin_check` 已扩展为 `('imported','shared','featured','featured_legacy')` | 02-§3.4.1 | |
| O-002 | 旧约束已 DROP，新约束已 ADD（非叠加） | 04-Phase0/0.1 | |
| O-003 | INSERT origin='featured' → 成功 | staging | |

### 2.2 recommendation_submissions 表

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| S-001 | 表已创建，全部字段与设计一致（id, submitter_user_id, import_item_id, title, author, source_url, intro, keywords(text[]), excerpt, add_count, recommend_score, status, created_at, updated_at） | 02-§3.1 | |
| S-002 | `status` CHECK 约束：`IN ('active', 'removed')` | 02-§3.1 | |
| S-003 | `intro` CHECK 约束：`length(intro) > 0` | 02-§3.1 | |
| S-004 | `excerpt` CHECK 约束：`length(excerpt) > 0` | 02-§3.1 | |
| S-005 | `keywords` 类型为 `text[]`，默认值 `'{}'` | 02-§3.1 | |
| S-006 | 索引 `idx_rec_submissions_active_score` 存在（条件索引 WHERE status='active'） | 02-§5 | |
| S-007 | 索引 `idx_rec_submissions_submitter` 存在 | 02-§5 | |
| S-008 | 索引 `idx_rec_submissions_source_url` 存在（条件索引 WHERE source_url IS NOT NULL AND status='active'） | 02-§5 | |
| S-009 | 索引 `idx_rec_submissions_import_item` 存在 | 02-§5 | |

### 2.3 recommendation_ratings 表

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| R-001 | 表已创建，字段与设计一致（submission_id, user_id, rating, created_at, updated_at） | 02-§3.2 | |
| R-002 | `rating` CHECK 约束：`IN ('recommend', 'average', 'not_good')` | 02-§3.2 | |
| R-003 | PRIMARY KEY `(submission_id, user_id)` 存在 | 02-§3.2 | |
| R-004 | 索引 `idx_rec_ratings_submission` 存在 | 02-§5 | |
| R-005 | 索引 `idx_rec_ratings_user` 存在 | 02-§5 | |

### 2.4 函数与触发器

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| F-001 | `compute_recommendation_score(text)` 函数存在，LANGUAGE sql STABLE | 02-§3.3 | |
| F-002 | 公式正确：`add_count + recommend_count * 2 - not_good_count` | 02-§3.3 | |
| F-003 | `update_recommendation_score()` 触发器函数存在 | 02-§8 | |
| F-004 | 使用 `COALESCE(NEW.submission_id, OLD.submission_id)` 处理 DELETE 场景 | 02-§8 | |
| F-005 | `RETURN COALESCE(NEW, OLD)` | 02-§8 | |
| F-006 | 触发器 `trg_rec_ratings_update_score` AFTER INSERT/UPDATE/DELETE ON recommendation_ratings | 02-§8 | |
| F-007 | `trg_rec_submissions_set_updated_at` 触发器：BEFORE UPDATE → `set_record_updated_at()` | 02-§8 | |
| F-008 | `trg_rec_ratings_set_updated_at` 触发器：BEFORE UPDATE → `set_record_updated_at()` | 02-§8 | |

### 2.5 SECURITY DEFINER RPC 函数

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| P-001 | `get_import_item_for_recommendation(p_import_item_id text)` 存在，SECURITY DEFINER | 02-§8.5 | |
| P-002 | 安全门控：仅存在活跃推荐条目时才返回数据 | 02-§8.5 | |
| P-003 | `increment_recommendation_add_count(p_submission_id text, p_user_id uuid)` 存在，SECURITY DEFINER | 02-§8.5 | |
| P-004 | 内部 `COUNT(DISTINCT user_id)` 计算权威 add_count 值 | 02-§8.5 | |
| P-005 | 同时更新 `add_count` 和 `recommend_score` | 02-§8.5 | |
| P-006 | `check_import_item_reading_completed(p_import_item_id text, p_user_id uuid)` 存在，SECURITY DEFINER | 02-§8.5 | |
| P-007 | 单 SQL JOIN 查询 `articles JOIN reading_marks` | 02-§8.5 | |
| P-008 | 3 个 RPC 均 `SET search_path = public` | 02-§8.5 | |
| P-009 | 3 个 RPC 均 `GRANT EXECUTE TO authenticated` | 02-§8.5 | |

### 2.6 RLS 策略

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| L-001 | `recommendation_submissions` RLS 已启用 | 02-§6.1 | |
| L-002 | "Anyone can view active submissions"：anon 可 SELECT active 条目 | 02-§6.1 | |
| L-003 | "Users can view own submissions"：已登录用户可读自己的所有条目 | 02-§6.1 | |
| L-004 | "Users can insert submissions"：仅可插入归属自己的条目 | 02-§6.1 | |
| L-005 | "Users can update own submissions"：仅提交者可更新自己的条目 | 02-§6.1 | |
| L-006 | 未授权 DELETE（通过 status='removed' 下架） | 02-§6.1 | |
| L-007 | `recommendation_ratings` RLS 已启用 | 02-§6.2 | |
| L-008 | "Anyone can view ratings"：所有人可读评分 | 02-§6.2 | |
| L-009 | "Users can insert ratings"：仅可插入自己的评分 | 02-§6.2 | |
| L-010 | "Users can update own ratings"：仅可更新自己的评分 | 02-§6.2 | |
| L-011 | 未授权 DELETE（评分不可删除） | 02-§6.2 | |

### 2.7 GRANT 权限

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| G-001 | `GRANT SELECT ON recommendation_submissions TO anon` | 02-§7 | |
| G-002 | `GRANT SELECT, INSERT, UPDATE ON recommendation_submissions TO authenticated` | 02-§7 | |
| G-003 | `GRANT SELECT ON recommendation_ratings TO anon` | 02-§7 | |
| G-004 | `GRANT SELECT, INSERT, UPDATE ON recommendation_ratings TO authenticated` | 02-§7 | |
| G-005 | 3 个 RPC 函数均 `GRANT EXECUTE TO authenticated` | 02-§8.5 | |

### 2.8 功能无退化

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| D-001 | 现有 `import_items` 表不受新约束影响 | 04-Phase0 | |
| D-002 | 现有 `articles`、`reading_marks`、`bookmarks` 表不受影响 | 04-Phase0 | |
| D-003 | 现有书架 CRUD、移入、阅读、收藏、回顾功能正常 | 04-Phase0 | |

---

## 三、Phase 1 审核：服务层

### 3.1 recommendationService.js — 查询

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| Q-001 | `listRecommendations({limit, offset, sort})` → `{items, total}` | 03-§3.2.2 | |
| Q-002 | 仅返回 `status='active'` 条目，按 `recommend_score DESC` 排序 | 02-§9.2 | |
| Q-003 | `getRecommendation(submissionId)` → `{submission, ratings}` | 03-§3.2.2 | |
| Q-004 | `listMySubmissions(userId)` → 按 `created_at DESC` 返回用户自己的提交 | 03-§3.2.2 | |
| Q-005 | `getMyRating(submissionId, userId)` → Rating \| null | 03-§3.2.2 | |

### 3.2 recommendationService.js — 校验

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| V-001 | `checkSubmissionEligibility(userId, importItemId)` 5 步校验链：存在性 → origin='imported' → completed → 周限制(引用 WEEKLY_SUBMIT_LIMIT 常量) → 重复检测 | 03-§3.2.4 | |
| V-002 | 周限制检查调用 `countWeeklySubmissions`，常量 `WEEKLY_SUBMIT_LIMIT = 1` | 03-§3.2.4 | |
| V-003 | `checkDuplicateSubmission`：source_url 优先去重 → NULL 时 fallback 到 title | 03-§3.2.4 | |
| V-004 | `checkRatingEligibility(userId, submissionId)` 3 步校验链：hasAdded → importItem 存在 → completed | 03-§3.2.5 | |
| V-005 | `checkReadingCompleted` 通过 RPC `check_import_item_reading_completed` 单 SQL JOIN 实现 | 03-§3.2.4 | |

### 3.3 recommendationService.js — 写入

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| W-001 | `submitRecommendation({importItemId, intro, keywords, excerpt}, userId)` 正确读取快照 + INSERT | 03-§3.2.2 | |
| W-002 | 提交前二次校验（intro/excerpt 非空） | 03-§3.2.2 | |
| W-003 | `addRecommendationToBookshelf(submissionId, userId)` 步骤 2 通过 RPC `get_import_item_for_recommendation` 跨用户读取 | 03-§3.2.3 | |
| W-004 | 步骤 3 `structuredClone` 深拷贝 sections | 03-§3.2.3 | |
| W-005 | 步骤 4 通过 `createImportItem` 工厂函数创建新 import_item（origin='featured', shareSourceId=submissionId） | 03-§3.2.3 | |
| W-006 | 步骤 5 通过 RPC `increment_recommendation_add_count` 更新 add_count + score | 03-§3.2.3 | |
| W-007 | `updateRecommendation(submissionId, patch, userId)` 仅允许提交者更新自己的条目 | 03-§3.2.2 | |
| W-008 | `removeRecommendation(submissionId, userId)` → status='removed' | 03-§3.2.2 | |
| W-009 | `syncAddCountAfterDelete(importItem)` 当 origin∈{featured, featured_legacy} 且 shareSourceId 非空时调用 RPC | 03-§3.2.3 | |

### 3.4 recommendationService.js — 评分

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| R-001 | `rateRecommendation(submissionId, rating, userId)` UPSERT 语义（ON CONFLICT submission_id, user_id） | 03-§3.2.2 | |
| R-002 | 评分后触发器自动更新 recommend_score | 02-§8 | |

### 3.5 recommendationService.js — 种子数据 + 统计

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| M-001 | `migrateLegacyFeaturedArticles()` 幂等检查（已存在则跳过） | 03-§3.2.2 | |
| M-002 | `getRecommendationStats()` → `{totalSubmissions, totalRatings}` | 03-§3.2.2 | |

### 3.6 废弃标记

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| D-001 | `featuredArticles.js` 顶部有 `@deprecated` 注释 | 04-Phase1/1.2 | |
| D-002 | `index.js` 中 `listFeaturedArticles` 导出保留（含 @deprecated 注释） | 04-Phase1/1.2 | |
| D-003 | `index.js` 新增 recommendationService 全部 15 个导出 | 04-Phase1/1.2 | |
| D-004 | `importItems.js` JSDoc origin 参数已更新为 4 个值 | C7 | |

---

## 四、Phase 2 审核：旧数据迁移

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| T-001 | migration `20260616000007` 创建并推送成功 | 04-Phase2/2.2 | |
| T-002 | 3 篇种子文章已创建 import_items（origin='featured_legacy'） | 04-Phase2/2.2 | |
| T-003 | 3 条 recommendation_submissions 已创建（status='active'） | 04-Phase2/2.2 | |
| T-004 | 迁移幂等：重复执行不产生重复数据 | 04-Phase2/2.2 | |
| T-005 | `featured_articles` 表已 DROP（查询返回 "Could not find the table"） | 04-Phase2/2.3 | |

---

## 五、Phase 3 审核：推荐 Tab 改造 + 评分

### 5.1 数据加载

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| L-001 | ImportPage 导入从 `listFeaturedArticles` 切换为 `listRecommendations` | 04-Phase3/3.4 | |
| L-002 | 状态变量从 `featured*` 重命名为 `recs*/recommendations` | 04-Phase3/3.4 | |
| L-003 | useEffect 在 `isAuthenticated` 和 `userId` 变化时重新加载 | 04-Phase3/3.4 | |
| L-004 | 加载时批量预取当前用户的评分（`getMyRating`） | 04-Phase3/3.4 | |

### 5.2 推荐卡片展示

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| C-001 | 卡片展示：title (Playfair Display)、author（若有）、source_url hostname（若有） | 02-§11 | |
| C-002 | 卡片展示：intro（介绍） | 02-§11 | |
| C-003 | 卡片展示：keywords 标签列表（gold 背景色） | 02-§11 | |
| C-004 | 卡片展示：excerpt 摘录（斜体 Lora 字体 + 左侧 gold 边框） | 02-§11 | |
| C-005 | 卡片底部：推荐值 + 添加人数 | 02-§11 | |

### 5.3 评分组件

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| R-001 | 三级评分按钮：推荐 / 一般 / 不行 | 02-§2.3 | |
| R-002 | 当前用户评分高亮（gold 背景 + gold-dark 边框） | 03-§4.5 | |
| R-003 | 评分后列表刷新（重新拉取 recommendations） | 03-§6.3 | |
| R-004 | 未登录用户可浏览推荐但不能评分（点击触发 requireAuth） | 03-§4.2.3 | |

### 5.4 加入书架

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| A-001 | [加入] 按钮调用 `addRecommendationToBookshelf(submissionId, userId)` | 03-§5.1 | |
| A-002 | 已加入的条目按钮变灰显示 "已加入 ✓"（通过 importItems.shareSourceId 匹配） | 03-§5.1 | |
| A-003 | 加入后乐观更新 add_count（+1） | 03-§5.1 | |
| A-004 | toast "已加入书架" 并刷新书架列表 | 03-§5.1 | |

### 5.5 状态处理

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| S-001 | 加载中：Skeleton 提示 "正在加载推荐内容…" | 04-Phase3 | |
| S-002 | 错误：红色错误卡片 + 错误信息 | 04-Phase3 | |
| S-003 | 空列表："还没有推荐内容，成为第一个推荐者" 引导 CTA | 04-Phase3 | |
| S-004 | Supabase 未配置：提示 "当前未配置云端推荐内容服务" | 04-Phase3 | |

---

## 六、Phase 4 审核：提交推荐

### 6.1 SubmitRecommendationModal

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| M-001 | 组件存在，包含批量资格预取逻辑（checkSubmissionEligibility 遍历 origin='imported' 的 items） | 04-Phase4/4.1 | |
| M-002 | 下拉选择器仅显示可提交的 item | 04-Phase4/4.1 | |
| M-003 | 无可提交内容时展示说明文案 | 04-Phase4/4.1 | |
| M-004 | intro textarea + 字数提示 | 04-Phase4/4.1 | |
| M-005 | keywords Tag 输入（回车添加 + × 删除 + 最多 5 个） | 04-Phase4/4.1 | |
| M-006 | excerpt textarea | 04-Phase4/4.1 | |
| M-007 | 提交前二次校验（非空检查） | 04-Phase4/4.1 | |
| M-008 | 提交成功后关闭 Modal + toast "推荐已提交" + 刷新推荐列表 | 04-Phase4/4.1 | |

### 6.2 书架 [提交推荐] 按钮

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| B-001 | ImportItemList 仅对 `origin='imported'` 的 item 显示按钮 | 04-Phase4/4.2 | |
| B-002 | 按钮使用 Sparkles 图标 + gold 配色 | 04-Phase4/4.2 | |
| B-003 | 点击按钮打开 SubmitRecommendationModal（预选当前 item） | 04-Phase4/4.2 | |
| B-004 | ImportPage 中 ImportItemList 传入 `onSubmitRecommendation` prop | 04-Phase4/4.3 | |

---

## 七、Phase 5 审核：清理

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| C-001 | AdminPage 导入中已移除 `changeFeaturedArticleStatus`/`createFeaturedArticle`/`listFeaturedArticles`/`seedBuiltInFeaturedArticles`/`updateFeaturedArticle` | 04-Phase5/5.4 | |
| C-002 | navItems 中 `articles` 入口已移除 | 04-Phase5/5.4 | |
| C-003 | 后台初始化 useEffect 中 featured_articles 加载已移除 | 04-Phase5/5.4 | |
| C-004 | 仪表盘 featuredSummary stat cards（3 个）已替换为管理员数量 + 审计记录 | 04-Phase5/5.4 | |
| C-005 | `articles` 页面显示迁移提示（而非报错） | 04-Phase5/5.4 | |
| C-006 | ImportPage 中 `src/components/SubmitRecommendationModal` 导入存在且路径正确 | 04-Phase5 | |

---

## 八、跨阶段综合审核

### 8.1 向后兼容

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| Z-001 | 所有现有用户数据可正常加载 | 设计原则 | |
| Z-002 | 书架 CRUD 不受推荐系统影响 | 03-§10 | |
| Z-003 | 阅读、收藏、回顾链路完全不变 | 03-§10 | |
| Z-004 | 匿名用户可浏览推荐列表（anon SELECT active 条目） | 03-§6.3 | |
| Z-005 | 匿名用户提交/评分需登录（requireAuth 门控） | 03-§6.3 | |

### 8.2 数据一致性

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| C-001 | `add_count` 权威值由 RPC `increment_recommendation_add_count` 从 `import_items` 实时计算 | 03-§6.1 | |
| C-002 | `recommend_score` 由评分触发器自动更新 | 02-§8 | |
| C-003 | `add_count` 变化时 score 同步更新（RPC 内同时更新两者） | 02-§8.5 | |
| C-004 | 用户删除书架内容 → `syncAddCountAfterDelete` → add_count 重新计算 | 03-§3.2.3 | |

### 8.3 跨用户安全

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| S-001 | 提交者的 import_item 通过 SECURITY DEFINER RPC 读取（非直接 SELECT） | 02-§8.5 | |
| S-002 | RPC `get_import_item_for_recommendation` 安全门控：仅存在活跃推荐条目时才返回 | 02-§8.5 | |
| S-003 | `recommendation_submissions` 的 add_count 通过 SECURITY DEFINER RPC 更新（非提交者不能直接 UPDATE） | 02-§8.5 | |
| S-004 | 评分仅限评分者本人操作（RLS `auth.uid() = user_id`） | 02-§6.2 | |

### 8.4 代码质量

| 编号 | 审核项 | 依据 | 结果 |
|------|--------|------|------|
| Q-001 | `recommendationService.js` 所有函数使用 `getSupabaseClient()` 模式（与现有服务一致） | client.js | |
| Q-002 | 行映射函数 `mapRecommendationRow`/`mapRatingRow` snake_case → camelCase | 服务层惯例 | |
| Q-003 | 错误处理复用 `isLibraryAccessError` / `resolveLibraryErrorMessage`（从 errorUtils.js 导入） | 03-§3.2.6 | |
| Q-004 | 业务规则拒绝通过 `{canSubmit, reason}` 返回（不抛异常） | 03-§3.2.6 | |
| Q-005 | `WEEKLY_SUBMIT_LIMIT` 常量硬编码为 1（远期可配置化） | 03-§3.2.4 | |
| Q-006 | `checkDuplicateSubmission` 处理 source_url=NULL 的 fallback | 03-§3.2.4 | |

---

## 九、审核结论

### 9.1 整体评估

| 阶段 | 状态 | 备注 |
|------|------|------|
| Phase 0：数据库层 | | |
| Phase 1：服务层 | | |
| Phase 2：旧数据迁移 | | |
| Phase 3：推荐 Tab + 评分 | | |
| Phase 4：提交推荐 | | |
| Phase 5：清理 + 完善 | | |

### 9.2 阻塞项

| 编号 | 问题描述 | 对应审核项 | 责任人 | 期限 |
|------|---------|-----------|--------|------|
| | | | | |

### 9.3 已知偏差

| 编号 | 偏差描述 | 原因 | 后续计划 |
|------|---------|------|---------|
| | Phase 2 先于 Phase 3 执行（与执行计划建议顺序相反） | Phase 0.1 约束扩展后旧 featured_articles 读取不受影响，直接切换新 API 更为简洁 | 无后续计划——偏差为有意的简化 |
| | 评分分布未实时展示在卡片上（仅展示推荐值 + 添加人数） | Phase 3 卡片设计将评分按钮与评分分布分开展示，当前阶段评分分布展示为远期优化 | 后续 Phase 可增加评分分布展示（推荐 N / 一般 N / 不行 N） |
| | "我的推荐"列表未作为独立视图实现 | Phase 5 中精简为推荐 Tab 内的评分交互，独立管理视图为远期优化 | 后续 Phase 可增加 "我的推荐" 筛选视图 |

### 9.4 审核签署

| 角色 | 姓名 | 日期 | 签署 |
|------|------|------|------|
| 审核人 | | | |
| 技术负责人 | | | |
| 产品负责人 | | | |

---

## 十、附录：快速验证脚本

### A.1 数据库层验证

```
1. Supabase Dashboard → recommendation_submissions 表存在，字段定义正确
2. Supabase Dashboard → recommendation_ratings 表存在，字段定义正确
3. RLS 策略列表：submissions 4 条 + ratings 3 条
4. 函数列表：compute_recommendation_score / update_recommendation_score 存在
5. RPC 列表：get_import_item_for_recommendation / increment_recommendation_add_count / check_import_item_reading_completed 存在
6. curl 测试：anon 可 SELECT recommendation_submissions → []
7. curl 测试：anon INSERT → RLS 拒绝
8. curl 测试：compute_recommendation_score RPC 可调用
```

### A.2 服务层验证

```
1. listRecommendations → 返回 3 条 migrated 种子数据
2. addRecommendationToBookshelf → import_items 出现新行（origin='featured'）
3. rateRecommendation → recommendation_ratings 出现新行
4. score 触发器：INSERT rating → recommend_score 自动更新
5. checkSubmissionEligibility → 未读完返回 canSubmit:false
6. checkRatingEligibility → 未添加返回 canRate:false
```

### A.3 前端功能验证

```
推荐 Tab:
  1. 加载 3 篇种子文章
  2. 每篇展示 intro / keywords / excerpt
  3. 评分按钮可点击（需登录）
  4. [加入] 按钮 → toast "已加入书架" → 按钮变 "已加入 ✓"

书架 Tab:
  5. origin='imported' 的 item 显示 [✨ 提交推荐] 按钮
  6. 点击按钮 → SubmitRecommendationModal 打开
  7. 下拉选择器仅显示可提交的 item
  8. 填写 intro/keywords/excerpt → 提交 → toast "推荐已提交"

AdminPage:
  9. 无 articles 管理标签
  10. 仪表盘 stat cards 无 featuredSummary
  11. 页面无报错
```

### A.4 端到端完整流程

```
提交推荐:
  1. 登录 → 书架 → 导入一篇英文文章
  2. 移入阅读区 → 阅读 → 标记读完
  3. 书架 → 点击 [✨] → 填写推荐信息 → 提交
  4. 切换到推荐 Tab → 新推荐出现

发现与评分:
  5. 另一用户登录 → 推荐 Tab → 看到该推荐
  6. 点击 [加入] → 书架出现新素材
  7. 移入阅读区 → 阅读 → 标记读完
  8. 返回推荐 Tab → 点击 [推荐] 评分
  9. 推荐值更新
```
