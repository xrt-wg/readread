# 实施过程登记

---

## 2026-06-13 — Phase 0 完成

### 任务 0.1：JSDoc 类型定义文件 ✅
- `src/types/document.js` — Document、Section 类型 + migrateArticleToDocument()
- `src/types/bookmark.js` — Bookmark、ReadingProgress 类型 + resolveSectionId()、resolveSectionHeading()
- `src/types/extractor.js` — ExtractionResult、ExtractionSection、ExtractorInput 类型 + fileToExtractorInput()
- 全部通过运行时验证

### 任务 0.2：向后兼容映射工具 ✅
- `library.js`：
  - ARTICLE_COLUMNS + author, format, cover_url, lang, sections, section_count
  - BOOKMARK_COLUMNS + section_id, section_heading
  - READING_MARK_COLUMNS + section_id, completed_sections
  - mapArticleRow / mapBookmarkRow / mapReadingMarkRow 返回新旧字段
  - saveArticle / saveBookmark / saveCloudReadingMarkRecord 写入新字段
  - assertImportData 接受 version 1 和 2
  - exportLibraryData version 1 → 2
- 构建成功（796.83 kB）

### 任务 0.3：数据库迁移脚本 ✅
- `supabase/migrations/20260613140000_extend_articles_for_document_model.sql`
  - ALTER TABLE articles/bookmarks/reading_marks 新增字段
  - CHECK 约束
  - 现有数据回填 SQL
- **需用户协作**：在 Supabase 上执行 migration

### 任务 0.4-0.6：storage.js 适配 + localStorage 回填 + 导出 v2 ✅
- `src/store/storage.js`：
  - createDocument() — doc_ 前缀 + 单 section
  - createArticle() deprecated 别名
  - createBookmark() + sectionId/sectionHeading
  - exportData() v2
  - importData() v1/v2 兼容 + 自动升级
  - migrateLocalStorageArticles()
  - readingMarkStore 新字段兼容
- `src/main.jsx`：启动时调用 migrateLocalStorageArticles()
- 构建成功（798.08 kB）

### 任务 0.7：ExtractorInput buffer 类型 + format 枚举 ✅
- 已在任务 0.1 的 extractor.js 中完成

---

## 2026-06-13 — Phase 1 完成

### 任务 1.1：提取器接口实现 ✅
- `src/services/extractors/index.js`：
  - createDocument(ExtractionResult) — 完整工厂
  - deriveParentIds(sections) — depth 推导算法
  - EXTRACTORS 注册表（初始全 null）

### 任务 1.2：现有提取器适配 ✅
- `urlExtractor.js` — 复用 fetchArticleFromUrl
- `pasteExtractor.js` — 纯文本包装
- `markdownExtractor.js` — ##/###/#### 标题扫描 + frontmatter 解析
- `htmlExtractor.js` — Readability + h2-h4 扫描
- 构建成功（798.08 kB，提取器未被使用故体积不变）

### 任务 1.3：ImportPage 重构 ✅
- 移除 fetchArticleFromUrl、Readability、htmlToMarkdown、createArticle 直接引用
- 三 Tab 统一走 EXTRACTORS[format] + createDocument 管道
- App.jsx handleImport 改为接受 Document
- 推荐阅读用 createDocumentFromStorage
- 构建成功（+3.76 kB → 801.84 kB）

### 任务 1.4：epub2MD 适配器 ✅
- `npm install epub2md`
- epub2MD 内部深度依赖 Node.js 内置模块，浏览器端 polyfill 链路脆弱
- 切换为 Netlify Function 方案：`netlify/functions/parse-epub.js`
  - 服务端运行 epub2MD.parseEpub()
  - TOC 树遍历 + spine 降级 + body 填充
  - 封面图提取（原始 base64）
- 前端 `epubExtractor.js`：调用 Function + 封面缩放 + 图片阈值
- vite.config.js 撤掉 polyfill，恢复原始配置
- 构建成功（+1.41 kB → 803.25 kB，epub2MD 不在客户端 bundle）

### 任务 1.5：deriveParentIds 工厂推导 ✅
- 已在 extractors/index.js 的 createDocument 中实现

### 任务 1.6：EPUB 封面图提取 ✅
- Netlify Function 服务端提取封面 → 返回原始 base64
- 前端 epubExtractor.js Canvas 缩放至 max 300px

---

## 2026-06-13 — Phase 2 完成

### 任务 2.0-2.5：ReaderPage 渲染层适配 ✅
- `ReaderPage.jsx`：
  - `currentSectionIdx` 状态 — 多 section 文档切换
  - `hasMultipleSections` 检测 — 单 section 文档走完全旧路径
  - 顶栏：section 标题 + 目录按钮（sectionCount > 1 时）
  - `handleBookmark` 注入 sectionId + sectionHeading
  - `handleSetReadingMark` 传入 sectionId（更新 saveReadingMark API）
  - `handleJumpToReadingMark` 先切换 section 再滚动
  - 渲染内容切换为 currentBody（多 section）或 article.text/markdown（单 section）
- `SectionTocPanel.jsx` 新建：
  - `buildTocTree(sections)` — depth 栈推导树
  - 点击节点切换 section + 关闭面板
  - 当前 section 高亮
- `library.js`：saveReadingMark 增加 sectionId 参数
- 构建成功（+2.93 kB → 806.18 kB）

---

## 2026-06-13 — Phase 3 完成

### 任务 3.1：回顾面板适配 ✅
- `ReviewModal.jsx` CardBack 新增 sectionHeading 展示

### 任务 3.2：文章库列表适配 ✅
- `ImportPage.jsx` 列表项新增 sectionCount 展示（> 1 时显示 "N 章"）

### 任务 3.3：备份导入导出适配 ✅
- 已在 Phase 0 完成：exportData v2、importData v1/v2 兼容、assertImportData 双版本

### 任务 3.4：数据迁移脚本适配 ✅
- 已在 Phase 0 完成：library.js mapArticleRow/mapBookmarkRow/mapReadingMarkRow 新字段映射

---

## 全部完成

| Phase 0 | ✅ | 类型 + DB + storage + localStorage 回填 + 导出 v2 |
| Phase 1 | ✅ | 5 个提取器 + ImportPage 重构 + epub2MD(Netlify Function) + 封面图 |
| Phase 2 | ✅ | ReaderPage 多 section + TOC 面板 + section 级段落索引 |
| Phase 3 | ✅ | 回顾面板 + 文章库 + 备份兼容 + 迁移字段映射 |

**最终构建**：806.56 kB（净增 ~9.7 kB）

---

## 需用户协作

- **数据库迁移**：在 Supabase 上执行 `supabase/migrations/20260613140000_extend_articles_for_document_model.sql`
- **Netlify Function 部署**：`netlify/functions/parse-epub.js` 需随项目部署
