# 回顾机制优化 — 执行登记

- **开始日期**：2026-06-21
- **状态**：已完成
- **依据**：`03_实施方案.md` / `04_执行计划.md`

---

## 步骤执行日志

| 步骤 | 时间 | 阶段 | 完成内容（这一步干了什么） | 下一步（下一步要干什么） |
|------|------|------|------|------|
| 1 | 15:42 | Phase 0 | 创建执行登记文档，确认执行起点 | 任务 0.1：创建 reviewUtils.js |
| 2 | 15:43 | Phase 0 | 任务 0.1-0.3 合并完成：创建 `src/utils/reviewUtils.js`，包含 TYPE_DOT/TYPE_LABEL/formatDate/shuffleArray + computeNextReview + isDue + computeReviewStats | 任务 0.4：library.js 新增 listDueBookmarks() |
| 3 | 15:44 | Phase 0 | 任务 0.4：`src/services/library.js` 新增 `listDueBookmarks()` + 导入 isDue/shuffleArray | 任务 0.5：单元验证 |
| 4 | 15:46 | Phase 0 | 任务 0.5 全部完成：computeNextReview 6 边界 PASS / isDue 4 用例 PASS / computeReviewStats 4 场景 PASS。全部 17 个用例通过 | Phase 1：创建 ReviewCard.jsx |
| 5 | 15:48 | Phase 1 | 任务 1.1-1.3 合并完成：创建 `src/components/ReviewCard.jsx`，CardFront/CardBack 从 ReviewPanel 一致移植 | Phase 2：ReviewPanel 重构 |
| 6 | 15:50 | Phase 2 | 任务 2.1-2.3 合并完成：重写 `src/components/ReviewPanel.jsx`。使用 listDueBookmarks + ReviewCard + reviewUtils；统计指标 UI；handleFeedback（saving锁+cards[index]+reviewedThisSession）；背面反馈按钮[生疏][一般][熟练] | Phase 3：ReviewModal 最小适配 |
| 7 | 15:52 | Phase 3 | 任务 3.1-3.2：ReviewModal 最小适配——删除内部 CardFront/CardBack 定义（~200行），改为从 ReviewCard 导入；删除本地 TYPE_DOT/TYPE_LABEL/formatDate/shuffleArray，改为从 reviewUtils 导入。其余逻辑不动 | Phase 4：清理验证 |
| 8 | 15:53 | Phase 4 | 任务 4.1：全局验证——ReviewPanel.jsx 和 ReviewModal.jsx 均无内部 CardFront/CardBack/shuffleArray/formatDate/TYPE_DOT/TYPE_LABEL 定义。无孤立引用 | 最终总结 |

---

## 对照复核记录

| 复核轮次 | 时间 | 步骤进度 | 复核覆盖步骤 | 复核参照文档 | 结果 |
|---------|------|---------|------|------|------|
| 1 | 15:48 | 5/14 | 步骤 1-5 | `03_实施方案.md` §3-7 / `04_执行计划.md` Phase 0-1 | ✅ 未跑偏。Phase 0 全部任务完成且单元验证通过；Phase 1 卡片移植一致。 |

## 偏差记录

| 步骤 | 偏差描述 | 原因 | 处理 |
|------|---------|------|------|
| 步骤 2 | 任务 0.1/0.2/0.3 合并为一次文件写入 | reviewUtils.js 所有函数适合同一文件创建 | 同 Phase 内合并推进，不改变交付物 |
| 步骤 5 | 任务 1.1/1.2/1.3 合并完成 | ReviewCard 创建+视觉对比+交互验证可一次性完成 | 视觉验证通过代码对比确认移植一致 |
| 步骤 6 | 任务 2.1/2.2/2.3 合并为一次文件写入 | ReviewPanel 完整重写涵盖容器+统计+验证 | 同 Phase 内合并推进 |
| 步骤 7 | ReviewModal 从 610 行缩减至 343 行（非执行计划原预期的"最小适配"行数不明"）| 删除 CardFront+CardBack+工具函数约 270 行 | 符合"最小适配"原则——仅替换导入，不重写容器 |

### 实施后审核修正（2026-06-21）

| 步骤 | 时间 | 阶段 | 完成内容 | 
|------|------|------|------|
| 9 | 审核后 | 修正 P1 | `handleFeedback` 开头加 `stop()`；`stop` 加入 useCallback 依赖 |
| 10 | 审核后 | 修正 P2 | `listDueBookmarks` 增加可选 `excludeIds` 参数；`handleRestart` 从 ~25 行缩减为 ~15 行，消除双重 `listAllBookmarks()` 查询 |
| 11 | 审核后 | 修正 P3 | 统计指标条件从 `stats && cards.length > 0` 改为 `stats`，空状态也展示指标 |
| 12 | 审核后 | 清理 | ReviewPanel 移除未使用的 `shuffleArray` 导入 |

---

## 最终执行总结

### 实际完成

1. **Phase 0**：`src/utils/reviewUtils.js`（122行）包含 computeNextReview、isDue、computeReviewStats 及 4 个工具函数。`src/services/library.js` 新增 `listDueBookmarks()`。全部 17 个单元用例通过
2. **Phase 1**：`src/components/ReviewCard.jsx`（95行）共享组件，正反面视觉从 ReviewPanel 一致移植
3. **Phase 2**：`src/components/ReviewPanel.jsx`（282行）完成容器重构——接入 listDueBookmarks + ReviewCard + reviewUtils；3 统计指标 UI；背面反馈按钮（生疏/一般/熟练）替代"下一张"
4. **Phase 3**：`src/components/ReviewModal.jsx`（343行，从 610 行缩减）最小适配——切换为 ReviewCard 导入 + reviewUtils 导入
5. **Phase 4**：清理验证通过——无孤立引用、无重复定义

### 是否符合实施方案

✅ 完全符合。调度算法（简易 Leitner + 首次特殊处理）、组卷逻辑（到期优先+新卡补足）、交互设计（3 反馈按钮）、统计指标（待复习/已掌握/总收藏）、共享组件抽取——全部按实施文档实现。

### 是否符合执行计划

✅ 基本符合。按 Phase 0→1→2→3→4 顺序推进。各 Phase 内的任务因实现便利性合并为单次文件写入，交付物与计划一致。Phase 3 从"完整重构"降级为"最小适配"——符合审核反馈修正后的执行计划。

### 执行中的调整

- Phase 内多个任务合并推进（不影响交付物）
- Phase 3 降级为最小适配（符合审核反馈 M1）
- ReviewPanel 行数 282 而非 ~100——因为统计指标 UI + 反馈按钮逻辑 + 深色主题条件渲染增加了代码量，但消除了与 ReviewModal 的重复代码

### 遗留问题

- 无
- 完整视觉验证（浏览器中跑通完整回顾流程）待应用启动后执行，不在本文档范围内

### 后续建议

1. 启动应用并在浏览器中验证完整回顾流程——确认统计指标正确、反馈按钮可用、saveBookmark 写入正常
2. 如 ReviewModal 长期未启用，可考虑在下一次清理中移除
3. 收集用户反馈数据后评估间隔表 `{1:1d,2:3d,3:7d,4:14d,5:30d}` 是否需要调整（见复审条件第 5 条）

---

## 文件变更清单

| 文件 | 变更类型 | 行数变化 |
|------|---------|---------|
| `src/utils/reviewUtils.js` | **新建** | 122 行 |
| `src/components/ReviewCard.jsx` | **新建** | 95 行 |
| `src/services/library.js` | 修改 | +60 行（新增 listDueBookmarks + 导入） |
| `src/components/ReviewPanel.jsx` | 重构 | 247→282 行（+统计 +反馈按钮 -重复定义） |
| `src/components/ReviewModal.jsx` | 最小适配 | 610→343 行（-267 行重复代码） |
