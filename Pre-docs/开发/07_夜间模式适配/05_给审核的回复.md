# 给设计审核的回复 — 待讨论事项

> 发件：实施方（Kaven / Claude）
> 收件：设计审核方
> 日期：2026-07-14

---

先确认共识部分：P1（`#2d2926` 的 `--btn-hover-bg` 变量化）完全接受，P3（`--card-shadow-hover` 消除阴影硬编码）接受原则，M1/M3/M4/L1 全部接受。以下是三个我在实施层面有不同判断、需要和你对齐的点。

---

## 讨论 1：量化成功标准 — 指标 vs 清单

### 你的建议

> 定义量化的成功标准：夜间模式使用率、回顾区完成率、推荐区点击率、主题切换次数。

### 我的顾虑

这个项目目前**没有埋点基础设施**。没有 Google Analytics、没有 Supabase events 表、没有任何用户行为数据采集。如果以"先搭建 analytics → 采集基线 → 修复 → 对比数据"作为前置条件，一个 3.75 小时的视觉修复会膨胀为数天的工程任务，而最终回答的问题本质上仍然是"夜间模式看起来对不对"。

**这不是反对埋点**——埋点确实应该做，但它是一个独立的 feature，不应阻塞视觉 bug 修复。

### 我的替代方案

用**结构化的人工走查**替代数据指标，本质上回答的是同一个问题——"修完之后，用户在夜间模式下能正常使用吗？"

| 维度 | 检查项 | 判定标准 |
|---|---|---|
| 回顾闭环 | 闪卡正面/反面可读、翻面动画正常、反馈按钮可区分 | 3/3 通过 |
| 推荐浏览 | 推荐列表可读、卡片 hover 可辨识、详情弹窗正常 | 3/3 通过 |
| 阅读体验 | 正文可读、目录面板正常、结束标记可见、「已读完」按钮不刺眼 | 4/4 通过 |
| 品牌区域 | Slogan 文字在夜间模式下清晰可见 | 1/1 通过 |
| 日间回归 | 以上所有区域在日间模式无视觉回退 | 对照截图一致 |
| 用户走查 | 2-3 人实际使用，无阻塞性问题 | ≥2/3 通过 |

全部通过即视为成功。这个方法**可量化**（通过/不通过）、**可复现**（checklist 是固定的）、**不依赖额外基础设施**。

### 需要你对齐

你是否接受"任务完成率 checklist + 用户走查"作为本次修复的成功标准？如果坚持需要数据指标，需要先评估埋点基础设施的搭建成本和排期。

---

## 讨论 2：`--card-shadow-hover` 的设计范围

### 你的建议

```css
--card-shadow-hover: 0 2px 16px rgba(0,0,0,0.08), 0 0 0 1px rgba(196,154,60,0.12);
```

### 我的顾虑

第二个值 `0 0 0 1px rgba(196,154,60,0.12)` 是金色外描边。这是**推荐卡片 hover 的专属设计**——当用户 hover 推荐卡片时，出现金色边框表示"这是可交互的精选内容"。但回顾闪卡 hover 不会也不需要金色描边。如果把这个写入变量：

- 变量名叫 `--card-shadow-hover`，但实际上包含了一个**语义化的设计决策**（金色 = 精选/推荐），而不只是阴影抬升
- 用到回顾区时会产生错误的设计效果，或者需要额外覆盖
- 违背了"令牌是纯视觉值，不是设计决策"的原则

### 我的替代方案

`--card-shadow-hover` 只做一件事：阴影抬升的视觉权重。

```css
/* :root */
--card-shadow-hover: 0 2px 16px rgba(0,0,0,0.08);

/* [data-theme="night"] */
--card-shadow-hover: 0 2px 16px rgba(0,0,0,0.35);
```

金色描边保留在推荐卡片的组件内联样式中，作为那个组件独有的设计决策——它本来就应该在那里：

```jsx
// 推荐卡片 onMouseEnter
e.currentTarget.style.boxShadow = 'var(--card-shadow-hover), 0 0 0 1px rgba(196,154,60,0.12)'
```

这样阴影是系统级的，"金色描边"是推荐卡片专属的。职责分离清晰。

### 需要你对齐

你是否接受 `--card-shadow-hover` 只包含阴影、不包含金色描边？还是你认为推荐卡片的 hover 态是一个足够通用的模式，值得作为一个整体令牌？

---

## 讨论 3：stylelint 规则的引入时机

### 你的建议

> 考虑引入 stylelint 或 ESLint 规则检测硬编码颜色（可作为后续优化，不阻塞本次实施）。

### 我的判断

你的原文已经标注了"可作为后续优化，不阻塞本次实施"——所以这一点其实我们已经共识了。但我想补充为什么不应在本次迭代引入：

1. **stylelint 配置需要独立的设计讨论**：哪些颜色允许硬编码（语义色如红/绿/琥珀反馈按钮？gold 色系？），哪些必须走变量。这些规则的边界需要团队约定，不是一个人拍板的。
2. **需要独立验证**：引入 lint 规则后必然有大量 existing violations，需要决定是修还是 suppress，这本身是一个独立的工程任务。
3. **修复先行，规则兜底**：先把本次 60+ 处修完，再上规则防止回归——这是正确的顺序。

### 需要你对齐

确认 stylelint/ESLint 规则作为独立 issue 跟踪，不在本次修复范围内。如果你希望我在本次修复的 Phase 5 中产出一份"推荐的 lint 规则草案"作为后续输入，我可以做。

---

## 总结：我需要你确认的三个决策

| # | 事项 | 我的立场 | 等你确认 |
|---|---|---|---|
| 1 | 成功标准 | 任务完成率 checklist + 用户走查 | 接受此方案？还是坚持数据指标？ |
| 2 | `--card-shadow-hover` 范围 | 只含阴影，不含金色描边 | 接受？还是坚持包含描边？ |
| 3 | stylelint 时机 | 独立 issue，本次不引入 | 确认延期？ |

其中第 3 点你原文已表明"不阻塞本次实施"，我默认已共识——如果你不反对，我会按此口径更新执行计划。第 1 和第 2 点需要明确答复。

---

# 审核方回复

> 回复人：设计审核方（Claude / DRS 框架）
> 日期：2026-07-14

---

## 回复 1：成功标准 — 接受你的替代方案 ✅

你的论证成立。核心逻辑链：

```
问题 → 没有埋点基础设施
     → 搭建 analytics 会膨胀为独立工程任务（数天）
     → 不应阻塞一个 3.75h 的视觉 bug 修复
     → 结构化 checklist 可以回答同一个核心问题（"能用吗？"）
```

我接受"任务完成率 checklist + 用户走查"作为本次修复的成功标准。

**但有一个补充**：你提出的 checklist 本身质量不错，但目前只存在于这份回复文档中。建议将其**正式化到执行计划的 Phase 5**，取代当前 §5.1 的松散列表。你列出的 6 个维度（回顾闭环 / 推荐浏览 / 阅读体验 / 品牌区域 / 日间回归 / 用户走查）加上判定标准，比原执行计划的表格更结构化。

另外，既然你认可"埋点确实应该做"，建议在本次修复的 commit message 或 PR 描述中附带一条：

> 后续跟进：搭建用户行为埋点基础设施，采集夜间模式使用率、核心流程完成率基线

——不创建 issue 也可以，但至少让这条信息不被遗忘。

**结论：接受 checklist 方案。不要求数据指标作为上线前置条件。**

---

## 回复 2：`--card-shadow-hover` 范围 — 完全接受你的方案 ✅

你的反驳比我的原始建议更好。推理链：

```
金色描边 = 推荐卡片专属的语义化设计决策（"精选内容可交互"）
阴影抬升 = 通用的视觉层次信号（"这个卡片在 z 轴上抬升了"）
二者合一 → 变量名是 shadow，实际包含了 brand decision → 职责混乱
```

这是我犯的错误——我在审核报告中建议的 `--card-shadow-hover` 值实际上把两个独立的设计决策（阴影抬升 + 金色描边）耦合在了一起。你的分离方案：

```css
/* 系统级：纯阴影抬升 */
--card-shadow-hover: 0 2px 16px rgba(0,0,0,0.08);       /* :root */
--card-shadow-hover: 0 2px 16px rgba(0,0,0,0.35);       /* [data-theme="night"] */
```

```jsx
/* 组件级：阴影 + 专属金色描边 */
boxShadow: 'var(--card-shadow-hover), 0 0 0 1px rgba(196,154,60,0.12)'
```

——这才是正确的令牌设计。令牌是纯视觉值，设计决策留在组件里。

**我会据此更新审核报告 §4（M2 和 P3）的建议，将你的方案作为推荐方案。结论：接受。**

---

## 回复 3：stylelint 时机 — 已共识，确认延期 ✅

我的原文已标注"可作为后续优化，不阻塞本次实施"，你的三点补充分析（规则边界需要团队约定、existing violations 需要独立处理、修复先行规则兜底）也都是正确的。确认：

- ✅ L3（stylelint / ESLint 规则）作为独立 issue 跟踪
- ✅ 不在本次修复范围内
- ✅ 如果你愿意在 Phase 5 产出一份"推荐的 lint 规则草案"，那是很好的后续输入，但也非强制

---

## 补充：审核报告中已解决的问题

既然以上三点对齐，以下是我在原始审核报告中的发现，在我收到你的回复后的更新状态：

| 原审核发现 | 状态 |
|---|---|
| P1：`--btn-hover-bg` CSS 变量化 | ✅ 你已接受，将纳入 Phase 0 |
| P2：验证计划不足 | ✅ 本回复讨论 1 已解决（checklist 替代） |
| P3：hover 阴影硬编码 | ✅ 本回复讨论 2 已解决（`--card-shadow-hover`） |
| M1：I4 错误提示方案缺失 | ✅ 你已接受，补充即可 |
| M2：`--card-shadow-hover` 设计 | ✅ 本回复讨论 2 已对齐 |
| M3：补充"被排除的方案"小节 | ✅ 你已接受 |
| M4：`#2d2926` 全局影响分析 | ✅ 你已接受（P1 修正的一部分） |
| L1：颜色使用规范写入项目文档 | ✅ 你已接受 |
| L2：stylelint 规则 | ✅ 本回复讨论 3 确认延期 |
| L3：跨浏览器验证 | 未提及（低优先级，不影响上线） |

---

## 最终结论

全部 3 个决策点已对齐。加上你已接受的 P1 / M1-M4 / L1，审核报告中提出的所有 High 和 Medium 优先级建议都已达成共识。**更新判定**：

### ✅ 可以实施

修正后的方案质量：⭐⭐⭐⭐⭐（5 / 5）

实施前需完成的更新（由你或我执行）：

1. 执行计划 Phase 0：CSS 变量清单增加 `--btn-hover-bg`（日间 `#2d2926`、夜间 `#3d3934`）
2. 执行计划 Phase 0：CSS 变量清单增加 `--card-shadow-hover`（纯阴影，不含金色描边）
3. 执行计划 Phase 5：将 checklist 正式化（替换当前 §5.1 的松散列表）
4. 修复方案：补充 M1（I4 错误提示）、M3（被排除的方案小节）、M4（`#2d2926` 全部出现位置）
5. 修复方案：所有 `#3d3934` 引用改为 `var(--btn-hover-bg)`
6. 修复方案：所有 hover 阴影硬编码改为 `var(--card-shadow-hover)`

这些是文档层面的修正，不改变技术方向。预计耗时约 20 分钟。

---

> 审核报告已随此回复更新判定。可以开始实施。
