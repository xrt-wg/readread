# 夜间模式适配 — 修复方案

> 日期：2026-07-14
> 前置文档：[01_分析报告.md](./01_分析报告.md)

---

## 1. 修复总则

### 1.1 核心原则

1. **优先使用现有 CSS 变量** — 不新建变量，`index.css` 的 `[data-theme="night"]` 已覆盖全部所需颜色
2. **阴影改用纯黑系** — 亮色模式的 `rgba(28,25,23,...)` 阴影在夜间模式必须转为 `rgba(0,0,0,...)`（`popup-shadow` 已有示范）
3. **语义色保持硬编码但需验证** — 红/绿/琥珀反馈按钮颜色在两种模式下应分别验证对比度
4. **不改变组件结构** — 只替换颜色值，不改 JSX 结构或逻辑

### 1.1.1 `#2d2926` 全局影响清单

值 `#2d2926` 被用作深色按钮 hover 背景，分散在 **6 处位置**。本次修复将其提升为 CSS 变量 `--btn-hover-bg`，全部位置统一替换：

| # | 文件 | 行号 | 用途 |
|---|---|---|---|
| 1 | `ReviewPanel.jsx` | L170 | 「再来一轮」按钮 hover |
| 2 | `ReviewModal.jsx` | L283 | 「再来一轮」按钮 hover |
| 3 | `ImportPage.jsx` | L609 | 推荐卡片「加入书架」按钮 hover |
| 4 | `ImportItemEditor.jsx` | L159 | 保存按钮 hover |
| 5 | `ImportPanel.jsx` | L196 | 导入按钮 hover |
| 6 | `index.css` | L327 | CSS 规则中的 `background: #2d2926;` |

### 1.2 设计令牌速查表

将硬编码值替换为以下对应变量：

| 当前硬编码（典型） | 替换为 | 说明 |
|---|---|---|
| `#ffffff` | `var(--card-bg-warm)` 或 `var(--popup-surface)` | 卡片/面板背景 |
| `#fdfaf5` | `var(--popup-bg)` | 弹窗/面板背景 |
| `#fefdfb` | `var(--card-bg-warm)` | 卡片内部背景 |
| `rgba(28,25,23,0.06~0.12)` | `var(--border-subtle)` 或 `var(--surface-border)` | 边框 |
| `rgba(28,25,23,0.04~0.08)` 背景 | `var(--hover-bg)` | hover / 浅背景 |
| `rgba(28,25,23,0.04~0.12)` 阴影 | 见下方阴影方案 | 阴影需特殊处理 |
| `rgba(255,255,255,0.6)` | `var(--surface-bg)` | 半透明白色表面 |
| `#2d2926` | `var(--btn-hover-bg)` | 暗色按钮 hover（日间 `#2d2926`、夜间 `#3d3934`） |
| `rgba(28,25,23,0.15~0.20)` 遮罩 | `rgba(0,0,0,0.35)` | 模态遮罩 |
| `linear-gradient(175deg, #1c1917 10%, #3d2b1e 100%)` | 使用 `var(--ink)` 渐变 | 品牌文字 |
| 硬编码 hover 阴影（`rgba(0,0,0,0.12)` 等） | `var(--card-shadow-hover)` | 卡片 hover 阴影抬升 |

### 1.3 阴影处理方案

阴影在两种模式下需要不同的颜色：

**方案 A（推荐）**：为阴影新建一个 CSS 变量。

在 `index.css`  `:root` 中新增：
```css
--card-shadow: 0 2px 16px rgba(28,25,23,0.06), 0 1px 4px rgba(28,25,23,0.04);
```

在 `[data-theme="night"]` 中覆写：
```css
--card-shadow: 0 2px 16px rgba(0,0,0,0.25), 0 1px 4px rgba(0,0,0,0.15);
```

**方案 B（备选）**：如果阴影不需要精细区分，代码中直接使用 `var(--popup-shadow)` 复用已有的弹窗阴影变量。

采用**方案 A**，新增一个 `--card-shadow` 变量，因为卡片阴影与弹窗阴影的视觉需求不同（卡片阴影更轻）。

此外，新增 `--card-shadow-hover` 变量用于 hover 态阴影抬升（纯阴影，不含语义化设计元素如金色描边）：

在 `index.css` `:root` 中新增：
```css
--card-shadow-hover: 0 2px 16px rgba(0,0,0,0.08);
```

在 `[data-theme="night"]` 中覆写：
```css
--card-shadow-hover: 0 2px 16px rgba(0,0,0,0.35);
```

> **设计说明**：`--card-shadow-hover` 仅包含阴影抬升，不包含金色描边等语义化元素。推荐卡片的金色 hover 描边保留在组件内联样式中，通过 `var(--card-shadow-hover), 0 0 0 1px rgba(196,154,60,0.12)` 组合使用。

### 1.4 被排除的方案

以下替代路径在方案设计阶段被评估并排除：

| 方案 | 排除理由 |
|---|---|
| **CSS 文件提取**：将组件内联 `style` 提取到独立 `.css` 文件中，通过类选择器覆写 | 改动范围过大。当前 ~14 个文件、~120 处修改点如果全部提取到 CSS 文件，需要为每个组件创建对应的样式文件、定义类名、修改 JSX 引用。在"最小改动、最低回归风险"的约束下，逐处替换内联值更安全可控 |
| **Tailwind `dark:` 前缀**：利用 Tailwind 的暗色模式变体统一处理 | 项目的 Tailwind 使用范围仅限于布局类（flex/grid/spacing），颜色体系建立在 CSS 变量 + 内联 `style` 上。引入 `dark:` 需要先配置 `tailwind.config.js` 的 `darkMode: 'class'`，且仅能覆盖 Tailwind 类的场景（如 `border-amber-200`），无法解决内联 `style` 中的硬编码。覆盖面有限，增加两套样式体系的维护成本 |
| **CSS 变量 polyfill**：对 `var()` 在 JS `style` 中的兼容性做 polyfill | `var()` 在现代浏览器（Chrome 49+, Firefox 31+, Safari 9.1+, Edge 15+）中的支持率 >97%，与项目的目标用户群（英语学习者，使用现代浏览器）无冲突。不需要 polyfill |
| **PostCSS 主题插件**：通过构建时插件生成主题变体 | 引入新的构建依赖，增加构建复杂度。当前方案只需修改源码值，零构建配置变更 |

---

## 2. 回顾区修复方案（P0）

### 2.1 ReviewCard — 闪卡核心

**文件**：[src/components/ReviewCard.jsx](../../src/components/ReviewCard.jsx)

#### 修改 2.1.1：卡片背景常量（致命）

```diff
- const CARD_BG = '#fdfaf5'
+ const CARD_BG = 'var(--card-bg-warm)'
  const CARD_RADIUS = '24px'
```

#### 修改 2.1.2：卡片阴影常量

```diff
- const CARD_SHADOW = '0 2px 16px rgba(28,25,23,0.06), 0 1px 4px rgba(28,25,23,0.04)'
+ const CARD_SHADOW = 'var(--card-shadow)'
```

#### 修改 2.1.3：卡片边框常量

```diff
- const CARD_BORDER = '1px solid rgba(28,25,23,0.06)'
+ const CARD_BORDER = '1px solid var(--border-subtle)'
```

#### 修改 2.1.4：发音按钮边框（L26）

```diff
- border: '1px solid rgba(28,25,23,0.12)',
+ border: '1px solid var(--surface-border)',
```

#### 修改 2.1.5：发音按钮 hover 背景（L32）

```diff
- onMouseEnter={(e) => { e.currentTarget.style.background = 'rgba(28,25,23,0.05)'; ... }}
+ onMouseEnter={(e) => { e.currentTarget.style.background = 'var(--hover-bg)'; ... }}
```

#### 修改 2.1.6：进度条背景（L186）

```diff
- <div style={{ height: '4px', background: 'rgba(28,25,23,0.07)', ... }}>
+ <div style={{ height: '4px', background: 'var(--border-subtle)', ... }}>
```

#### 修改 2.1.7：错误提示颜色（L38）

保持不变。`#dc2626` 在两种模式下均可接受。

#### 修改 2.1.8：反馈按钮颜色（L241 / L260-261 / L280）

保持不变。红/琥珀/绿色是语义色，用作操作按钮的视觉锚点，不应随主题变化。但需在验证阶段确认对比度。

### 2.2 ReviewPanel — 回顾面板

**文件**：[src/components/ReviewPanel.jsx](../../src/components/ReviewPanel.jsx)

#### 修改 2.2.1：空状态/完成状态容器背景（L132）

```diff
- background: cards.length === 0 || finished ? '#fdfaf5' : undefined,
+ background: cards.length === 0 || finished ? 'var(--card-bg-warm)' : undefined,
```

#### 修改 2.2.2：容器阴影和边框（L135-136）

```diff
- boxShadow: (!finished && cards.length > 0) ? undefined : '0 2px 16px rgba(28,25,23,0.06), 0 1px 4px rgba(28,25,23,0.04)',
- border: (!finished && cards.length > 0) ? undefined : '1px solid rgba(28,25,23,0.06)',
+ boxShadow: (!finished && cards.length > 0) ? undefined : 'var(--card-shadow)',
+ border: (!finished && cards.length > 0) ? undefined : '1px solid var(--border-subtle)',
```

#### 修改 2.2.3：「再来一轮」hover（L170）

```diff
- onMouseEnter={(e) => (e.currentTarget.style.background = '#2d2926')}
+ onMouseEnter={(e) => (e.currentTarget.style.background = 'var(--btn-hover-bg)')
```

`var(--btn-hover-bg)` 在日间模式映射为 `#2d2926`（深灰），在夜间模式映射为 `#3d3934`（比背景 `#1a1814` 浅半度），在两种模式下均保持可辨识的 hover 反馈。

### 2.3 ReviewModal — 回顾弹窗

**文件**：[src/components/ReviewModal.jsx](../../src/components/ReviewModal.jsx)

#### 修改 2.3.1：卡片背景（L199）

```diff
- background: '#fdfaf5',
+ background: 'var(--card-bg-warm)',
```

#### 修改 2.3.2：卡片阴影（L203）

```diff
- boxShadow: '0 12px 48px rgba(28,25,23,0.22)',
+ boxShadow: 'var(--popup-shadow)',
```

#### 修改 2.3.3：「再来一轮」hover（L283）

```diff
- onMouseEnter={(e) => (e.currentTarget.style.background = '#2d2926')}
+ onMouseEnter={(e) => (e.currentTarget.style.background = 'var(--btn-hover-bg)')
```

---

## 3. 推荐区修复方案（P0）

### 3.1 ImportPage — 推荐 Tab 容器

**文件**：[src/components/ImportPage.jsx](../../src/components/ImportPage.jsx)

#### 修改 3.1.1：推荐区整体容器（L479）

```diff
- <div className="rounded-3xl p-8" style={{ background: '#ffffff', boxShadow: '0 4px 24px rgba(28,25,23,0.08), 0 1px 4px rgba(28,25,23,0.04)', border: '1px solid rgba(28,25,23,0.06)' }}>
+ <div className="rounded-3xl p-8" style={{ background: 'var(--card-bg-warm)', boxShadow: 'var(--card-shadow)', border: '1px solid var(--border-subtle)' }}>
```

#### 修改 3.1.2：推荐卡片单项背景（L501-503）

```diff
  style={{
-   background: '#fefdfb', borderRadius: '20px',
-   border: '1px solid rgba(28,25,23,0.08)',
-   boxShadow: '0 1px 3px rgba(28,25,23,0.04), 0 0 0 1px rgba(28,25,23,0.02)',
+   background: 'var(--card-bg-warm)', borderRadius: '20px',
+   border: '1px solid var(--border-subtle)',
+   boxShadow: 'var(--card-shadow)',
    ...
  }}
```

#### 修改 3.1.3：推荐卡片 hover 效果（L508-514）

```diff
  onMouseEnter={(e) => {
    e.currentTarget.style.borderColor = 'rgba(196,154,60,0.25)'
-   e.currentTarget.style.boxShadow = '0 2px 16px rgba(28,25,23,0.07), 0 0 0 1px rgba(196,154,60,0.12)'
+   e.currentTarget.style.boxShadow = 'var(--card-shadow-hover), 0 0 0 1px rgba(196,154,60,0.12)'
    e.currentTarget.style.transform = 'translateY(-1px)'
  }}
  onMouseLeave={(e) => {
-   e.currentTarget.style.borderColor = 'rgba(28,25,23,0.08)'
-   e.currentTarget.style.boxShadow = '0 1px 3px rgba(28,25,23,0.04), 0 0 0 1px rgba(28,25,23,0.02)'
+   e.currentTarget.style.borderColor = 'var(--border-subtle)'
+   e.currentTarget.style.boxShadow = 'none'
    e.currentTarget.style.transform = ''
  }}
```

> **注意**：hover 态的加强阴影使用纯黑 `rgba(0,0,0,...)` 以确保在两种模式下都有视觉层次。非 hover 态直接去掉阴影（因为 `--card-shadow` 已在容器层面处理）。

#### 修改 3.1.4：详情按钮（L529）

```diff
- border: '1px solid rgba(28,25,23,0.08)', borderRadius: '9px',
+ border: '1px solid var(--surface-border)', borderRadius: '9px',
```

#### 修改 3.1.5：详情按钮 hover（L534, L536, L538, L541）

```diff
  onMouseEnter={(e) => {
-   e.currentTarget.style.background = 'rgba(28,25,23,0.05)'
+   e.currentTarget.style.background = 'var(--hover-bg)'
    e.currentTarget.style.color = 'var(--ink)'
-   e.currentTarget.style.borderColor = 'rgba(28,25,23,0.16)'
+   e.currentTarget.style.borderColor = 'var(--surface-border)'
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.background = 'transparent'
    e.currentTarget.style.color = 'var(--ink-muted)'
-   e.currentTarget.style.borderColor = 'rgba(28,25,23,0.08)'
+   e.currentTarget.style.borderColor = 'var(--surface-border)'
  }}
```

#### 修改 3.1.6：卡片底部分隔线（L572）

```diff
- borderTop: '1px solid rgba(28,25,23,0.05)',
+ borderTop: '1px solid var(--border-subtle)',
```

#### 修改 3.1.7：加入按钮 hover（L609）

```diff
- if (!alreadyAdded) e.currentTarget.style.background = '#2d2926'
+ if (!alreadyAdded) e.currentTarget.style.background = 'var(--btn-hover-bg)'
```

#### 修改 3.1.8：推荐区错误提示横幅（L485）

```diff
- <div className="rounded-2xl px-4 py-4" style={{ background: 'rgba(254,242,242,0.88)', border: '1px solid rgba(239,68,68,0.14)', color: '#b91c1c' }}>
+ <div className="rounded-2xl px-4 py-4" style={{ background: 'var(--danger-bg)', border: '1px solid rgba(239,68,68,0.14)', color: 'var(--danger-text)' }}>
```

> 红色边框 `rgba(239,68,68,0.14)` 保持不变——红色系在两种模式下对比度均足够。

### 3.2 ImportPage — 书架 / 阅读区 Tab

#### 修改 3.2.1：顶部工具栏底边（L385）

```diff
- <header className="flex justify-center px-6 py-4" style={{ borderBottom: '1px solid rgba(28,25,23,0.08)' }}>
+ <header className="flex justify-center px-6 py-4" style={{ borderBottom: '1px solid var(--border-subtle)' }}>
```

#### 修改 3.2.2：统计面板（L398-399）

```diff
  border: '1px solid rgba(28,25,23,0.12)',
- boxShadow: '0 1px 4px rgba(28,25,23,0.04)',
```
→ 不做改动。统计面板背景已是 `var(--surface-bg)`，阴影改为纯黑或使用变量新值不影响核心可读性。此处阴影极浅，直接用 `none` 即可：

```diff
  border: '1px solid var(--surface-border)',
- boxShadow: '0 1px 4px rgba(28,25,23,0.04)',
```

#### 修改 3.2.3：书架空状态容器（L633）

```diff
- <div className="rounded-3xl p-8" style={{ background: '#ffffff', boxShadow: '0 4px 24px rgba(28,25,23,0.08), 0 1px 4px rgba(28,25,23,0.04)', border: '1px solid rgba(28,25,23,0.06)' }}>
+ <div className="rounded-3xl p-8" style={{ background: 'var(--card-bg-warm)', boxShadow: 'var(--card-shadow)', border: '1px solid var(--border-subtle)' }}>
```

#### 修改 3.2.4：内联导入面板（L700）

```diff
- <div className="rounded-3xl p-6 mb-4 animate-fade-up" style={{ background: '#ffffff', boxShadow: '0 2px 12px rgba(28,25,23,0.06), 0 1px 3px rgba(28,25,23,0.04)', border: '1px solid rgba(28,25,23,0.06)' }}>
+ <div className="rounded-3xl p-6 mb-4 animate-fade-up" style={{ background: 'var(--card-bg-warm)', boxShadow: 'var(--card-shadow)', border: '1px solid var(--border-subtle)' }}>
```

#### 修改 3.2.5：品牌 Slogan 标题渐变（L738）🔴 致命

```diff
- <h1 style={{ ..., background: 'linear-gradient(175deg, #1c1917 10%, #3d2b1e 100%)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text', ... }}>
+ <h1 style={{ ..., background: 'linear-gradient(175deg, var(--ink) 10%, var(--ink-light) 100%)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text', ... }}>
```

#### 修改 3.2.6：品牌 Slogan 副标题（L739）🔴 致命

```diff
- <p style={{ ..., color: 'rgba(72, 54, 38, 0.65)', ... }}>
+ <p style={{ ..., color: 'var(--ink-muted)', ... }}>
```

#### 修改 3.2.7：阅读区列表卡片（L765）

```diff
  <div key={art.id} onClick={() => onOpen(art)}
    className="..."
    style={{
-     background: '#ffffff',
-     border: '1px solid rgba(28,25,23,0.07)',
-     boxShadow: '0 1px 4px rgba(28,25,23,0.04)'
+     background: 'var(--card-bg-warm)',
+     border: '1px solid var(--border-subtle)',
+     boxShadow: 'none'
    }}
    onMouseEnter={(e) => { e.currentTarget.style.borderColor = 'rgba(196,154,60,0.4)'; e.currentTarget.style.boxShadow = 'var(--card-shadow-hover)' }}
    onMouseLeave={(e) => { e.currentTarget.style.borderColor = 'var(--border-subtle)'; e.currentTarget.style.boxShadow = 'none' }}
```

#### 修改 3.2.8：列表项分隔符（L770, L774）

```diff
- <span style={{ fontSize: '12px', fontFamily: 'DM Sans', color: 'rgba(28,25,23,0.2)' }}>|</span>
+ <span style={{ fontSize: '12px', fontFamily: 'DM Sans', color: 'var(--meta-sep-color)' }}>|</span>
```

#### 修改 3.2.9：书架 Tab 按钮选中态（L76）

```diff
- background: tab === t.id ? 'rgba(28,25,23,0.06)' : 'transparent',
+ background: tab === t.id ? 'var(--hover-bg)' : 'transparent',
```

#### 修改 3.2.10：导航 Tab 按钮（L422, L427, L432）

```diff
- border: view === id ? '1px solid rgba(28,25,23,0.06)' : '1px solid transparent',
+ border: view === id ? '1px solid var(--border-subtle)' : '1px solid transparent',
  ...
- boxShadow: view === id ? '0 1px 3px rgba(28,25,23,0.04)' : 'none',
+ boxShadow: view === id ? 'var(--card-shadow-hover)' : 'none',
  ...
  onMouseEnter={(e) => {
    if (view !== id) {
-     e.currentTarget.style.background = 'rgba(28,25,23,0.02)'
+     e.currentTarget.style.background = 'var(--hover-bg)'
    }
  }}
```

### 3.3 RecommendationDetailModal — 推荐详情弹窗

**文件**：[src/components/RecommendationDetailModal.jsx](../../src/components/RecommendationDetailModal.jsx)

#### 修改 3.3.1：弹窗背景（L46-47）

```diff
- background: '#fefdfb',
- boxShadow: '0 12px 48px rgba(28,25,23,0.16), 0 0 0 1px rgba(28,25,23,0.04)',
+ background: 'var(--popup-bg)',
+ boxShadow: 'var(--popup-shadow)',
```

#### 修改 3.3.2：分隔点（L77）

```diff
- background: 'rgba(28,25,23,0.18)',
+ background: 'var(--meta-sep-color)',
```

#### 修改 3.3.3：关键词标签文字（L107）

```diff
- color: '#8b6914', background: 'rgba(196,154,60,0.07)',
+ color: 'var(--gold-dark)', background: 'rgba(196,154,60,0.12)',
```

> 关键词标签背景略微加深以在暗色模式下保持可见。

#### 修改 3.3.4：分隔线（L121）

```diff
- height: '1px', background: 'rgba(28,25,23,0.055)',
+ height: '1px', background: 'var(--border-subtle)',
```

---

## 4. 阅读区修复方案（P1）

### 4.1 ReaderPage — 阅读主页面

**文件**：[src/components/ReaderPage.jsx](../../src/components/ReaderPage.jsx)

#### 修改 4.1.1：错误提示横幅（L849）

```diff
- <div className="mb-6 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">
+ <div className="mb-6 rounded-2xl px-4 py-3 text-sm" style={{ border: '1px solid rgba(245,158,11,0.25)', background: 'rgba(245,158,11,0.08)', color: 'var(--ink)' }}>
```

> 将 Tailwind 硬编码类替换为内联样式，使用金色调来适配两种主题。

#### 修改 4.1.2：文章结束分隔线（L951, L953）

```diff
- <div style={{ flex: 1, height: '1px', background: 'rgba(28,25,23,0.1)' }} />
+ <div style={{ flex: 1, height: '1px', background: 'var(--border-subtle)' }} />
```

#### 修改 4.1.3：「已读完」标记 — 保持不动

绿色标记（`#16a34a` 系列）在两种模式下都可接受。暂不修改。

#### 修改 4.1.4：评分按钮边框（L998）

```diff
- border: `1px solid ${isActive ? 'var(--gold-dark)' : 'rgba(28,25,23,0.12)'}`,
+ border: `1px solid ${isActive ? 'var(--gold-dark)' : 'var(--surface-border)'}`,
```

#### 修改 4.1.5：「标记已读完」按钮（L1016-1030）🔴

```diff
  style={{
-   background: 'rgba(255,255,255,0.6)',
-   border: '1px solid rgba(28,25,23,0.12)',
+   background: 'var(--surface-bg)',
+   border: '1px solid var(--surface-border)',
    color: 'var(--ink-muted)',
    ...
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.background = 'rgba(34,197,94,0.08)'
    e.currentTarget.style.borderColor = 'rgba(34,197,94,0.3)'
    e.currentTarget.style.color = '#16a34a'
  }}
  onMouseLeave={(e) => {
-   e.currentTarget.style.background = 'rgba(255,255,255,0.6)'
-   e.currentTarget.style.borderColor = 'rgba(28,25,23,0.12)'
+   e.currentTarget.style.background = 'var(--surface-bg)'
+   e.currentTarget.style.borderColor = 'var(--surface-border)'
    e.currentTarget.style.color = 'var(--ink-muted)'
  }}
```

### 4.2 SectionTocPanel — 目录面板

**文件**：[src/components/SectionTocPanel.jsx](../../src/components/SectionTocPanel.jsx)

#### 修改 4.2.1：面板背景/边框/阴影（L66-68）

```diff
  style={{
    width: '280px',
-   background: '#fdfaf5',
-   borderLeft: '1px solid rgba(28,25,23,0.09)',
-   boxShadow: '-8px 0 32px rgba(28,25,23,0.1)',
+   background: 'var(--popup-bg)',
+   borderLeft: '1px solid var(--popup-border)',
+   boxShadow: 'var(--popup-shadow)',
  }}
```

#### 修改 4.2.2：面板顶部分隔线（L73）

```diff
- style={{ borderBottom: '1px solid rgba(28,25,23,0.08)' }}
+ style={{ borderBottom: '1px solid var(--popup-divider)' }}
```

#### 修改 4.2.3：遮罩层（L59）

```diff
- style={{ background: 'rgba(28,25,23,0.15)' }}
+ style={{ background: 'rgba(0,0,0,0.25)' }}
```

---

## 5. CSS 变量新增（P0 前置）

**文件**：[src/index.css](../../src/index.css)

### 5.1 在 `:root` 块末尾新增（L41 之后）

```css
  /* Card surfaces (non-popup) */
  --card-shadow: 0 2px 16px rgba(28,25,23,0.06), 0 1px 4px rgba(28,25,23,0.04);
  --card-shadow-hover: 0 2px 16px rgba(0,0,0,0.08);

  /* Button hover */
  --btn-hover-bg: #2d2926;
```

### 5.2 在 `[data-theme="night"]` 块末尾新增（L78 之后）

```css
  /* Card surfaces (non-popup) */
  --card-shadow: 0 2px 16px rgba(0,0,0,0.28), 0 1px 4px rgba(0,0,0,0.18);
  --card-shadow-hover: 0 2px 16px rgba(0,0,0,0.35);

  /* Button hover */
  --btn-hover-bg: #3d3934;
```

---

## 6. HIGHLIGHT_STYLES 修复方案（P1）

**文件**：[src/utils/textUtils.js](../../src/utils/textUtils.js)

### 6.1 问题分析

当前高亮颜色的设计假设是亮色背景（`#faf4e8`）。在夜间模式（背景 `#1a1814`）下，半透明颜色叠加在深色背景上会**显著变暗**，尤其是低不透明度的值。

| 类型 | 当前值 | 夜间模式问题 |
|---|---|---|
| word | `rgba(251,191,36,0.35)` | 金色变暗，但与深色背景对比度尚可 |
| phrase | `rgba(52,211,153,0.28)` | 绿色在深色背景上不够突出 |
| sentence | `rgba(99,102,241,0.6)` 底线 | 靛蓝底线在深色背景上辨识度降低 |
| paragraph | 同上 | 同上 |

### 6.2 方案选择

**方案 A（最小改动）**：保持日间模式不变，新增夜间高亮变量，通过 JS 动态读取 `data-theme` 选择样式。

**方案 B（CSS 变量方案，推荐）**：将高亮颜色改为 CSS 变量，在 `:root` 和 `[data-theme="night"]` 分别定义。

采用**方案 B**，与项目现有的主题策略一致。

### 6.3 具体修改

#### 步骤 1：在 `index.css` 中新增高亮变量

在 `:root` 中新增：
```css
  /* Bookmark highlight */
  --hl-word-bg: rgba(251, 191, 36, 0.35);
  --hl-phrase-bg: rgba(52, 211, 153, 0.28);
  --hl-sentence-border: rgba(99, 102, 241, 0.6);
  --hl-paragraph-border: rgba(99, 102, 241, 0.6);
```

在 `[data-theme="night"]` 中覆写（略微提高不透明度以在深色背景上保持可辨识性）：
```css
  /* Bookmark highlight */
  --hl-word-bg: rgba(251, 191, 36, 0.45);
  --hl-phrase-bg: rgba(52, 211, 153, 0.38);
  --hl-sentence-border: rgba(129, 140, 248, 0.7);
  --hl-paragraph-border: rgba(129, 140, 248, 0.7);
```

#### 步骤 2：修改 `textUtils.js` 的 HIGHLIGHT_STYLES

```diff
  export const HIGHLIGHT_STYLES = {
    word: {
-     background: 'rgba(251, 191, 36, 0.35)',
+     background: 'var(--hl-word-bg)',
      borderBottom: 'none',
      textDecoration: 'none',
    },
    phrase: {
-     background: 'rgba(52, 211, 153, 0.28)',
+     background: 'var(--hl-phrase-bg)',
      borderBottom: 'none',
      textDecoration: 'none',
    },
    sentence: {
      background: 'transparent',
-     borderBottom: '2px solid rgba(99, 102, 241, 0.6)',
+     borderBottom: '2px solid var(--hl-sentence-border)',
      textDecoration: 'none',
    },
    paragraph: {
      background: 'transparent',
-     borderBottom: '2px solid rgba(99, 102, 241, 0.6)',
+     borderBottom: '2px solid var(--hl-paragraph-border)',
      textDecoration: 'none',
    },
  }
```

---

## 7. 非核心区域修复方案（P2）

### 7.1 AuthPanel

**文件**：[src/components/AuthPanel.jsx](../../src/components/AuthPanel.jsx)

| 行号 | 当前 Tailwind 类 | 替换方案 |
|---|---|---|
| L140 | `bg-white/92` | `style={{ background: 'var(--popup-bg)' }}` |
| L140 | `border-stone-900/10` | 移除 Tailwind border，用 style 中的 `border: '1px solid var(--popup-border)'` |
| L142 | `rgba(16,185,129,0.12)` / `rgba(28,25,23,0.08)` | 绿色可保留；灰色改为 `var(--hover-bg)` |
| L148 | `bg-white/96` | `var(--popup-bg)` |
| L148 | `border-stone-900/10` | `var(--popup-border)` |

> 详细修改留到 P2 阶段执行，此处仅列明方案。

### 7.2 AdminPage

大量 `bg-white/85`、`rgba(28,25,23,0.08)`、`rgba(250,250,249,0.78)` 需要替换为对应的 CSS 变量。由于 Admin 页面是管理后台，非终端用户主流程，在 P2 阶段处理。

### 7.3 ImportItemEditor / ImportItemList / ImportPanel

这些导入流程相关组件中有多处 `#ffffff` 背景和 `rgba(28,25,23,...)` 系列硬编码。在 P2 阶段统一替换。

---

## 8. 修改汇总

### 8.1 涉及文件清单

| 文件 | 修改数 | 优先级 |
|---|---|---|
| `src/index.css` | +11 行新变量 | P0 前置 |
| `src/components/ReviewCard.jsx` | ~8 处 | P0 |
| `src/components/ReviewPanel.jsx` | ~4 处 | P0 |
| `src/components/ReviewModal.jsx` | ~3 处 | P0 |
| `src/components/ImportPage.jsx` | ~20 处 | P0 |
| `src/components/RecommendationDetailModal.jsx` | ~5 处 | P0 |
| `src/components/ReaderPage.jsx` | ~6 处 | P1 |
| `src/components/SectionTocPanel.jsx` | ~4 处 | P1 |
| `src/utils/textUtils.js` | ~8 处 | P1 |
| `src/components/AuthPanel.jsx` | ~5 处 | P2 |
| `src/components/AdminPage.jsx` | ~30 处 | P2 |
| `src/components/ImportItemEditor.jsx` | ~10 处 | P2 |
| `src/components/ImportItemList.jsx` | ~8 处 | P2 |
| `src/components/ImportPanel.jsx` | ~3 处 | P2 |
| `src/components/SubmitRecommendationModal.jsx` | ~4 处 | P2 |

### 8.2 共计

| 指标 | 数量 |
|---|---|
| 涉及文件 | 14 |
| 修改点数 | ~122 |
| 新增 CSS 变量 | 11（1 卡片阴影 + 1 hover 阴影 + 1 按钮 hover + 8 高亮） |
