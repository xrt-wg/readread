# 实施后审核反馈处理记录

> 日期：2026-07-12
> 审核来源：`12_实施后审核反馈.md`
> 处理状态：全部修正完成

---

## 一、处理概览

| 类别 | 总数 | 已修正 | 无需修改/已记录 |
|---|---|---|---|
| 🟡 重要 | 6 | 5 | 1（I6 验证由用户执行） |
| 🟢 次要 | 4 | 1 | 3（M2-M4 为已知技术债） |

---

## 二、逐项处理

### I1 · init_reading_status 可能突破上限 → ✅ 已修正

**处理**：新建 `20260712000001_fix_reading_limit.sql`——`ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY updated_at DESC)` 排名，每人最多保留 5 本 reading，其余转回 in_progress。

### I2 · 3 处 TODO → ✅ 已修正

| 位置 | TODO | 处理 |
|---|---|---|
| ImportPage:193 | "书架上的重新阅读按钮" | 已实现，移除 TODO |
| ImportPage:239 | "阅读区删除走 deleteReading" | 已实现，移除 TODO |
| ReaderPage:128 | "用 article.origin 替代 import_items 查询" | 已实现 + 死代码已清理，移除 TODO |

### I3 · ImportPage 仍在导入废弃函数 → ✅ 已修正

- 移除 `countImportReferences`、`copyToReadingZone` import
- `handleMoveToReading` 改用 `startReading` 直接调用
- 保留 `createImportItem`、`fetchImportItems`、`updateImportItem`（通过 re-export 正常工作，下阶段迁移）

### I4 · ReaderPage 引用已 DROP 的 import_items 表 → ✅ 已修正

删除 L128-166 的完整 fallback 逻辑（含 import_items 查询和 sourceImportId 回退），仅保留 article.origin 路径。同时移除不再使用的 `getSupabaseClient` import。

### I5 · importCounts 死代码 → ✅ 已修正

- 移除 `importCounts` 状态声明
- 移除 `loadImportItems` 中的 `countImportReferences` 调用和 counts 计算逻辑
- 移除 `handleMoveToReading` 中的 `setImportCounts` 更新

### I6 · 端到端验证清单 → 用户执行

39 项验证待用户完成。

### M1 · ImportPanel 未使用的 createImportItem import → ✅ 已修正

### M2-M4 · 命名过时 → 已知技术债

已在执行登记 §6 中记录，不纳入本轮修复。

---

## 三、改动文件

| 文件 | 改动 |
|---|---|
| `supabase/migrations/20260712000001_fix_reading_limit.sql` | 新建 |
| `src/components/ImportPage.jsx` | 移除 2 TODO + 移除 importCounts + 移除 countImportReferences/copyToReadingZone + handleMoveToReading 改用 startReading |
| `src/components/ReaderPage.jsx` | 移除 TODO + 删除 import_items fallback 死代码 + 移除 getSupabaseClient |
| `src/components/ImportPanel.jsx` | 移除未使用的 createImportItem import |
