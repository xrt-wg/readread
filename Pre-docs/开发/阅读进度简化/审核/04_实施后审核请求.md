# 实施后审核请求 — 阅读进度计算简化

- **日期**：2026-06-20
- **分支**：feat/translation-test
- **状态**：待审核
- **审核对象**：阅读进度从「段落词数重算」迁移到「scroll% 快照」——6 个文件，删除 `calcProgress` 计算链
- **依据文档**：
  - [01_决策记录.md](../01_决策记录.md)
  - [02_实施方案.md](../02_实施方案.md)
  - [审核/01_实施前审核反馈.md](01_实施前审核反馈.md)
  - [审核/03_实施前审核反馈复核记录.md](03_实施前审核反馈复核记录.md)

---

## 一、审核说明

### 1.1 审核目标

验证实施是否正确、完整，确保：

- 6 个文件改动严格匹配方案设计
- `progressPercent` 读写链路完整：ReaderPage 写入 → library.js 传递 → 数据库存储 → ImportPage 读取
- `calcProgress` 及其依赖函数完全移除
- 现有阅读器功能（段落标记、书签跳转、本地存储）无退化
- 存量数据（`progress_percent IS NULL`）向后兼容

### 1.2 审核方式

| 标记 | 含义 |
|------|------|
| ✅ 通过 | 实施完全符合方案，无遗留问题 |
| ⚠️ 有条件通过 | 实施基本符合方案，存在已知的、可接受的偏差 |
| ❌ 不通过 | 实施与方案存在实质性偏差，需修复后重新审核 |
| — 不适用 | 该项在当前阶段不适用 |

---

## 二、代码改动审核

### 2.1 改动清单对照

| 编号 | 方案要求 | 文件 | 审核项 |
|------|---------|------|--------|
| C-001 | 新建 migration — `progress_percent INTEGER` | `supabase/migrations/20260620000000_...sql` | 幂等（`if not exists`）、NULL 兼容 |
| C-002 | `READING_MARK_COLUMNS` 追加 `progress_percent` | `src/services/library.js` | 常量包含新列 |
| C-003 | `saveReadingMark` 签名增加 `progressPercent` 参数 | `src/services/library.js` | 末尾参数、默认 `null`、云端先读后写合并 |
| C-004 | `clearReadingMark` 保留 `progressPercent` | `src/services/library.js` | `existing?.progress_percent ?? null` |
| C-005 | `setReadingMarkCompleted` 设置 `progressPercent = 100` | `src/services/library.js` | 完成 = 100% |
| C-006 | `saveCloudReadingMarkRecord` upsert 体增加字段 | `src/services/library.js` | `record.progressPercent ?? null` |
| C-007 | `mapReadingMarkRow` 映射增加 `progressPercent` | `src/services/library.js` | `row.progress_percent` |
| C-008 | `readingMarkStore.save` 签名增加 `progressPercent` | `src/store/storage.js` | 末尾参数、存储对象增加字段、`setCompleted` 同步 |
| C-009 | 新增 `scrollPercentRef`，`handleScroll` 中赋值 | `src/components/ReaderPage.jsx` | `useRef(0)`、`scrollPercentRef.current = pct` |
| C-010 | `handleSetReadingMark` 读取 `scrollPercentRef` | `src/components/ReaderPage.jsx` | `scrollPercent = scrollPercentRef.current` |
| C-011 | 单章直接传 scroll%，多章调用加权函数 | `src/components/ReaderPage.jsx` | `hasMultipleSections ? calcMulti... : scrollPercent` |
| C-012 | `calcMultiSectionProgressAtMark` 函数定义 | `src/components/ReaderPage.jsx` | 4 参数、wordCount 加权、`break` |
| C-013 | `saveReadingMark` 调用增加第 5 参数 | `src/components/ReaderPage.jsx` | `progressPercent` 传入 |
| C-014 | 删除 `import { calcProgress }` | `src/components/ImportPage.jsx` | 导入行移除 |
| C-015 | `calcProgress(art, mark)` → `mark?.progressPercent` | `src/components/ImportPage.jsx` | 排序 + 展示两处 |
| C-016 | 删除 `calcProgress`/`calcSingle`/`calcMulti` + 注释 | `src/services/progress.js` | 行 83–134，合计 ~47 行移除 |

### 2.2 附加改动

无。严格按方案实施。

---

## 三、写入链路审核（ReaderPage → 数据库）

| 编号 | 审核项 | 预期 | 结果 |
|------|--------|------|------|
| W-001 | `scrollPercentRef.current = pct` 在 `handleScroll` 中每帧更新 | 标记时取到的是最近的滚动值 | |
| W-002 | `scrollPercent = scrollPercentRef.current` 在标记事件中读取 | 获取整数 0–100 | |
| W-003 | 单章节：`progressPercent = scrollPercent` | 与顶部栏当时显示值相同 | |
| W-004 | 多章节：`progressPercent = calcMultiSectionProgressAtMark(...)` | 全书词数加权进度 | |
| W-005 | `saveReadingMark(..., progressPercent)` 云端路径先读取已有 record → 合并 `completedSections` → upsert | `progressPercent` 被持久化到 `reading_marks.progress_percent` | |
| W-006 | `clearReadingMark` 保留 `progressPercent` | 数据库中值不丢失（功能上无影响——行被过滤后不显示；下次标记时覆盖） | |
| W-007 | `setReadingMarkCompleted` 设置 `progressPercent = 100` | 读完 = 100% | |
| W-008 | 本地路径 `readingMarkStore.save` 存储 `progressPercent` | localStorage 中记录包含 `progressPercent` | |

---

## 四、读取链路审核（数据库 → ImportPage）

| 编号 | 审核项 | 预期 | 结果 |
|------|--------|------|------|
| R-001 | `READING_MARK_COLUMNS` 包含 `progress_percent` | Supabase select 返回新列 | |
| R-002 | `mapReadingMarkRow` 映射 `progressPercent` | mark 对象含 `progressPercent` 字段 | |
| R-003 | `listReadingMarks` → `mapReadingMarks` → 每篇文章的 mark 含 `progressPercent` | `readingMarks[articleId].progressPercent` 可访问 | |
| R-004 | ImportPage：`mark?.progressPercent ?? null` | 列表展示存储的值；存量数据（NULL）→ 不显示进度 | |
| R-005 | 排序：`progressPercent` 替代 `calcProgress` 返回值 | 排序行为不变（group 分组 + 数值排序） | |

---

## 五、删除链路审核（calcProgress 移除）

| 编号 | 审核项 | 预期 | 结果 |
|------|--------|------|------|
| D-001 | `progress.js` 行 83–134 已删除 | `calcProgress`/`calcSingleProgress`/`calcMultiSectionProgress` 不可访问 | |
| D-002 | `ImportPage.jsx` 不再 `import { calcProgress }` | 构建无「未使用导入」警告 | |
| D-003 | `ImportPage.jsx` 不再调用 `calcProgress()` | 无引用 | |
| D-004 | `progress.js` 保留 `getParagraphs` (export) | ReaderPage 非 markdown 渲染正常 | |
| D-005 | `progress.js` 保留 `getMarkdownParagraphs`/`isListBlock` | markdown 段落解析正常 | |

---

## 六、回归审核

### 6.1 阅读器核心功能

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-001 | 打开文章 → 阅读器正常渲染（markdown 路径） | |
| G-002 | 打开文章 → 阅读器正常渲染（纯文本路径，`getParagraphs`） | |
| G-003 | 顶部栏滚动进度实时更新 | |
| G-004 | 点击段落标记 → 书签图标切换 | |
| G-005 | 再次点击同一段落 → 标记清除 | |
| G-006 | `readingMark` 状态正确更新（含 `progressPercent`） | |
| G-007 | 已读完标记 → `completed: true, progressPercent: 100` | |
| G-008 | 书签跳转（`handleJumpToReadingMark`）正常 | |

### 6.2 列表功能

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-009 | 阅读标签列表正常加载 | |
| G-010 | 文章排序正常（有进度的在前，无进度在后，已完成最后） | |
| G-011 | 进度百分比显示为存储的值（非 99% 假阳性） | |
| G-012 | 无 `calcProgress` 相关崩溃 | |

### 6.3 本地/云端兼容

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-013 | 匿名模式（本地存储）→ 标记段落 → 进度存取正常 | |
| G-014 | 存量文章（`progress_percent IS NULL`）→ 列表不显示进度（`null`），不崩溃 | |
| G-015 | 旧 reading mark（无 `progressPercent` 字段）→ `?? null` 兜底 | |

### 6.4 其他功能不受影响

| 编号 | 审核项 | 结果 |
|------|--------|------|
| G-016 | 收藏（bookmark）保存/删除 | |
| G-017 | 划词翻译 | |
| G-018 | 书架移入阅读区 | |
| G-019 | 文章删除 | |

---

## 七、验证方案

### 7.1 部署前提

```
□ 远端 Supabase 已执行 migration：supabase db push
□ 本地 npm run build 通过，无报错
```

### 7.2 手动验证 — 单章节文章

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-001 | 打开一篇 markdown 文章 | 阅读器正常渲染，顶部栏 = 0% | |
| V-002 | 滚动到页面约 65% 位置 | 顶部栏显示 ~65% | |
| V-003 | 点击段落标记按钮 | 书签变金色 | |
| V-004 | 返回阅读标签 → 查看该文章 | 进度显示 ~65%（而非 99%） | |
| V-005 | 重新打开文章 → 滚到 30% → 再次标记 | 列表进度更新为 ~30% | |
| V-006 | 点击已标记段落取消 | 列表进度消失（无标记 = null） | |
| V-007 | 标记后点击「标记已读完」 | 列表显示「读完」，不显示百分比 | |

### 7.3 手动验证 — 多章节书

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-008 | 打开多章节 EPUB 书 | 目录按钮可见 | |
| V-009 | 在 Section A 滚动到底部 → 标记 | 进度反映 Section A 占全书的词数比例 | |
| V-010 | 切换到 Section B → 滚动到 50% → 标记 | 进度 = (Section A 全词数 + Section B × 0.5) / 总词数 | |
| V-011 | 标记 Section C 为已读完（如果有此功能） | `completedSections` 包含 Section C，进度计入其全部词数 | |

### 7.4 手动验证 — 存量兼容

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-012 | 找一个在修复**前**已有阅读标记的文章 | `progress_percent IS NULL` | |
| V-013 | 查看该文章在列表中的显示 | 无进度百分比显示，不崩溃 | |
| V-014 | 重新打开该文章 → 标记新段落 | 新 `progressPercent` 写入，列表正常显示 | |

### 7.5 手动验证 — 回归

| 步骤 | 操作 | 预期 | 结果 |
|------|------|------|------|
| V-015 | 纯文本文章（无 markdown）→ 标记段落 | `getParagraphs` 回退到 text split 路径，正常 | |
| V-016 | 匿名模式 → 标记段落 | localStorage 路径，`progressPercent` 正常存取 | |
| V-017 | 划词翻译 | 正常 | |
| V-018 | 收藏 → 保存 → 删除 | 正常 | |
| V-019 | 浏览器控制台 | 无红色报错，无 `calcProgress` 相关日志 | |

---

## 八、审核结论

### 8.1 整体评估

| 维度 | 状态 | 备注 |
|------|------|------|
| 改动匹配方案 | | C-001 ~ C-016 十六处改动，严格按方案实施 |
| 写入链路完整 | | ReaderPage → library.js → DB，progressPercent 全链路贯通 |
| 读取链路完整 | | DB → library.js → ImportPage，直接取值，零计算 |
| 删除完整性 | | calcProgress 三个函数 + 导入 + 调用全部移除 |
| 回归安全 | | getParagraphs 保留、本地路径兼容、存量 NULL 兼容 |
| 实施前审核反馈处理 | | C1/C2/C3 全部修复，Q1/Q2 全部补充 |

### 8.2 阻塞项

| 编号 | 问题描述 | 对应审核项 | 责任人 | 期限 |
|------|---------|-----------|--------|------|
| | 无 | | | |

### 8.3 已知偏差

| 编号 | 偏差描述 | 原因 | 后续计划 |
|------|---------|------|---------|
| D-001 | 进度值在字体大小变化后不更新（标记时刻的快照） | D1 决策已明确接受 10% 精度容忍 | 无需处理 |
| D-002 | 存量数据 `progress_percent IS NULL` → 列表不显示进度 | 旧数据没有快照值——用户需重新标记以生成新进度 | 可接受——重新标记是低频操作 |

### 8.4 审核签署

| 角色 | 结论 | 日期 | 签署 |
|------|------|------|------|
| 审核人 | | 2026-06-20 | |
