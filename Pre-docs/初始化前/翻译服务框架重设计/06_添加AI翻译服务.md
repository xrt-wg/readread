先看 [netlify/functions/translate.js](cci:7://file:///e:/Coding/ReadRead/netlify/functions/translate.js:0:0-0:0) 的路由结构，找准插入点：



现在给你完整的逐步指引，以后新增任何 AI 服务都按这个流程走：

---

## 添加新 AI 服务的通用步骤

**共 4 处改动，涉及 3 个文件。**

---

### 第一步：[config/presetModels.json](cci:7://file:///e:/Coding/ReadRead/config/presetModels.json:0:0-0:0) — 注册模型

这是前端和 Netlify Function 共用的「服务目录」，每新增一个服务加一个条目：

```json
{
  "gemini-preset": { ... },
  "deepseek-preset": { ... },

  "kimi-preset": {
    "label": "Kimi (Moonshot)",
    "kind": "openai-compat",
    "defaultModel": "moonshot-v1-8k"
  },

  "zhipu-preset": {
    "label": "智谱 GLM",
    "kind": "openai-compat",
    "defaultModel": "glm-4-flash"
  }
}
```

> `kind` 纯属备注，不影响运行逻辑，方便未来扩展时识别类型。

---

### 第二步：[netlify/functions/translate.js](cci:7://file:///e:/Coding/ReadRead/netlify/functions/translate.js:0:0-0:0) — 实现调用函数

在文件顶部的常量区添加 Base URL：

```js
const KIMI_BASE   = 'https://api.moonshot.cn/v1'
const ZHIPU_BASE  = 'https://open.bigmodel.cn/api/paas/v4'
```

然后在 [callDeepSeek](cci:1://file:///e:/Coding/ReadRead/netlify/functions/translate.js:45:0-68:1) 函数之后，添加两个调用函数（两者均为 OpenAI 兼容格式，直接复制 DeepSeek 模板改 3 处）：

```js
async function callKimi(prompt, maxTokens, model) {
  const apiKey = process.env.PRESET_KIMI_API_KEY        // ← 改1：env 变量名
  if (!apiKey) throw new Error('Kimi preset key not configured on server')

  const res = await fetch(`${KIMI_BASE}/chat/completions`, {   // ← 改2：Base URL
    method: 'POST',
    headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${apiKey}` },
    body: JSON.stringify({
      model,
      messages: [{ role: 'user', content: prompt }],
      max_tokens: maxTokens,
      temperature: 0.2,
    }),
  })
  if (!res.ok) {
    const err = await res.json().catch(() => ({}))
    throw new Error(err?.error?.message ?? `Kimi error ${res.status}`)   // ← 改3：错误前缀
  }
  const data = await res.json()
  return data.choices?.[0]?.message?.content?.trim() ?? ''
}

async function callZhipu(prompt, maxTokens, model) {
  const apiKey = process.env.PRESET_ZHIPU_API_KEY
  if (!apiKey) throw new Error('Zhipu preset key not configured on server')

  const res = await fetch(`${ZHIPU_BASE}/chat/completions`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${apiKey}` },
    body: JSON.stringify({
      model,
      messages: [{ role: 'user', content: prompt }],
      max_tokens: maxTokens,
      temperature: 0.2,
    }),
  })
  if (!res.ok) {
    const err = await res.json().catch(() => ({}))
    throw new Error(err?.error?.message ?? `Zhipu error ${res.status}`)
  }
  const data = await res.json()
  return data.choices?.[0]?.message?.content?.trim() ?? ''
}
```

---

### 第三步：[netlify/functions/translate.js](cci:7://file:///e:/Coding/ReadRead/netlify/functions/translate.js:0:0-0:0) — 注册路由

找到以下路由块（约第 249 行），**在 `else` 分支前**插入新的 `else if`：

```js
if (provider === 'gemini-preset') {
  resolvedModel = resolvePresetModel(provider)
  result = await callGemini(prompt, maxTokens, resolvedModel)
} else if (provider === 'deepseek-preset') {
  resolvedModel = resolvePresetModel(provider)
  result = await callDeepSeek(prompt, maxTokens, resolvedModel)
// ↓ 新增这两段
} else if (provider === 'kimi-preset') {
  resolvedModel = resolvePresetModel(provider)
  result = await callKimi(prompt, maxTokens, resolvedModel)
} else if (provider === 'zhipu-preset') {
  resolvedModel = resolvePresetModel(provider)
  result = await callZhipu(prompt, maxTokens, resolvedModel)
// ↑ 新增结束
} else {
  return { statusCode: 400, ... }
}
```

---

### 第四步：配置 API Key

**本地**（[.env](cci:7://file:///e:/Coding/ReadRead/.env:0:0-0:0) 文件）：

```
PRESET_KIMI_API_KEY=sk-xxxxxxxx
PRESET_ZHIPU_API_KEY=xxxxxxxx.xxxxxxxx
```

**生产**（Netlify Dashboard → Site → Environment variables）：添加相同的两个变量。

---

### 第五步：切换使用

改 [config/translation.js](cci:7://file:///e:/Coding/ReadRead/config/translation.js:0:0-0:0)：

```js
export const aiConfig = {
  activePreset: 'kimi-preset',      // 使用 Kimi 作主力
  fallbackPreset: 'zhipu-preset',   // 智谱作备用
  fallbackToDirect: true,
}
```

---

## 规律总结

**新增 OpenAI 兼容服务**（Kimi、智谱、通义千问等）：
- 改 3 处：env 变量名、Base URL、错误前缀
- 模板直接复制 [callDeepSeek](cci:1://file:///e:/Coding/ReadRead/netlify/functions/translate.js:45:0-68:1)

**新增非兼容服务**（如 Claude）：
- 需要根据该服务的 API 文档重写调用函数，其余步骤相同

---

是否要我直接帮你把 Kimi 和智谱的改动写入代码？