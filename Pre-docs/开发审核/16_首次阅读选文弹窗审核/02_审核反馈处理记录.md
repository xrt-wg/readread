# 首次阅读选文弹窗：审核反馈处理记录

- 日期：2026-09-17
- 依据：01_实施前审核反馈.md，以及用户对独立复核结论的“确认”。
- 当前阶段：方案修正与代码实施已完成，本地隔离验证通过；生产迁移及正式文章配置未执行。

| 项目 | 处理结论 | 确认后的更新 |
| --- | --- | --- |
| P1 恢复路径 | 接受问题，修正建议 | null 为不可用；网络可重试；权限走身份恢复；正文异常独立处理。禁止 handleOpen 空详情回退旧对象。 |
| P2 迁移检测 | 接受并补强 | 检测已登录刷新及本地有效残留；未知不视为空；修复部分写入失败仍清理数据；按账号门控、失败保留与重试。 |
| P3 副本字段 | 接受补齐，纠正事实 | 现有推荐路径没有透传 kind。补 cover_url/kind 并同步公共创建逻辑。 |
| P4 字数口径 | 接受 | 英文统计不扩展分词，内容配置验收核对字数。 |
| P5 并发断言 | 有条件接受 | 隔离成功用例恰一条，回滚零条；计数核对真实不同用户数。无需把故意移除锁作为必需测试。 |
| §5.1/5.2 并发结论 | 不接受原结论 | 资格复查不锁定其他写入；采用跨入口用户级数据库锁与阅读区容量校验。 |

## 额外核实

- runSilentImport 的文章错误未抛出，收藏/进度错误被吞掉，末尾仍清本地数据，不能表述为失败总会保留。
- createReading 当前按 format 推断 kind，与审核“完整透传”表述不同。
- SQL 行锁只能约束参与者；空资格不天然豁免跨入口并发。

## 修改范围与顺序

1. 更新开发目录的讨论记录与实施方案（本记录创建时已完成）。
2. 新增默认关闭的配置、账号状态、受控 RPC 和并发保护迁移。
3. 实施前端弹窗、迁移恢复、推荐字段修复及空状态衔接。
4. 运行构建、隔离测试和可用的界面验证；记录实际结果与限制。

两篇正式文章尚未指定。不擅自填入生产 ID，不启用入口，不宣称已完成线上联调。

## 实施结果回填

- P1：详情查询支持超时，null 不再回退旧对象；账号错误刷新身份、正文异常退出、网络失败重试。
- P2：新增 firstReadingMigration 服务，认证恢复后检查本地数据，部分失败保留，插入式重试保留云端进度；本地变更和换号不误清理。
- P3：推荐 meta.kind 传递到 createReading，优先使用合法类型；新 RPC 保留快照封面与类型。
- P4：SQL 归一化统计沿用空白切分；真实内容核查待用户指定两篇后完成。
- P5 / 并发：33 项隔离 SQL 断言、8 个迁移故障场景和 5 个真实 PostgreSQL 多连接场景通过。没有用顺序执行冒充真实并发。
- UI：真实浏览器验证桌面、390px 夜间布局、A/B 选择、失败重试、Tab 与 Esc；UI 测试为拦截全部业务请求的隔离夹具。
- 构建：npm run build 通过。

详见 [实施验证记录](../../开发/13_首次阅读选文弹窗_2026-09-17/03_实施验证记录.md)。
