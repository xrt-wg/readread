# ReadRead 性能优化方案

> 配套诊断报告: `01_性能分析报告.md` | 日期: 2026-07-12
> 审核状态: ✅ 已通过实施前审核，方案已修正（见 `03_实施前审核反馈处理记录.md`）

---

## 一、总览

本方案将 44 项优化按**投入产出比 (ROI)** 编排为三个梯队：

| 梯队 | 时间 | 项数 | 目标 |
|------|------|------|------|
| 🥇 **第一梯队** | 当天，每项 <30min | 9 | 消除用户可感知的性能退化 |
| 🥈 **第二梯队** | 本周内 | 15 | 系统性修复高频路径 |
| 🥉 **第三梯队** | 本月内 | 9 | 架构级优化，长期健康 |

---

## 二、🥇 第一梯队：立即执行（当天）

### 1.1 App.jsx — 路由级代码分割 (React.lazy)

**影响**: 首屏 Bundle 减小 ~70%，首次加载时间减半
**投入**: 5 分钟

**文件**: `src/App.jsx`

```jsx
import { lazy, Suspense } from 'react'

const AdminPage = lazy(() => import('./components/AdminPage'))
const ReaderPage = lazy(() => import('./components/ReaderPage'))
const ImportPage = lazy(() => import('./components/ImportPage'))

// Loading 骨架屏
function PageLoader() {
  return (
    <div className="min-h-screen flex items-center justify-center"
         style={{ backgroundColor: 'var(--parchment)' }}>
      <div className="w-6 h-6 border-2 border-[var(--gold)] border-t-transparent 
                   rounded-full animate-spin" />
    </div>
  )
}

// 在 return 中使用（保留 AuthPanel 条件渲染）:
<Suspense fallback={<PageLoader />}>
  {view !== 'admin' ? <AuthPanel onOpenAdmin={() => setView('admin')} 
                                  triggerOpen={authPanelTrigger} /> : null}
  {view === 'admin' ? <AdminPage onExit={() => setView('reader')} />
    : article ? <ReaderPage article={article} onBack={handleBack} />
    : <ImportPage onImport={handleImport} onOpen={handleOpen} 
                  onTriggerAuth={() => setAuthPanelTrigger(v => v + 1)} />}
</Suspense>
```

> **审核修正**: 补充了 `AuthPanel` 的条件渲染（`view !== 'admin'`），确保与非 lazy 版本行为一致。

---

### 1.2 ReaderPage — paragraphs 包裹 useMemo

**影响**: 消除每次渲染的全文解析（对长文/书籍尤其显著）
**投入**: 2 分钟

**文件**: `src/components/ReaderPage.jsx:73-76`

```jsx
// 前: 每次渲染都解析
const paragraphs = !!article.markdown
  ? getParagraphs(article)
  : parseText(text)

// 后:
const paragraphs = useMemo(() =>
  article.markdown ? getParagraphs(article) : parseText(text)
, [article.markdown, text])
```

---

### 1.3 localStorage — 添加内存缓存层

**影响**: 消除每次保存的 4 次同步 localStorage 操作
**投入**: 15 分钟

**文件**: `src/store/storage.js`

```js
// —— 内存缓存层 ——
let _articlesCache = null
let _bookmarksCache = null
let _marksCache = null

function getArticlesCache() {
  if (_articlesCache === null) _articlesCache = readJSON(KEYS.ARTICLES, [])
  return _articlesCache
}
function getBookmarksCache() {
  if (_bookmarksCache === null) _bookmarksCache = readJSON(KEYS.BOOKMARKS, [])
  return _bookmarksCache
}
function getMarksCache() {
  if (_marksCache === null) _marksCache = readJSON(KEYS.READING_MARKS, {})
  return _marksCache
}

export const articleStore = {
  getAll() {
    return getArticlesCache()
  },
  getById(id) {
    return getArticlesCache().find(a => a.id === id) ?? null
  },
  save(article) {
    const articles = getArticlesCache()
    const idx = articles.findIndex(a => a.id === article.id)
    if (idx >= 0) articles[idx] = article
    else articles.unshift(article)
    writeJSON(KEYS.ARTICLES, articles)
    return article
  },
  delete(id) {
    const articles = getArticlesCache().filter(a => a.id !== id)
    _articlesCache = articles
    writeJSON(KEYS.ARTICLES, articles)
    bookmarkStore.deleteByArticle(id)
    readingMarkStore.delete(id)
  },
}

// bookmarkStore、readingMarkStore 同样改造

// 导入时清除缓存
export function importData(data) {
  // ... 现有逻辑 ...
  _articlesCache = null
  _bookmarksCache = null
  _marksCache = null
}
```

同时移除 `touchLocalMigrationData()` 调用（迁移已关闭，每次写入无意义）：

```js
// articleStore.save()、bookmarkStore.save()、readingMarkStore.save() 中删除:
// touchLocalMigrationData()  ← 移除这行
```

> **审核确认**: `touchLocalMigrationData` 仅在 storage.js 内部 5 个 store 方法中调用，无外部依赖方，可安全移除。

---

### 1.4 readings.js — 拆分为列表列和详情列

**影响**: 书架列表传输量从 MB 级降至 KB 级
**投入**: 10 分钟

**文件**: `src/services/readings.js`

```js
// 替换现有的 READING_COLUMNS:
const READING_LIST_COLUMNS = 'id, user_id, title, author, format, cover_url, lang, '
  + 'source_url, total_word_count, section_count, kind, reading_status, '
  + 'reading_started_at, reading_finished_at, origin, share_status, '
  + 'share_source_id, created_at, updated_at, deleted_at'

const READING_DETAIL_COLUMNS = READING_LIST_COLUMNS + ', sections'

// 列表查询改用 LIST:
// listShelfReadings: .select(READING_LIST_COLUMNS)
// listReadingZone:   .select(READING_LIST_COLUMNS)
// listArticles:      .select(READING_LIST_COLUMNS)
// fetchImportItems:  .select(READING_LIST_COLUMNS)

// 只有 getReading 使用:
// .select(READING_DETAIL_COLUMNS)
```

> **审核补充**: `listArticles`（deprecated）和 `fetchImportItems`（deprecated）也需要从 `READING_COLUMNS` 切换到 `READING_LIST_COLUMNS`，确保所有列表路径受益。

---

### 1.5 readings.js — 阅读进度保存消除 fetch-before-upsert（使用 RPC）

**影响**: 每次保存进度从 2 次往返 → 1 次
**投入**: 20 分钟（含 SQL 编写和测试）

**文件**: `src/services/readings.js:828-889` + 新建 Supabase migration

#### Step 1: 创建 RPC 函数

**文件**: `supabase/migrations/20260712000003_reading_mark_partial_upsert.sql`

```sql
-- saveReadingMark: 只更新 paragraph_index / progress_percent / section_id
-- 保留 completed / completedSections 不变
CREATE OR REPLACE FUNCTION upsert_reading_mark_position(
  p_user_id uuid,
  p_reading_id text,
  p_paragraph_index int,
  p_progress_percent int DEFAULT NULL,
  p_section_id text DEFAULT NULL
) RETURNS void
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
  INSERT INTO public.reading_marks (user_id, reading_id, paragraph_index, progress_percent, section_id, updated_at)
  VALUES (p_user_id, p_reading_id, p_paragraph_index, p_progress_percent, p_section_id, NOW())
  ON CONFLICT (user_id, reading_id)
  DO UPDATE SET
    paragraph_index = EXCLUDED.paragraph_index,
    progress_percent = COALESCE(EXCLUDED.progress_percent, reading_marks.progress_percent),
    section_id = COALESCE(EXCLUDED.section_id, reading_marks.section_id),
    updated_at = NOW();
END;
$$;

-- clearReadingMark: 清除 paragraph_index，保留 completed/completedSections
CREATE OR REPLACE FUNCTION clear_reading_mark_position(
  p_user_id uuid,
  p_reading_id text
) RETURNS void
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
  UPDATE public.reading_marks
  SET paragraph_index = NULL, updated_at = NOW()
  WHERE user_id = p_user_id AND reading_id = p_reading_id;
END;
$$;

-- setReadingMarkCompleted: 标记完成，设置 progress_percent = 100
CREATE OR REPLACE FUNCTION set_reading_mark_completed(
  p_user_id uuid,
  p_reading_id text
) RETURNS void
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
  INSERT INTO public.reading_marks (user_id, reading_id, completed, progress_percent, updated_at)
  VALUES (p_user_id, p_reading_id, true, 100, NOW())
  ON CONFLICT (user_id, reading_id)
  DO UPDATE SET
    completed = true,
    progress_percent = 100,
    updated_at = NOW();
END;
$$;
```

#### Step 2: 前端调用 RPC

```js
// saveReadingMark — 用 RPC 替代 fetch-then-upsert
export async function saveReadingMark(readingId, paragraphIndex, options,
                                       sectionId = null, progressPercent = null) {
  if (!useCloudSource(options)) {
    return readingMarkStore.save(readingId, paragraphIndex, sectionId, progressPercent)
  }
  const client = getSupabaseClient()
  await client.rpc('upsert_reading_mark_position', {
    p_user_id: options.userId,
    p_reading_id: readingId,
    p_paragraph_index: paragraphIndex,
    p_progress_percent: progressPercent,
    p_section_id: sectionId,
  })
  // 返回更新后的 mark（可选，用于 UI 更新）
  return getReadingMark(readingId, options)
}

// clearReadingMark — 同样改造
export async function clearReadingMark(readingId, options) {
  if (!useCloudSource(options)) {
    readingMarkStore.delete(readingId)
    return null
  }
  const client = getSupabaseClient()
  await client.rpc('clear_reading_mark_position', {
    p_user_id: options.userId,
    p_reading_id: readingId,
  })
  return null
}

// setReadingMarkCompleted — 同样改造
export async function setReadingMarkCompleted(readingId, options) {
  if (!useCloudSource(options)) {
    return readingMarkStore.setCompleted(readingId)
  }
  const client = getSupabaseClient()
  await client.rpc('set_reading_mark_completed', {
    p_user_id: options.userId,
    p_reading_id: readingId,
  })
  return getReadingMark(readingId, options)
}
```

> **⚠️ 审核修正**: 原方案直接 upsert 并硬编码 `completed: false`、`completedSections: []`，会覆盖已存在的数据。RPC 方案使用 `COALESCE(EXCLUDED.field, existing.field)` 语义，只更新指定列，保留其余字段不变。三个函数各有不同的保留字段需求，各自使用独立 RPC。

---

### 1.6 数据库 — 创建核心缺失索引

**影响**: 覆盖 80% 的高频查询优化
**投入**: 5 分钟写 SQL + 执行

> **⚠️ 审核修正**: 原方案对 #1 和 #7 使用 `DROP INDEX + CREATE INDEX`，有短暂空窗风险且 Supabase Migration 不支持 `CONCURRENTLY`。改为两次 migration：本次 Migration A 全部用 `CREATE INDEX IF NOT EXISTS` 建新索引（#1/#7 与旧索引用不同名称共存）；后续 Migration B 清理删除旧索引。

**Migration A** — `supabase/migrations/20260712000002_performance_indexes.sql`:

```sql
-- Migration A: 创建性能索引（全部 IF NOT EXISTS，不删除旧索引）
-- 旧索引 idx_readings_user_status 和 idx_readings_share_source 由后续清理 Migration 处理

-- 1. readings 阅读区查询（listReadingZone + start_reading RPC）
--    替换 idx_readings_user_status，新名称为 idx_readings_active_zone
CREATE INDEX IF NOT EXISTS idx_readings_active_zone
  ON public.readings (user_id, reading_status, deleted_at)
  INCLUDE (reading_started_at, created_at)
  WHERE reading_status = 'reading';

-- 2. readings updated_at 排序（fix_reading_limit migration 的 ROW_NUMBER 窗口）
CREATE INDEX IF NOT EXISTS idx_readings_user_updated
  ON public.readings (user_id, updated_at DESC);

-- 3. bookmarks 用户+文章+软删除（最频繁查询：listBookmarksByArticle）
CREATE INDEX IF NOT EXISTS idx_bookmarks_user_reading_active
  ON public.bookmarks (user_id, reading_id, deleted_at);

-- 4. bookmarks 列表排序（listAllBookmarks）
CREATE INDEX IF NOT EXISTS idx_bookmarks_user_created
  ON public.bookmarks (user_id, deleted_at, created_at DESC);

-- 5. bookmarks 服务端复习筛选（listDueBookmarks RPC）
CREATE INDEX IF NOT EXISTS idx_bookmarks_user_next_review
  ON public.bookmarks (user_id, review_count, next_review_at, deleted_at);

-- 6. recommendation_submissions 标题去重
CREATE INDEX IF NOT EXISTS idx_rec_submissions_title_active
  ON public.recommendation_submissions (title, status)
  WHERE status = 'active';

-- 7. readings share_source RPC 查询
--    替换 idx_readings_share_source，新名称为 idx_readings_share_source_origin
CREATE INDEX IF NOT EXISTS idx_readings_share_source_origin
  ON public.readings (share_source_id, origin, deleted_at)
  WHERE share_source_id IS NOT NULL;

-- 8. reading_marks 完成状态查询
CREATE INDEX IF NOT EXISTS idx_reading_marks_user_completed
  ON public.reading_marks (user_id, completed)
  WHERE completed = true;

-- 9. audit_logs 默认列表排序
CREATE INDEX IF NOT EXISTS idx_audit_logs_created_at
  ON public.audit_logs (created_at DESC);

-- 10. profiles 管理页面排序
CREATE INDEX IF NOT EXISTS idx_profiles_last_seen
  ON public.profiles (last_seen_at DESC NULLS LAST);
```

**Migration B**（后续清理，单独发）:

```sql
-- 验证新索引 idx_readings_active_zone 和 idx_readings_share_source_origin
-- 已被查询使用后再执行：
DROP INDEX IF EXISTS idx_readings_user_status;
DROP INDEX IF EXISTS idx_readings_share_source;
```

---

### 1.7 ImportPage — 删除死代码

**影响**: 代码清洁，消除每渲染无意义遍历
**投入**: 1 分钟

**文件**: `src/components/ImportPage.jsx:375-379`

```diff
- const importItemTitleById = (() => {
-     const map = {}
-     importItems.forEach((item) => { map[item.id] = item.title })
-     return map
- })()
```

---

### 1.8 App.jsx — 串行导入改批量

**影响**: 50 篇导入从 50 次往返 → 1 次
**投入**: 15 分钟

> **⚠️ 审核修正**: 原方案直接硬编码字段映射，绕过了 `saveReading`（实际实现为 `saveReading`）的数据转换逻辑。修正为：先提取共享映射函数，再批量 upsert。

#### Step 1: 提取字段映射函数

**文件**: `src/services/readings.js`（新增导出）

```js
/**
 * 将 legacy article/document 对象映射为 readings 表的数据库行。
 * 与 saveReading() 内部 upsert 使用的字段映射保持一致。
 */
export function toReadingDbRow(article, userId) {
  return {
    id: article.id,
    user_id: userId,
    title: article.title,
    author: article.author ?? null,
    format: article.format ?? article.sourceType ?? 'paste',
    cover_url: article.coverUrl ?? null,
    lang: article.lang ?? 'auto',
    source_url: article.sourceUrl ?? null,
    sections: article.sections ?? [],
    total_word_count: article.totalWordCount ?? article.wordCount ?? 0,
    section_count: article.sectionCount ?? 1,
    kind: article.kind ?? 'article',
    reading_status: article.readingStatus ?? article.reading_status ?? 'unread',
    reading_started_at: article.readingStartedAt ?? null,
    reading_finished_at: article.readingFinishedAt ?? null,
    origin: article.origin ?? 'imported',
    share_status: article.shareStatus ?? 'private',
    share_source_id: article.shareSourceId ?? null,
    created_at: article.createdAt ?? article.created_at ?? new Date().toISOString(),
    updated_at: article.updatedAt ?? article.updated_at ?? new Date().toISOString(),
  }
}
```

#### Step 2: 使用批量 upsert

**文件**: `src/App.jsx:48-53`

```jsx
import { getSupabaseClient } from './services/supabase'
import { toReadingDbRow } from './services/readings'

// 前: 串行循环
// for (const art of validArticles) {
//   await saveArticle(art, cloudOptions)
// }

// 后: 批量 upsert
const client = getSupabaseClient()
const rows = validArticles.map(art => toReadingDbRow(art, currentUserId))
const chunkSize = 50
for (let i = 0; i < rows.length; i += chunkSize) {
  const chunk = rows.slice(i, i + chunkSize)
  const { error } = await client.from('readings').upsert(chunk, {
    onConflict: 'id',
    ignoreDuplicates: false,
  })
  if (error) console.warn('批量导入部分失败:', error)
}
```

同样改造书签导入，使用 `saveBookmark` 内部的映射逻辑。

---

### 1.9 🆕 字体加载优化

**影响**: 减少 FOUT（Flash of Unstyled Text）和 CLS（Cumulative Layout Shift）
**投入**: 5 分钟

**文件**: `index.html`

```html
<head>
  <!-- ... existing ... -->
  
  <!-- 预加载关键字体 -->
  <link rel="preload" as="font" type="font/woff2" 
        href="/fonts/playfair-display-700.woff2" crossorigin />
  <link rel="preload" as="font" type="font/woff2" 
        href="/fonts/dm-sans-400.woff2" crossorigin />
  
  <style>
    /* font-display: swap 确保文字立即可见 */
    @font-face {
      font-family: 'Playfair Display';
      font-display: swap;
      /* ... */
    }
    @font-face {
      font-family: 'DM Sans';
      font-display: swap;
      /* ... */
    }
    @font-face {
      font-family: 'Lora';
      font-display: swap;
      /* ... */
    }
  </style>
</head>
```

> **说明**: 此项为审核补充。根据字体实际托管位置（Google Fonts / 自托管）调整 `href` 路径。如果使用 Google Fonts，可用 `<link rel="preconnect" href="https://fonts.googleapis.com">` + `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` 替代。

---

## 三、🥈 第二梯队：本周执行

### 3.1 AuthProvider — 拆分 Context 避免全量重渲染

**影响**: 消除最常见的全应用重渲染触发链
**投入**: 30 分钟

**文件**: `src/providers/AuthProvider.jsx`

```jsx
// 拆分为两个 Context:
const AuthStateContext = createContext(null)   // status, userId, isAdmin, ...
const AuthActionsContext = createContext(null) // refreshAuthState

// refreshAuthState 提升为 useCallback（稳定引用）:
const refreshAuthState = useCallback(() => {
  setRefreshToken(v => v + 1)
}, [])

// 状态 context 的 useMemo 细化依赖:
const stateValue = useMemo(() => ({
  status: authState.status,
  userId: authState.userId,
  isAdmin: authState.isAdmin,
  isReady: authState.status !== 'initializing',
  isAuthenticated: authState.status === 'authenticated',
  canUseCloudLibrary: authState.status === 'authenticated',
  canAccessAdmin: authState.isAdmin && authState.sessionValid,
  error: authState.error,
  profile: authState.profile,
}), [authState.status, authState.userId, authState.isAdmin, 
    authState.sessionValid, authState.error, authState.profile])

const actionsValue = useMemo(() => ({ refreshAuthState }), [refreshAuthState])

return (
  <AuthStateContext.Provider value={stateValue}>
    <AuthActionsContext.Provider value={actionsValue}>
      {children}
    </AuthActionsContext.Provider>
  </AuthStateContext.Provider>
)
```

> **执行前检查**: 审计所有 `useAuth()` 消费点（`App.jsx`、`ReaderPage.jsx`、`ImportPage.jsx`、`AdminPage.jsx`、`AuthPanel.jsx`、`ReviewPanel.jsx`、`ReviewModal.jsx`、`useBookmarkAI.js`、`useDirectTranslation.js`），确认拆分后无遗漏字段。

---

### 3.2 ReaderPage — 拆分头部为独立 React.memo 组件

**影响**: 字体大小/主题切换不再重渲染全文
**投入**: 45 分钟

**文件**: 新建 `src/components/ReaderHeader.jsx`，从 `ReaderPage.jsx:607-831` 提取

```jsx
// ReaderHeader.jsx
const ReaderHeader = React.memo(function ReaderHeader({
  headerVisible, onBack, scrollPercentTextRef, panelOpen,
  setPanelOpen, fontSize, setFontSize, fontSizeOpen, setFontSizeOpen,
  fontSizeRef, theme, toggleTheme, tocOpen, setTocOpen,
  readingMark, handleJumpToReadingMark, handleMarkCompleted,
  handleReturnToShelf,
}) {
  return (
    <header className="sticky top-0 z-40 ...">
      {/* 现有 header JSX，不含内联 style 对象 */}
    </header>
  )
})
```

同时将以下函数/常量提升为模块级以避免每渲染重建：
- `roundProgress` → 模块级函数 (ReaderPage)
- `FACE_BASE_STYLE` → 模块级常量 (ReviewCard)
- `navItems` → 模块级常量 (AdminPage)
- `TABS` 数组 → 模块级常量 (ImportPage ShelfTabs)

---

### 3.3 替换 JS onMouseEnter/onMouseLeave 为 CSS :hover

**影响**: 消除 30+ 个内联函数分配（ImportPage + ReaderPage + BookmarkPanel）
**投入**: 30 分钟

> **审核共识**: 经逐处审查，确认全部内联回调仅修改 `e.currentTarget.style` 的静态字符串值，无一处引用 useState/props/运行时条件变量。可安全**全部替换**，无需分类保留。

**文件**: `src/index.css`

```css
/* —— 全局可复用的 hover 样式 —— */

/* 可点击卡片的 hover */
.card-interactive {
  background: transparent;
  transition: background 0.15s ease;
}
.card-interactive:hover {
  background: rgba(28, 25, 23, 0.04);
}

/* 图标按钮的 hover */
.btn-icon {
  background: transparent;
  color: var(--ink-muted);
  transition: background 0.15s, color 0.15s;
  border: none;
  cursor: pointer;
}
.btn-icon:hover {
  background: var(--hover-bg);
  color: var(--ink);
}

/* 书签原文高亮 */
.bookmark-highlight {
  background: rgba(196, 154, 60, 0.12);
  border-radius: 3px;
  padding: 0 2px;
  cursor: pointer;
  transition: background 0.15s;
}
.bookmark-highlight:hover {
  background: rgba(196, 154, 60, 0.25);
}

/* ImportPage ReadingZoneCard 的 border + shadow hover */
.reading-zone-card {
  border: 1px solid var(--border-subtle);
  transition: border-color 0.15s, box-shadow 0.15s;
}
.reading-zone-card:hover {
  border-color: rgba(196, 154, 60, 0.3);
  box-shadow: 0 4px 16px rgba(28, 25, 23, 0.06);
}
```

然后用 CSS class 替换内联 `onMouseEnter`/`onMouseLeave`。实现时注意 CSS 中的颜色值与原 JS 值精确对齐。

---

### 3.4 ImportPage — 提取 RecommendationCard + ReadingZoneCard

**影响**: 推荐列表滚动/交互不再重建整个卡片列表
**投入**: 30 分钟

**文件**: `src/components/ImportPage.jsx`

```jsx
// 提取为独立文件或模块级组件:
const RecommendationCard = React.memo(function RecommendationCard({
  rec, onAdd, onDetail
}) {
  return (
    <div className="card-interactive rounded-xl p-4 ..."
         onClick={() => onAdd(rec)}>
      {/* 卡片内容，用 CSS :hover 替代 inline onMouseEnter/onMouseLeave */}
    </div>
  )
})

const ReadingZoneCard = React.memo(function ReadingZoneCard({
  art, readingMarks, onOpen, onReturnToShelf
}) {
  return (
    <div className="reading-zone-card rounded-xl p-4 ..."
         onClick={() => onOpen(art)}>
      {/* 卡片内容 */}
    </div>
  )
})
```

> **依赖**: 此项依赖 3.3（CSS hover 替换）先完成，否则提取出的卡片组件仍包含内联回调。

---

### 3.5 ImportPage — 派生数组包裹 useMemo

**影响**: 消除每渲染的 O(n) filter + O(n log n) sort
**投入**: 15 分钟

**文件**: `src/components/ImportPage.jsx`

```jsx
// 651-656: 书架筛选
const filteredItems = useMemo(() => {
  if (shelfTab === 'all') return importItems
  return importItems.filter(i => 
    (i.readingStatus || 'unread') === shelfTab
  )
}, [shelfTab, importItems])

// 691-706: 阅读统计
const readingStats = useMemo(() => {
  const marksArr = Object.values(readingMarks)
  const completedCount = marksArr.filter(m => m.completed).length
  const totalBookmarks = bookmarks.length
  const wordsRead = readingZoneItems
    .filter(a => readingMarks[a.id]?.completed)
    .reduce((sum, a) => sum + (a.totalWordCount ?? 0), 0)
  return { completedCount, totalBookmarks, wordsRead }
}, [readingMarks, bookmarks, readingZoneItems])

// 708-717: 阅读区排序
const sortedReadingZone = useMemo(() =>
  [...readingZoneItems].sort((a, b) => {
    const markA = readingMarks[a.id]
    const markB = readingMarks[b.id]
    if (markA?.completed && !markB?.completed) return 1
    if (!markA?.completed && markB?.completed) return -1
    return new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime()
  })
, [readingZoneItems, readingMarks])
```

---

### 3.6 ThemeProvider — 包裹 useMemo

**影响**: 父组件渲染不再引发所有 useTheme 消费者重渲染
**投入**: 2 分钟

**文件**: `src/hooks/useTheme.jsx`

```jsx
// 前:
<ThemeContext.Provider value={{ theme, toggleTheme }}>

// 后:
const value = useMemo(() => ({ theme, toggleTheme }), [theme, toggleTheme])
<ThemeContext.Provider value={value}>
```

---

### 3.7 组件添加 React.memo — 热路径

**影响**: 隔离父组件渲染，防止子树无意义 diff
**投入**: 10 分钟

```jsx
// 每个文件:
export default React.memo(function ComponentName({ ... }) { ... })

// 优先级最高的 6 个:
// 1. ParagraphRenderer.jsx    — 每段渲染一次
// 2. TranslationPopup.jsx     — 阅读中频繁显示
// 3. BookmarkPanel.jsx        — 大列表
// 4. ImportItemList.jsx       — 下拉菜单触发重渲染
// 5. SectionFlow.jsx          — MarkdownContent 未 memo
// 6. ShelfTabs (ImportPage)   — 3 次 filter 每渲染
```

> **依赖**: 此项需与 3.1（AuthProvider 拆分）和 3.6（ThemeProvider useMemo）配套执行，否则 Context 值/Props 不稳定，React.memo 几乎不会命中缓存。

---

### 3.8 recommendationService — 并行化资格检查

**影响**: 资格检查延迟减半
**投入**: 10 分钟

**文件**: `src/services/supabase/recommendationService.js:266-283`

```jsx
// 前: 3 次串行
const isCompleted = await checkReadingCompleted(userId, importItemId)
const weeklyCount = await countWeeklySubmissions(userId)
const duplicate = await checkDuplicateSubmission(...)

// 后: 并行
const [isCompleted, weeklyCount, duplicate] = await Promise.all([
  checkReadingCompleted(userId, importItemId),
  countWeeklySubmissions(userId),
  checkDuplicateSubmission({ sourceUrl: item.source_url, title: item.title }),
])
```

同样并行化:
- `getRecommendation()` lines 111-124: submission + ratings
- `getRecommendationStats()` lines 696-703: 两个 count 查询

---

### 3.9 recommendationService — 合并重复查询

**影响**: 消除 checkRatingEligibility 中的完全重复查询
**投入**: 5 分钟

**文件**: `src/services/supabase/recommendationService.js`

```js
// findImportItemByShareSource 改为返回 id 而不仅是 boolean:
async function findImportItemByShareSource(userId, submissionId) {
  const client = getClient()
  const { data, error } = await client
    .from('readings')
    .select('id')
    .eq('user_id', userId)
    .eq('share_source_id', submissionId)
    .in('origin', ['featured', 'featured_legacy'])
    .is('deleted_at', null)
    .maybeSingle()
  if (error) throw error
  return data?.id ?? null  // 返回 id 或 null
}

// checkRatingEligibility 中复用:
const importItemId = await findImportItemByShareSource(userId, submissionId)
if (!importItemId) return { canRate: false, reason: '...' }
// 直接使用 importItemId，不再做第二次查询
```

---

### 3.10 recommendationService — checkDuplicateSubmission 合并为单查询

**影响**: 去重检查从 2 次 → 1 次往返
**投入**: 5 分钟

**文件**: `src/services/supabase/recommendationService.js:205-224`

```js
async function checkDuplicateSubmission(item) {
  const client = getClient()
  const conditions = []
  if (item.sourceUrl) conditions.push(`source_url.eq.${item.sourceUrl}`)
  conditions.push(`title.eq.${item.title}`)
  
  const { data } = await client
    .from('recommendation_submissions')
    .select('id')
    .or(conditions.join(','))
    .eq('status', 'active')
    .maybeSingle()
  return !!data
}
```

> **注意**: `.or()` 语法中参数值需要 SQL 注入防护。如果 `item.title` 或 `item.sourceUrl` 包含逗号或特殊字符，需做转义处理。

---

### 3.11 ImportPage — 合并重复数据加载

**影响**: 消除单次操作中的重复请求（shelfReadings 2 次、readingZone 2 次）
**投入**: 15 分钟

**文件**: `src/components/ImportPage.jsx`

```jsx
// 将三个函数合并为一个:
const loadAllState = useCallback(async () => {
  if (!isAuthenticated || !userId) return
  try {
    const snapshot = await loadLibrarySnapshot({ canUseCloudLibrary, userId })
    setArticles(snapshot.articles)
    setBookmarks(snapshot.bookmarks)
    setReadingMarks(snapshot.readingMarks)
    // readingZone 从 snapshot 中提取:
    setReadingZoneItems(
      snapshot.articles.filter(a => a.readingStatus === 'reading')
    )
  } catch (e) {
    if (isLibraryAccessError(e)) refreshAuthState()
  }
}, [isAuthenticated, userId, canUseCloudLibrary, refreshAuthState])

// 替换所有 loadLibraryState + loadReadingZone + loadImportItems 的调用点
```

同时修改 `loadLibrarySnapshot` 使其在 `Promise.all` 中包含 `listReadingZone`：

```js
const [readings, bookmarks, readingMarks, activeReadings] = await Promise.all([
  listShelfReadings(options),
  listAllBookmarks(options),
  listReadingMarks(options),
  useCloudSource(options) ? listReadingZone(options) : Promise.resolve([]),
])
```

> **依赖**: 此项需与 1.4（列拆分）配套执行，否则合并后的一次请求仍会传输 sections 列。

---

### 3.12 vite.config.js — 配置代码分包

**影响**: 改善缓存命中率，减小单个 chunk 体积
**投入**: 5 分钟

**文件**: `vite.config.js`

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  build: {
    target: 'es2020',
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          supabase: ['@supabase/supabase-js'],
          markdown: ['react-markdown', 'remark-gfm'],
        },
      },
    },
  },
})
```

---

### 3.13 🆕 服务端书签复习 (从第三梯队提升)

**影响**: 消除全量书签下载，每次复习区访问传输量从全部书签 → 仅需 20 条
**投入**: 30 分钟

> **提级原因**: 与 1.4（sections 列拆分）本质相同的线性增长网络浪费。500 书签用户每次进复习区下载全部书签 = 500KB+。RPC 复杂度评估为中等偏下。

**文件**: 新建 `supabase/migrations/20260712000004_get_due_bookmarks_rpc.sql`

```sql
CREATE OR REPLACE FUNCTION get_due_bookmarks(
  p_user_id uuid,
  p_limit int DEFAULT 20
)
RETURNS TABLE(
  id text, user_id uuid, reading_id text, type text, text text,
  translation text, translation_provider text,
  context_sentence text, context_translation text,
  translation_status text, paragraph_index int, char_offset int,
  review_count int, next_review_at timestamptz, familiarity smallint,
  section_id text, section_heading text,
  created_at timestamptz, updated_at timestamptz, deleted_at timestamptz,
  article_title text
)
LANGUAGE plpgsql
STABLE
SECURITY DEFINER
AS $$
DECLARE
  v_new_count int;
  v_due_count int;
BEGIN
  -- 获取新卡片数 (review_count = 0)
  SELECT COUNT(*) INTO v_new_count
  FROM public.bookmarks
  WHERE user_id = p_user_id
    AND (review_count IS NULL OR review_count = 0)
    AND deleted_at IS NULL;

  -- 获取到期卡片数 (next_review_at <= now())
  SELECT COUNT(*) INTO v_due_count
  FROM public.bookmarks
  WHERE user_id = p_user_id
    AND review_count > 0
    AND next_review_at <= NOW()
    AND deleted_at IS NULL;

  RETURN QUERY
  -- 新卡片优先
  SELECT b.*, r.title AS article_title
  FROM public.bookmarks b
  JOIN public.readings r ON r.id = b.reading_id
  WHERE b.user_id = p_user_id
    AND (b.review_count IS NULL OR b.review_count = 0)
    AND b.deleted_at IS NULL
  ORDER BY b.created_at DESC
  LIMIT p_limit

  UNION ALL

  -- 到期卡片其次
  SELECT b.*, r.title AS article_title
  FROM public.bookmarks b
  JOIN public.readings r ON r.id = b.reading_id
  WHERE b.user_id = p_user_id
    AND b.review_count > 0
    AND b.next_review_at <= NOW()
    AND b.deleted_at IS NULL
  ORDER BY b.next_review_at ASC
  LIMIT GREATEST(0, p_limit - v_new_count)

  UNION ALL

  -- 不够 p_limit 时用未来卡片补位
  SELECT b.*, r.title AS article_title
  FROM public.bookmarks b
  JOIN public.readings r ON r.id = b.reading_id
  WHERE b.user_id = p_user_id
    AND b.review_count > 0
    AND b.next_review_at > NOW()
    AND b.deleted_at IS NULL
  ORDER BY b.next_review_at ASC
  LIMIT GREATEST(0, p_limit - v_new_count - v_due_count);
END;
$$;
```

**前端替换**:

```js
// readings.js
export async function listDueBookmarks(options, excludeIds = new Set()) {
  if (!useCloudSource(options)) {
    // 本地路径保持原逻辑
    const all = bookmarkStore.getAll()
    // ... 现有逻辑 ...
    return shuffleArray(session)
  }

  const client = getSupabaseClient()
  const { data, error } = await client.rpc('get_due_bookmarks', {
    p_user_id: options.userId,
    p_limit: 10,
  })
  if (error) throw error
  // RPC 返回原始行，使用与 listAllBookmarks 相同的映射
  return (data || []).map(mapBookmarkRow).filter(b => !excludeIds.has(b.id))
}
```

> **执行前**: 在 Supabase SQL Editor 单独测试 RPC，确认返回列与 `mapBookmarkRow` 的字段映射对齐。

---

### 3.14 🆕 Error Boundary

**影响**: 防止 ReaderPage/AdminPage 渲染异常导致全应用白屏
**投入**: 15 分钟

**文件**: 新建 `src/components/ErrorBoundary.jsx`

```jsx
import { Component } from 'react'

export class ErrorBoundary extends Component {
  constructor(props) {
    super(props)
    this.state = { hasError: false, error: null }
  }

  static getDerivedStateFromError(error) {
    return { hasError: true, error }
  }

  componentDidCatch(error, errorInfo) {
    console.error('[ErrorBoundary]', error, errorInfo)
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || (
        <div className="min-h-screen flex items-center justify-center"
             style={{ backgroundColor: 'var(--parchment)' }}>
          <div className="max-w-md px-6 text-center text-sm"
               style={{ color: 'var(--ink-muted)' }}>
            <p className="mb-3" style={{ fontSize: '18px', color: 'var(--ink)' }}>
              页面出现错误
            </p>
            <p>请尝试刷新页面。如果问题持续存在，请联系支持。</p>
            <button
              onClick={() => window.location.reload()}
              className="mt-4 px-4 py-2 rounded-lg"
              style={{
                background: 'var(--gold)',
                color: '#fff',
                border: 'none',
                cursor: 'pointer',
              }}
            >
              刷新页面
            </button>
          </div>
        </div>
      )
    }
    return this.props.children
  }
}
```

**使用** (`src/App.jsx`):

```jsx
<Suspense fallback={<PageLoader />}>
  {view === 'admin'
    ? <ErrorBoundary><AdminPage onExit={() => setView('reader')} /></ErrorBoundary>
    : article
      ? <ErrorBoundary><ReaderPage article={article} onBack={handleBack} /></ErrorBoundary>
      : <ErrorBoundary><ImportPage ... /></ErrorBoundary>
  }
</Suspense>
```

---

## 四、🥉 第三梯队：本月执行

### 4.1 组件拆分 — 全面重构

| 当前组件 | 拆分为 |
|---------|--------|
| `AdminPage.jsx` (1294行) | `AdminSidebar` + `DashboardTab` + `UsersTab` + `AuditTab` + `ArticleEditorPanel` + `Toast` |
| `ReaderPage.jsx` (1117行) | `ReaderHeader` + `ProgressBar` + `ChapterNav` + `EndOfDocument` + `HeaderRevealHotZone` |
| `ImportPage.jsx` (787行) | `RecommendationCard` + `ReadingZoneCard` + `TabBar` + `StatsBar` |

每个子组件包裹 `React.memo`。

### 4.2 列表虚拟化

对以下长列表引入虚拟滚动（推荐 `@tanstack/react-virtual` 或轻量实现）：
- `ImportItemList` — 书架列表
- `BookmarkPanel` — 书签列表
- `ReviewPanel` — 复习卡片列表

### 4.3 请求缓存层

在 `src/services/` 添加轻量请求去重层：

```js
// services/cache.js
const pending = new Map()

export async function dedupe(key, fetcher) {
  if (pending.has(key)) return pending.get(key)
  const promise = fetcher().finally(() => pending.delete(key))
  pending.set(key, promise)
  return promise
}

// 使用:
const bookmarks = await dedupe(`bookmarks:${articleId}`, () =>
  listBookmarksByArticle(articleId, options)
)
```

### 4.4 urlImport.js — 主线程分片处理

> **⚠️ 审核修正**: 原方案为 Web Worker 化。`@mozilla/readability` 需要完整的 DOM API（`document`、`DOMParser`、`Node`），Web Worker 不提供 DOM 环境。修正为 `requestIdleCallback` 分片方案。

```js
// utils/idleScheduler.js
export function scheduleIdleTask(task, { timeout = 2000 } = {}) {
  return new Promise((resolve, reject) => {
    const run = (deadline) => {
      try {
        const result = task(deadline)
        resolve(result)
      } catch (e) {
        reject(e)
      }
    }
    if ('requestIdleCallback' in window) {
      requestIdleCallback(run, { timeout })
    } else {
      setTimeout(() => run({ timeRemaining: () => 50, didTimeout: false }), 0)
    }
  })
}

// urlImport.js 使用:
import { scheduleIdleTask } from '../utils/idleScheduler'

async function parseArticle(html, baseUrl) {
  return scheduleIdleTask((deadline) => {
    const doc = new DOMParser().parseFromString(html, 'text/html')
    // 如果剩余时间不足，Readability 可能在下一帧继续
    // 当前实现：Readability 一次性执行（中等页面 50-150ms）
    const reader = new Readability(doc)
    return reader.parse()
  }, { timeout: 5000 })
}
```

> **备选方案**: 如果 Readability 执行时间显著超过一帧（16ms），可进一步探索 (a) `linkedom` 在 Worker 中模拟 DOM；或 (b) 拆分 Readability 算法步骤，在多个 `requestIdleCallback` 中逐步执行。

### 4.5 重型依赖 lazy load

```jsx
// 仅在需要时动态导入:
const ReactMarkdown = lazy(() => import('react-markdown'))
const { default: remarkGfm } = await import('remark-gfm')
// epub2md, turndown, @mozilla/readability 同理
```

### 4.6 CSS 性能优化

- `body::before` 噪声纹理: 降低 opacity 到 0.3，使用更小的 SVG 平铺
- ReaderPage header: 替换 `backdrop-filter: blur(12px)` 为不透明背景
- 长段落列表: 添加 `content-visibility: auto; contain-intrinsic-size: 0 500px`

### 4.7 移除废弃代码

- `AuthProvider.jsx:18-61` — `getLocalMigrationState()` 函数体
- `storage.js` — 所有 `touchLocalMigrationData()` 调用（已在 1.3 中处理）
- `importItems.js:25-53` — 所有返回空/no-op 的废弃函数

### 4.8 模块顶层 migrateLocalStorageArticles 延迟

```jsx
// main.jsx
// 前: 同步阻塞
migrateLocalStorageArticles()

// 后: 空闲时执行
requestIdleCallback(() => migrateLocalStorageArticles(), { timeout: 2000 })
```

### 4.9 AuthProvider useEffect 重订阅优化

**文件**: `src/providers/AuthProvider.jsx:203`

将 `refreshToken` 从 state 改为 ref，避免 `refreshAuthState()` 调用时触发 useEffect 退订/重订 Supabase auth 订阅：

```jsx
const refreshTokenRef = useRef(0)

const refreshAuthState = useCallback(() => {
  refreshTokenRef.current += 1
  // 如果需要在 refreshToken 变更时触发副作用，使用 ref 值代替 state
}, [])
```

---

## 五、实施检查清单

### 执行顺序（按依赖关系排列）

```
第1批（无依赖，立即执行）:
  1.4 (列拆分) → 1.2 (useMemo) → 1.3 (缓存) → 1.7 (死代码) → 3.6 (ThemeProvider)

第2批（依赖第1批的稳定化）:
  1.1 (lazy) → 1.9 (字体加载)

第3批（依赖 Context 稳定化）:
  3.1 (AuthProvider) → 3.7 (memo) → 3.3 (CSS hover) → 3.4 (卡片拆分)

第4批（依赖列拆分 + 数据合并）:
  3.11 (数据合并) → 3.5 (useMemo)

第5批（独立可并行）:
  3.8/3.9/3.10 (recommendationService) → 3.13 (服务端复习)

第6批（独立，最后执行）:
  3.12 (vite) → 3.14 (Error Boundary) → 1.6 (索引) → 1.5 (RPC进度) → 1.8 (批量导入)
```

### 执行前检查

- [ ] `saveReadingMark` / `clearReadingMark` / `setReadingMarkCompleted` 三个 RPC 函数已编写并通过 Supabase SQL Editor 测试
- [ ] `get_due_bookmarks` RPC 函数已测试，确认返回列与 `mapBookmarkRow` 字段映射对齐
- [ ] 数据库索引 migration 全部使用 `CREATE INDEX IF NOT EXISTS`
- [ ] `listArticles`（deprecated）、`fetchImportItems`（deprecated）也从 `READING_COLUMNS` 切换到 `READING_LIST_COLUMNS`
- [ ] 审计所有 `useAuth()` 消费点，确保 Context 拆分后不会遗漏字段
- [ ] 验证 `toReadingDbRow` 字段映射与 `saveReading` 内部 upsert 一致

### 第一梯队 ✅ (当天)

- [ ] 1.4 readings.js — READING_LIST_COLUMNS / READING_DETAIL_COLUMNS
- [ ] 1.2 ReaderPage — paragraphs useMemo
- [ ] 1.3 storage.js — 内存缓存 + 移除 touchLocalMigrationData
- [ ] 1.7 ImportPage — 删除死代码 importItemTitleById
- [ ] 3.6 ThemeProvider — useMemo
- [ ] 1.1 App.jsx — React.lazy 代码分割（含 AuthPanel 条件渲染）
- [ ] 1.9 字体加载优化 — preload + font-display: swap
- [ ] 1.6 Migration A — 10 个性能索引（全部 IF NOT EXISTS）
- [ ] 1.5 readings.js — 阅读进度 RPC 函数 + 前端调用
- [ ] 1.8 App.jsx — 批量导入（提取 toReadingDbRow + 批量 upsert）

### 第二梯队 ✅ (本周)

- [ ] 3.1 AuthProvider — 拆分 Context
- [ ] 3.7 6 个热路径组件 React.memo
- [ ] 3.3 CSS :hover 替代 JS mouseEnter/mouseLeave（全部替换）
- [ ] 3.4 ImportPage — 提取 RecommendationCard + ReadingZoneCard
- [ ] 3.11 ImportPage — 合并重复数据加载 + loadLibrarySnapshot Promise.all
- [ ] 3.5 ImportPage — useMemo 包裹派生数组
- [ ] 3.8 recommendationService — 并行化查询
- [ ] 3.9 recommendationService — 合并重复查询（findImportItemByShareSource 返回 id）
- [ ] 3.10 recommendationService — .or() 合并去重
- [ ] 3.13 服务端书签复习 — get_due_bookmarks RPC + 前端替换
- [ ] 3.2 ReaderPage — 拆分 ReaderHeader
- [ ] 3.12 vite.config.js — manualChunks
- [ ] 3.14 Error Boundary — 新建组件 + App.jsx 包裹

### 第三梯队 ✅ (本月)

- [ ] 4.1 三大组件全面拆分
- [ ] 4.2 列表虚拟化
- [ ] 4.3 请求缓存层
- [ ] 4.4 urlImport requestIdleCallback 分片
- [ ] 4.5 重型依赖 lazy load
- [ ] 4.6 CSS 性能优化
- [ ] 4.7 废弃代码清理
- [ ] 4.8 requestIdleCallback 延迟 migrate
- [ ] 4.9 AuthProvider refreshToken ref 化

### 执行后验证

- [ ] 书架列表加载：浏览器 Network 面板确认响应体积显著减小（sections 已被移除）
- [ ] 阅读进度保存：保存后刷新页面，确认 `completedSections` 和 `completed` 未被清空
- [ ] 首屏加载：Lighthouse 评分和实际 FCP 对比
- [ ] 书签保存：localStorage 中 `touchLocalMigrationData` 调用不再发生
- [ ] `checkRatingEligibility`：确认不再发送重复的 readings 查询
- [ ] font-display: swap 生效：Playfair Display 标题在字体加载期间使用 fallback 字体而不可见闪烁

---

## 六、预期收益

| 指标 | 当前 | 优化后（预估） |
|------|------|--------------|
| 首屏 Bundle 大小 | ~300KB+ JSX | ~80KB（代码分割后） |
| 首次内容绘制 | 含重型依赖解析 | 快 ~50% |
| 书架列表加载 | sections 列在内（数 MB） | 仅 metadata（KB 级） |
| 保存书签 | 4 次 localStorage 同步 IO | 1 次 localStorage + 内存 |
| 保存阅读进度 | 2 次网络往返 | 1 次（RPC） |
| 导入 50 篇文章 | 50 次串行请求 | 1-2 次批量 |
| 全应用重渲染 | 每次 auth 事件 → 7 组件 | 仅状态消费者重渲染 |
| 列表交互（Tab 切换/搜索） | O(n) filter + sort 每渲染 | useMemo 缓存 |
| 数据库查询（列表/书签/去重） | 缺索引 | 索引覆盖 |
| 书签复习区 | 全部书签下载 | 仅 20 条服务端筛选 |
| 字体加载 CLS | 无策略，FOUT 明显 | font-display: swap，文字立即可见 |

---

> 配套诊断详情见 `01_性能分析报告.md`
> 审核反馈及处理记录见 `03_实施前审核反馈处理记录.md`
